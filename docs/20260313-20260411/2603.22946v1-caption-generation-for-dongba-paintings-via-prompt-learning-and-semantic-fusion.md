---
title: Caption Generation for Dongba Paintings via Prompt Learning and Semantic Fusion
title_zh: 基于提示学习与语义融合的东巴画图像描述生成
authors: "Shuangwu Qian, Xiaochan Yuan, Pengfei Liu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22946v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 通过提示学习桥接通用图像与特定文化艺术之间的领域偏移
tldr: 针对纳西族东巴画独特的文化象征意义及主流模型在特定领域表现不佳的问题，本文提出PVGF-DPC框架。该方法结合MobileNetV2编码器与基于BERT初始化的Transformer解码器，通过内容提示模块生成文化感知标签，并利用视觉语义生成融合损失函数进行联合优化。此外，研究者构建了包含9408张图像的东巴画专用数据集，有效提升了自动生成描述的准确性与文化契合度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22946v1/fig-001.webp\", \"caption\": \"Table 2: Comparison of image captioning metrics between PVGF-DPC and baseline methods on the Dongba painting test set. Bold values indicate the best result in each column; “–” denotes missing or unreliable values.\", \"page\": 10, \"index\": 1, \"width\": 896, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.22946v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of image captioning metrics with different encoders. Bold values indicate the best result in each column.\", \"page\": 10, \"index\": 2, \"width\": 683, \"height\": 390}]"
motivation: 传统图像描述模型难以处理东巴画中复杂的文化象征和显著的领域偏移问题。
method: 提出PVGF-DPC框架，利用内容提示模块引导解码器生成文化相关描述，并结合视觉语义生成融合损失进行优化。
result: 构建了包含9408张图像的东巴画数据集，并证明了该模型在生成准确且具有文化深度的描述方面的有效性。
conclusion: 通过提示学习与语义融合，该研究为少数民族文化遗产的数字化保护与自动理解提供了新方案。
---

## 摘要
东巴画是中国西南部纳西族珍贵的绘画遗产，具有丰富的视觉元素层次、鲜艳的色彩以及显著的民族和地域文化象征意义。然而，由于主流图像描述模型在直接应用时存在严重的领域偏移，其自动文本描述在很大程度上仍未得到探索。本文提出了 PVGF-DPC（基于提示与视觉语义生成融合的东巴画图像描述），这是一个编码器-解码器框架，通过集成内容提示模块和一种新颖的视觉语义生成融合损失，旨在弥合通用自然图像描述与东巴艺术中特定文化意象之间的差距。MobileNetV2 编码器提取具有判别性的视觉特征，并将其注入到由预训练 BERT 权重初始化的 10 层 Transformer 解码器的层归一化中；同时，内容提示模块将图像特征向量映射到具有文化意识的标签（如“神灵”、“祭祀图案”或“地狱鬼怪”），并构建后置提示以引导解码器生成主题准确的描述。视觉语义生成融合损失共同优化了提示预测器和描述生成器的交叉熵目标，鼓励模型提取关键的文化和视觉线索，并生成与输入图像语义一致的描述。我们构建了一个专门的东巴画图像描述数据集，包含 9,408 张经过增强的图像，并附带涵盖七个主题类别的文化背景标注。

## Abstract
Dongba paintings, the treasured pictorial legacy of the Naxi people in southwestern China, feature richly layered visual elements, vivid color palettes, and pronounced ethnic and regional cultural symbolism, yet their automatic textual description remains largely unexplored owing to severe domain shift when mainstream captioning models are applied directly. This paper proposes \textbf{PVGF-DPC} (\textit{Prompt and Visual Semantic-Generation Fusion-based Dongba Painting Captioning}), an encoder-decoder framework that integrates a content prompt module with a novel visual semantic-generation fusion loss to bridge the gap between generic natural-image captioning and the culturally specific imagery found in Dongba art. A MobileNetV2 encoder extracts discriminative visual features, which are injected into the layer normalization of a 10-layer Transformer decoder initialized with pretrained BERT weights; meanwhile, the content prompt module maps the image feature vector to culture-aware labels -- such as \emph{deity}, \emph{ritual pattern}, or \emph{hell ghost} -- and constructs a post-prompt that steers the decoder toward thematically accurate descriptions. The visual semantic-generation fusion loss jointly optimizes the cross-entropy objectives of both the prompt predictor and the caption generator, encouraging the model to extract key cultural and visual cues and to produce captions that are semantically aligned with the input image. We construct a dedicated Dongba painting captioning dataset comprising 9{}408 augmented images with culturally grounded annotations spanning seven thematic categories.

---

## 论文详细总结（自动生成）

这篇论文针对纳西族东巴画这一特定文化遗产领域，提出了一种结合提示学习与语义融合的图像描述生成框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的图像描述（Image Captioning）模型在处理具有深厚文化底蕴的艺术作品（如东巴画）时，面临严重的**领域偏移（Domain Shift）**问题。通用模型往往只能描述表层视觉对象，无法捕捉东巴画中特有的宗教象征、神话人物及文化内涵（如“净化瓶”象征吉祥、“白蝙蝠”作为神灵使者等）。
*   **研究动机**：东巴画数据集稀缺，且其独特的视觉风格（粗线条、高对比度色彩）和复杂的语义层次（神灵、祭祀、鬼怪等）使得现有模型容易产生“幻觉”或生成肤浅的描述。因此，需要一种能够融合特定文化知识的生成框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **PVGF-DPC** 框架，其核心思想是通过“内容提示”引导解码器，并利用“融合损失”强化视觉与语义的对齐。
*   **编码器（Encoder）**：采用 **MobileNetV2** 作为骨干网络。选择轻量级 CNN 而非大型 ViT 是为了在有限的数据集上防止过拟合，并平衡计算效率。
*   **内容提示模块（Content Prompt Module）**：
    *   该模块将图像特征向量映射到预定义的文化标签（如：神灵、地狱鬼怪、祭祀图案等）。
    *   根据预测标签构建“后置提示”（Post-prompt），例如：“这是一幅关于[神灵]的东巴画”，将其与文本序列拼接，为解码器提供明确的文化上下文。
*   **解码器（Decoder）**：采用 10 层 **Transformer** 结构，并使用预训练的 **BERT** 权重初始化。
    *   **特征注入**：将图像特征向量注入到每一层 Transformer 的层归一化（Layer Normalization）过程中，实现视觉信息对文本生成的持续调制。
*   **视觉语义生成融合损失（Fusion Loss）**：
    *   公式表达为：$L_{fusion} = \alpha L_{text} + \lambda L_{prompt}$。
    *   通过联合优化描述生成损失（交叉熵）和提示分类损失，强制编码器提取具有文化判别性的视觉特征。

### 3. 实验设计
*   **数据集**：构建了专门的东巴画数据集，包含 7 大主题类别（神灵、鬼怪、禽兽、植物、骑射、舞乐、宗教图案）。通过几何变换、噪声注入和像素操作等增强手段，将原始稀缺样本扩充至 **9408 张图像**。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流及前沿模型，包括：**BLIP、OFA、ClipCap、ViECap、MacCap、DeCap、CgT-GAN、MeaCap** 以及基础的 **ATT+LSTM**。
    *   评价指标：BLEU-1/2/3/4、METEOR、ROUGE、CIDEr。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 2080Ti GPU**。
*   **软件框架**：基于 TensorFlow 和 Keras。
*   **训练细节**：Batch size 为 8，初始学习率为 $1 \times 10^{-5}$，使用 AdamW 优化器，每 8 个 Epoch 进行权重衰减。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比实验。
    *   **编码器对比**：对比了 MobileNetV2、VGG16、ResNet-50、EfficientNetB0 等，验证了轻量级网络在小样本文化遗产领域的优越性。
    *   **消融实验**：分别验证了基础模型（DBC）、仅含融合损失的模型（VGF-DPC）以及全功能模型（PVGF-DPC）的效果。
    *   **深度实验**：测试了 6 到 12 层不同解码器深度对性能的影响。
*   **充分性评价**：实验设计较为全面，涵盖了客观指标对比、主观定性分析（Case Study）以及组件有效性验证，能够客观反映模型在特定领域的性能提升。

### 6. 主要结论与发现
*   **性能领先**：PVGF-DPC 在所有指标上均显著优于基准模型。其中 **CIDEr 得分达到 0.599**，远超第二名 ViECap（0.183），证明了该模型在生成描述的相关性和多样性方面具有压倒性优势。
*   **提示学习的价值**：内容提示模块能有效减少模型在处理陌生文化符号时的“幻觉”现象，使生成的文本不仅符合视觉逻辑，更符合文化常识。
*   **融合损失的作用**：联合优化策略显著提升了编码器捕捉文化关键特征的能力。

### 7. 优点（亮点）
*   **领域针对性强**：首次系统性地解决了东巴画这一非物质文化遗产的自动描述问题。
*   **文化知识集成**：通过 Prompt Learning 将隐性的文化背景知识转化为显性的文本引导，巧妙弥合了领域偏移。
*   **轻量且高效**：模型参数量适中（MobileNetV2 方案约 8.73M 参数），适合在文化遗产保护等实际应用场景中部署。

### 8. 不足与局限
*   **数据集依赖**：虽然进行了数据增强，但原始真实样本量依然较小，可能限制了模型对极少数罕见图案的泛化能力。
*   **提示词粒度**：目前的内容提示主要基于类别标签，对于更复杂的叙事性场景（如多个神灵间的互动），提示词的粒度可能仍显粗糙。
*   **应用限制**：该方法高度依赖于特定领域的专家标注（用于构建提示模块），在迁移到其他缺乏标注的文化领域时，前期准备成本较高。

（完）
