---
title: "GridVAD: Open-Set Video Anomaly Detection via Spatial Reasoning over Stratified Frame Grids"
title_zh: GridVAD：通过分层帧网格进行空间推理的开放集视频异常检测
authors: "Mohamed Eltahir, Ahmed O. Ibrahim, Obada Siralkhatim, Tabarak Abdallah, Sondos Mohamed"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25467v1"
tags: ["query:vlm-da"]
score: 7.0
evidence: 无需领域特定训练，将VLM用作异常提议器
tldr: 针对视频异常检测中视觉语言模型（VLM）易产生幻觉和漏检的问题，本文提出GridVAD，一种无需训练的“提议-定位-传播”框架。该方法将VLM定位为异常提议器，通过在分层网格图像上进行空间推理生成自然语言描述，并利用自一致性整合机制过滤幻觉，最后结合Grounding DINO和SAM2实现像素级掩码生成。GridVAD在多个基准测试中刷新了零样本检测性能，显著提升了检测精度与计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25467v1/fig-001.webp\", \"caption\": \"Fig. 2: Qualitative comparison with anomaly detection models adapted for video anomaly detection on the UCSD Ped2 dataset.\", \"page\": 9, \"index\": 1, \"width\": 862, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2603.25467v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison on UCSD Ped2. Baseline numbers are transcribed from the TAO paper; our results are in the last row.\", \"page\": 7, \"index\": 2, \"width\": 964, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.25467v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of the GridVAD pipeline. We convert a video into M stratified frame grids and process them by a VLM to generate anomaly proposals. A semantic consolidation step filters inconsistent proposals. The anomaly description is input to a prompt-based object detection model (Grounding DINO) to localize the anomaly, and SAM2 propagates it across frames to produce temporally consistent pixel-level anomaly masks.\", \"page\": 3, \"index\": 3, \"width\": 976, \"height\": 579}]"
motivation: 直接将视觉语言模型用于视频异常检测时，由于缺乏校准的异常先验，容易导致严重的幻觉误报或漏检。
method: 采用“提议-定位-传播”原则，利用VLM在分层网格上生成异常提议，通过自一致性过滤后，结合检测与分割模型实现时空定位。
result: 在UCSD Ped2数据集上取得了77.59的最高Pixel-AUROC，在物体级指标上优于其他零样本方法5倍以上，且推理效率提升了2.7倍。
conclusion: GridVAD证明了将VLM作为异常提议器并结合空间推理与时序传播，是实现高效、精准的开放集视频异常检测的有效途径。
---

## 摘要
视觉语言模型（VLM）是强大的开放集推理器，但直接将其作为视频监控中的异常检测器却十分脆弱：在缺乏校准的异常先验的情况下，它们往往在漏检和幻觉导致的虚警之间交替。我们认为问题不在于 VLM 本身，而在于其使用方式。VLM 应当充当异常提议器，生成开放集候选描述，随后由专门构建的空间和时间模块进行定位和跟踪。我们在 GridVAD 中实例化了这种“提议-定位-传播”原则。GridVAD 是一个无需训练的流水线，无需任何特定领域训练即可生成像素级异常掩码。VLM 对视频片段的分层网格表示进行推理，以生成自然语言异常提议。自一致性整合（SCC）通过仅保留在多次独立采样中重复出现的提议来过滤幻觉。Grounding DINO 将每个保留的提议锚定到边界框，而 SAM2 则将其作为密集掩码在异常区间内进行传播。无论视频长度如何，每个视频片段的 VLM 预算固定为 M+1 次调用，其中 M 可根据所需的提议数量进行设置。在 UCSD Ped2 数据集上，GridVAD 在所有对比方法中实现了最高的 Pixel-AUROC（77.59），甚至超过了经过部分微调的 TAO（75.11），并且在物体级 RBDC 指标上比其他零样本方法高出 5 倍以上。消融实验表明，SCC 提供了一种可控的精确率-召回率权衡：过滤操作以适度的物体级召回率为代价，提升了所有像素级指标。效率实验表明，GridVAD 的调用效率是逐帧均匀 VLM 查询的 2.7 倍，同时还能生成密集分割掩码。代码和定性视频结果可在 https://gridvad.github.io 获取。

## Abstract
Vision-Language Models (VLMs) are powerful open-set reasoners, yet their direct use as anomaly detectors in video surveillance is fragile: without calibrated anomaly priors, they alternate between missed detections and hallucinated false alarms. We argue the problem is not the VLM itself but how it is used. VLMs should function as anomaly proposers, generating open-set candidate descriptions that are then grounded and tracked by purpose-built spatial and temporal modules. We instantiate this propose-ground-propagate principle in GridVAD, a training-free pipeline that produces pixel-level anomaly masks without any domain-specific training. A VLM reasons over stratified grid representations of video clips to generate natural-language anomaly proposals. Self-Consistency Consolidation (SCC) filters hallucinations by retaining only proposals that recur across multiple independent samplings. Grounding DINO anchors each surviving proposal to a bounding box, and SAM2 propagates it as a dense mask through the anomaly interval. The per-clip VLM budget is fixed at M+1 calls regardless of video length, where M can be set according to the proposals needed. On UCSD Ped2, GridVAD achieves the highest Pixel-AUROC (77.59) among all compared methods, surpassing even the partially fine-tuned TAO (75.11) and outperforms other zero-shot approaches on object-level RBDC by over 5x. Ablations reveal that SCC provides a controllable precision-recall tradeoff: filtering improves all pixel level metrics at a modest cost in object-level recall. Efficiency experiments show GridVAD is 2.7x more call-efficient than uniform per-frame VLM querying while additionally producing dense segmentation masks.Code and qualitative video results are available at https://gridvad.github.io.