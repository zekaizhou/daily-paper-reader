---
title: Compositional Context Fine-Tuning Vision-Language Model for Complex Assembly Action Understanding from Videos
title_zh: 用于视频复杂装配动作理解的组合上下文微调视觉语言模型
authors: "Hao Zheng, Jinyi Huang, Tiantian Zheng, Xun Xu, Tuka Alhanai"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10797v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 针对复杂装配动作领域微调VLM
tldr: 针对人机协作装配中动作识别难的问题，本文提出了一种组合上下文微调（CCFT）方法。该方法将复杂的装配动作分解为动词、对象和工具三个语义要素，并利用视觉语言模型通过问答形式进行识别。此外，引入了层分区交替训练（LP-AT）策略，通过特定要素的低秩适配器减少多任务干扰。实验表明，该方法在多个装配视频数据集上表现优异，且具有良好的可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10797v1/fig-001.webp\", \"caption\": \"TABLE I: Action Recognition Accuracy on HA-ViD-VQA and IKEA-ASM-VQA. The accuracy (%) for left-hand and right-hand action recognition is reported separately under side, front, and top views for HA-ViD-VQA. Our method outperforms baselines.\", \"page\": 5, \"index\": 1, \"width\": 394, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-002.webp\", \"caption\": \"Fig. 2: Qualitative Comparison of Assembly Action Understanding Outputs from Our Fine-Tuned Model, the Base Model, and the Base Model with Contextual Prompt.\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 740}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-003.webp\", \"caption\": \"Fig. 1: The Overall Framework of Compositional Context Fine-Tuning with Layer-Partitioned Alternating Training.\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 442}]"
motivation: 传统的装配动作识别受限于细微动作和复杂的手物交互，难以实现精确且具有可解释性的理解。
method: 提出CCFT方法将动作分解为语义要素，并结合LP-AT策略利用特定层的LoRA适配器进行交替训练以优化多任务学习。
result: 在HA-ViD-VQA和IKEA-ASM-VQA数据集上的实验证明，该方法在识别精度上显著优于现有的强动作识别基准模型。
conclusion: 该研究通过分解语义要素和优化多任务微调，为复杂装配场景下的视觉语言模型应用提供了高效且可解释的解决方案。
---

## 摘要
装配动作理解是实现高效人机协作装配的关键使能技术，但由于动作细微且存在细粒度的手物交互，该任务仍具挑战性。我们通过组合上下文微调（Compositional Context Fine-Tuning, CCFT）将视觉语言模型（VLM）应用于这一挑战领域。该方法将装配动作分解为语义元素（动词、物体、工具），并利用模板化问答对微调 VLM 以识别各动作元素，从而确保输出的近乎确定性。为了在有限数据下实现高效且有效的多任务学习，本文提出了一种层分区交替训练（Layer-Partitioned Alternating Training, LP-AT）方法，该方法通过元素特定的低秩适配器（low-rank adapters）分配不同的模型层来识别特定的动作元素。LP-AT 在各元素特定适配器之间交替进行权重更新，在减少跨任务干扰的同时实现了针对每个适配器的超参数优化。此外，我们基于现有的装配视频数据集构建了 HA-ViD-VQA 和 IKEA-ASM-VQA 数据集。在这些数据集上的广泛实验表明，我们的方法始终优于强动作识别基线模型，并能提供可解释的元素级预测，从而支持多种下游应用。

## Abstract
Assembly action understanding is a key enabler for effective human-robot collaborative assembly, yet it remains challenging due to subtle motions and fine-grained hand-object interactions. We adapt vision-language models (VLMs) to this challenging domain with Compositional Context Fine-Tuning (CCFT), a method that decomposes assembly actions into semantic elements (Verb, Object, Tool) and fine-tunes VLMs to recognize each action element using templated question-answering pairs. This approach ensures near-deterministic outputs. To enable efficient and effective multi-task learning under limited data, a Layer-Partitioned Alternating Training (LP-AT) method is presented, which assigns distinct model layers to recognize specific action elements through element-specific low-rank adapters. LP-AT alternates weight updates across element-specific adapters, reducing cross-task interference while enabling per-adapter hyperparameter optimization. Furthermore, we create HA-ViD-VQA and IKEA-ASM-VQA datasets from existing assembly video datasets. Extensive experiments on these datasets demonstrate that our method consistently outperforms strong action recognition baselines while providing interpretable element-level predictions that can support diverse downstream applications.

---

## 论文详细总结（自动生成）

### 论文总结：用于视频复杂装配动作理解的组合上下文微调视觉语言模型

#### 1. 核心问题与整体含义（研究动机和背景）
在人机协作装配（HRC）中，机器人需要精准理解人类的装配动作。然而，装配动作具有**动作细微、手物交互复杂、视觉相似但语义对立**（如拧紧与拧松）等挑战。现有的视觉语言模型（VLM）虽具备强大的推理能力，但多用于通用场景，且其生成式输出具有随机性，难以满足工业制造对**确定性**和**可解释性**的要求。本文旨在通过微调 VLM，使其能够精确、确定地理解复杂的装配动作。

#### 2. 方法论：核心思想与关键技术
论文提出了两个核心技术组件：
*   **组合上下文微调（CCFT）：**
    *   **核心思想：** 将复杂的整体动作分解为三个语义要素：**动词（Verb）**、**操作对象（Object）**和**工具（Tool）**。
    *   **技术细节：** 采用模板化的视觉问答（VQA）形式，将动作识别转化为对特定要素的查询。通过限制答案空间，使 VLM 产生近乎确定性的输出，并提供元素级的可解释预测。
*   **层分区交替训练（LP-AT）：**
    *   **核心思想：** 针对多任务学习中的干扰问题，将 VLM 的层进行分区，不同层组负责不同的语义要素。
    *   **关键细节：** 为每个要素分配独立的 LoRA 适配器，并将其应用于不相交的层组（如前 10 层负责动词，中间 14 层负责对象）。训练时采用**交替优化**策略：在一个 epoch 内，依次更新各要素的适配器并合并回基础模型，从而减少任务间的梯度冲突，并允许为每个要素独立设置超参数（如学习率、秩）。

#### 3. 实验设计
*   **数据集：** 基于现有数据集重新构建了两个 VQA 格式数据集：
    *   **HA-ViD-VQA：** 包含双手动作为主的 4 要素标注（动词、被操作物、目标物、工具），涵盖多视角。
    *   **IKEA-ASM-VQA：** 包含家具装配的 2 要素标注（动词、对象）。
*   **基准模型（Benchmark）：** 选择了三种强力动作识别模型：**TSM**（基于 CNN）、**UniFormerV2**（基于 Transformer）和 **VideoMAE V2**（基于预训练掩码自编码器）。
*   **对比实验：** 将本文方法与上述基准模型在整体动作识别准确率上进行对比，并进行了消融实验验证 CCFT 和 LP-AT 的有效性。

#### 4. 资源与算力
*   **硬件：** 使用了 **4 台 NVIDIA RTX A6000 GPU**（每台 48GB 显存）。
*   **训练细节：** 基础模型为 Qwen2.5-VL-7B，仅微调语言解码器（28 层）。视频采样率为 2 fps，最大帧数 76 帧。训练时长为 40 个 epoch，batch size 为 1。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个不同风格的数据集（微观操作 vs. 宏观家具装配）上进行了测试，涵盖了左/右手、侧视/前视/顶视等多种变量。
*   **消融实验：** 做了多组深入的消融实验，包括：
    1. 组合式 vs. 非组合式微调。
    2. 层分区适配器 vs. 共享适配器。
    3. 交替训练 vs. 独立训练 vs. 顺序训练。
*   **评价：** 实验设计较为充分且客观。通过对比不同训练策略（如揭示了顺序训练会导致灾难性遗忘），有力证明了 LP-AT 的必要性。

#### 6. 主要结论与发现
*   **性能领先：** 本文方法在所有数据集和视角下均显著优于传统的动作识别基准模型。
*   **任务复杂度差异：** 工具识别准确率最高（视觉特征明显），而动词和对象的识别难度取决于动作的粒度（微观动作中动词更易识别，宏观动作中对象更易识别）。
*   **LP-AT 的优越性：** 相比于共享适配器或简单的多任务微调，层分区和交替训练能有效缓解参数干扰，提升模型稳定性。

#### 7. 优点（亮点）
*   **确定性与可解释性：** 通过 VQA 模板解决了 VLM 在工业应用中“幻觉”和输出不稳定的痛点。
*   **高效微调：** LP-AT 策略在有限数据下实现了高效的多任务适配，且具有很强的模块化灵活性。
*   **数据集贡献：** 填补了装配领域缺乏结构化 VQA 数据集的空白。

#### 8. 不足与局限
*   **实时性挑战：** 尽管使用了 LoRA，但基于 7B 参数规模的 VLM 进行视频推理，在实时人机协作任务中可能存在延迟压力。
*   **层分区依赖经验：** 目前的层分配策略（如 10:14:4）主要基于经验和启发式规则，缺乏自动化的理论指导。
*   **视觉编码器冻结：** 由于数据量小，实验中冻结了视觉编码器，这可能限制了模型捕捉极细微手物交互特征的能力。
*   **组合逻辑简单：** 目前仅通过简单的字符串拼接组合要素，缺乏更高级的逻辑推理来纠正单个要素预测错误导致的整体失败。

（完）
