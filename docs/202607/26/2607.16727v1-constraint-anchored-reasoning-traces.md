---
title: Constraint-Anchored Reasoning Traces
title_zh: 约束锚定推理轨迹
authors: "Zehua Cheng, Wei Dai, Jiahao Sun"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16727v1"
tags: ["query:vlm-da"]
score: 6.0
evidence: 改进多模态大语言模型的推理能力
tldr: 针对多模态大模型在思维链推理中容易出现的“错误滚雪球”现象，本文提出CART框架。该框架通过在自然语言推理中嵌入可机器检查的符号约束锚点，并结合神经接地与布尔约束传播模块进行实时验证。一旦检测到逻辑矛盾，系统将自动回溯至一致状态，有效阻止错误累积。实验表明，CART显著降低了错误传播率，提升了推理准确性且推理开销较小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16727v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the CART framework. Given an image and question, the MLLM generates interleaved reasoning steps and symbolic constraint anchors. The Constraint Propagation Module (CPM) verifies each anchor via a neural grounding head and Boolean Constraint Propagation. When a violation is detected, the backtrack controller reverts to the last consistent checkpoint and resumes generation with a <RETRY> token.\", \"page\": 3, \"index\": 1, \"width\": 1056, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2607.16727v1/fig-002.webp\", \"caption\": \"Table 3: Inference latency profiling across backbone scales and batch sizes on a single A100-80GB GPU. Overhead factor is the ratio of CART latency to base autoregressive latency, showing that the constraint propagationmodule adds atmost 18% wall-clock overhead. Bold = lowest overhead per backbone.\", \"page\": 7, \"index\": 2, \"width\": 502, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2607.16727v1/fig-003.webp\", \"caption\": \"Table 2: Ablation studies on LLaVA-NeXT-13B. Top block: subtractive ablations removing individual CART components. Middle block: additive build-up from fine-tuned baseline. Bottom block: sensitivity to retry budget 𝐵 and visual grounding threshold 𝜏𝑣 . Snowball rate, constraint violation rate (CVR), mean trace length, and per-instance latency are measured on a 2,000-instance diagnostic subset of GQA-testdev. Bold = best in column.\", \"page\": 7, \"index\": 3, \"width\": 836, \"height\": 577}]"
motivation: 解决多模态大模型在长链条推理中因早期单一错误导致后续步骤全面溃败的“错误滚雪球”问题。
method: 提出CART框架，将自然语言推理与符号约束交织，利用神经接地头和布尔传播进行实时验证与回溯纠错。
result: "在多个基准测试中将错误滚雪球率从0.65降至0.14，GQA准确率提升4.6个百分点，且推理额外开销低于18%。"
conclusion: 通过引入符号约束锚点和动态回溯机制，CART为增强多模态大模型的推理可靠性和逻辑一致性提供了一种高效的神经符号方案。
---

## 摘要
自回归多模态大语言模型（MLLM）面临错误滚雪球效应：思维链（CoT）轨迹早期的一个错误推断会破坏所有后续推理。我们发现，在最先进的开源 MLLM 中，一旦发生第一个错误，在 65% 的情况下推理会级联导致后续所有步骤失败（我们将这一指标称为“滚雪球率”）。现有的缓解措施——如采样多条推理链、事后自我验证或全程序合成——要么缺乏符号落地（symbolic grounding），要么发现错误太晚，或者牺牲了自然语言推理的灵活性。我们提出了约束锚定推理轨迹（CART），这是一个神经符号框架，通过训练 MLLM 将自然语言推理步骤与符号约束断言交织在一起：这些断言是关于视觉内容的轻量级、机器可检查的陈述（例如，count(red_objects) = 3）。一个双管齐下的约束传播模块——结合了学习到的神经落地头与布尔约束传播——持续根据提取的视觉特征验证这些锚点，并检查它们的相互逻辑一致性。当检测到矛盾时，回溯控制器会停止生成并恢复到最后一个一致的检查点，从而防止错误传播。变频发射机制允许模型自适应地控制锚点密度，避免轨迹冗余。我们通过使用源自场景图的真值约束标注增强 GQA、CLEVR-CoGenT 和 VCR 数据集，构建了 218K 个训练实例，并通过 LoRA 微调了开源 MLLM（LLaVA-NeXT、Qwen2-VL）。在五个基准测试中，CART 将滚雪球率从 0.65 降低到 0.14，在 GQA 准确率上比仅训练基线提高了 4.6 个百分点，并在 POPE 上达到了 89.1 的 F1 分数，而推理开销最高仅为 18%。

## Abstract
Autoregressive multimodal large language models (MLLMs) suffer from error snowballing: a single incorrect inference early in a chainof-thought (CoT) trace corrupts all downstream reasoning. We find that in state-of-the-art open-source MLLMs, once the first error occurs, the reasoning cascades into failure across all remaining steps in 65% of such cases (a metric we term the snowball rate). Existing mitigations-sampling multiple chains, post-hoc self-verification, or full program synthesis-either lack symbolic grounding, catch errors too late, or sacrifice the flexibility of natural language reasoning. We propose Constraint-Anchored Reasoning Traces (CART), a neuro-symbolic framework that trains MLLMs to interleave natural language reasoning steps with symbolic constraint assertions: lightweight, machine-checkable statements about visual content (e.g., count(red_objects) = 3). A dual-pronged Constraint Propagation Module-combining a learned neural grounding head with Boolean Constraint Propagation-continuously verifies these anchors against extracted visual features and checks their mutual logical consistency. When a contradiction is detected, a backtrack controller halts generation and reverts to the last consistent checkpoint, preventing error propagation. A variable-frequency emission mechanism allows the model to adaptively control anchor density, avoiding trace bloat. We construct 218K training instances by augmenting GQA, CLEVR-CoGenT, and VCR with ground-truth constraint annotations derived from scene graphs, and fine-tune open-source MLLMs (LLaVA-NeXT, Qwen2-VL) via LoRA. On five benchmarks, CART reduces the snowball rate from 0.65 to 0.14, improves GQA accuracy by +4.6 percentage points over trainingonly baselines, and achieves 89.1 F1 on POPE-all with at most 18% inference overhead.