---
title: "PRiSM: Prototype Regularization for Few-Shot VLMs"
title_zh: PRiSM：少样本视觉语言模型的原型正则化
authors: "Ghassen Baklouti, Omprakash Chakraborty, Jose Dolz, Ismail Ben Ayed"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17820v1"
tags: ["query:vlm-da"]
score: 10.0
evidence: 视觉语言模型（VLM）的少样本自适应方法
tldr: 针对视觉语言模型（VLM）在少样本自适应中过度依赖类别平衡假设的问题，本文提出了一个更符合现实的非平衡基准测试。为此，作者开发了 PRiSM 插件模块，通过最大化类间距离、特征对齐和原型保真度的多项损失函数进行原型正则化，并引入高效的 Majorize-Minimize 优化器，显著提升了模型在复杂分布下的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17820v1/fig-001.webp\", \"caption\": \"Table 1: Severe imbalance with high effective classes. We report Top-1 accuracy (%) across 10 datasets. Differences with base methods are shown as subscripts.\", \"page\": 7, \"index\": 1, \"width\": 830, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-002.webp\", \"caption\": \"Table 5: Severe imbalance with low effective classes. We report Top-1 accuracy (%) across 10 datasets. Improvements over base methods are shown as subscripts; positive gains are highlighted in green.\", \"page\": 14, \"index\": 2, \"width\": 830, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-003.webp\", \"caption\": \"Figure 1: Few-shot methods under realistic imbalance and effectiveness of prototype rectification. (Left) Variation of mean Top-1 accuracy across 10 datasets with the number of shots under severe imbalance. Contrary to standard few-shot assumptions, increasing the number of support samples does not necessarily improve performance; instead, several strong baselines degrade as additional samples amplify majority-class bias in prototype estimation. (Right) Performance at 16-shot under near balanced and severe imbalance settings. Our proposed prototype rectification (PRiSM) consistently improves all baselines, with particularly large gains under severe imbalance.\", \"page\": 3, \"index\": 3, \"width\": 780, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-004.webp\", \"caption\": \"Table 4: Near-balanced setting with low effective classes. We report Top-1 accuracy (%) across 10 datasets. Improvements over base methods are shown as subscripts; positive gains are highlighted in green.\", \"page\": 13, \"index\": 4, \"width\": 830, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-005.webp\", \"caption\": \"Table 3: Severe imbalance and near balanced settings with low effective classes. We report mean Top-1 accuracy (%) across 10 datasets. Improvements over base methods are shown as subscripts; positive gains are highlighted in green.\", \"page\": 8, \"index\": 5, \"width\": 582, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-006.webp\", \"caption\": \"Table 2: Near-balanced setting with high effective classes. Top-1 accuracy (%) across 10 datasets.\", \"page\": 8, \"index\": 6, \"width\": 829, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-007.webp\", \"caption\": \"Figure 3: Effect of the Dirichlet concentration parameter δ on class imbalance across three different classes. Smaller δ values yield more skewed class distributions, while larger values approach uniform sampling.\", \"page\": 12, \"index\": 7, \"width\": 568, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2607.17820v1/fig-008.webp\", \"caption\": \"Figure 2: Prototype-block performance as a function of different values of the learning rates. The dotted vertical lines show the Lipschitz-based learning rate.\", \"page\": 9, \"index\": 8, \"width\": 829, \"height\": 204}]"
motivation: 现有的少样本自适应方法通常假设类别平衡，但在现实的非平衡和多类别场景下，现有 SOTA 方法的性能会显著下降。
method: 提出 PRiSM 正则化框架，通过优化包含类间距离最大化、特征对齐和原型保真度的多项损失函数，并采用基于 Gershgorin 圆定理的高效优化器进行求解。
result: 实验表明，PRiSM 作为即插即用模块，在处理严重类别不平衡和高类别数量的任务时，能显著提升多种免训练基线方法的表现。
conclusion: PRiSM 有效解决了 VLM 在现实少样本场景下的适应性问题，为非平衡分布下的模型优化提供了高效且鲁棒的解决方案。
---

## 摘要
在视觉语言模型（VLMs）的背景下，无需训练的少样本自适应方法近年来受到了广泛关注。然而，目前的基准测试依赖于对自适应数据统计特性的强假设，例如类别平衡。我们对这些简化假设提出质疑，并引入了一个更真实的基准，通过狄利克雷采样（Dirichlet sampling）改变少样本任务中的类别平衡程度和有效类别数量。令人惊讶的是，在我们的设置下，我们观察到最先进方法的性能大幅下降，且随着标记样本数量的增加，下降幅度更为显著。为了缓解这一问题，我们引入了 PRiSM，这是一种类别原型正则化方法，可以作为即插即用模块部署在任何现有基准方法之上，从而显著提高性能。我们的方法优化了一种新型的多项损失函数，其中包括一个最大化类间成对距离的正则化项，以及促进支持特征对齐和对基准原型保真度的附加项。此外，我们为该目标函数引入了一种有效且计算高效的分块主化最小化（Majorize-Minimize）优化器。具体而言，我们推导出了一个有效的块级 Lipschitz 常数（即 Hessian 矩阵谱范数的界限），该常数可以通过盖尔圆定理（Gershgorin circle theorem）高效计算。大量实验表明，PRiSM 改进了多个无需训练的基准方法，在处理严重的类别不平衡和大量类别时取得了显著收益。

## Abstract
Training-free few-shot adaptation methods have gained significant attention recently in the context of Vision-language Models (VLMs). Yet, current benchmarks rely on strong assumptions about the statistics of the adaptation data, e.g., class balance. We question these simplifying assumptions and introduce a more realistic benchmark that varies both the levels of class balance and the effective number of classes in few-shot tasks via Dirichlet sampling. Surprisingly, under our setting, we observe substantial drops in the performances of state-of-the-art methods, more so when the number of labeled samples increases. To mitigate this, we introduce PRiSM, a class-prototype regularization that can be deployed as a plug and play module on top of any existing baseline method, significantly improving performances. Our method optimizes a novel multi-term loss, which includes a regularizer maximizing inter-class pairwise distances, along with additional terms promoting support-feature alignment and fidelity to the baseline prototypes. Furthermore, we introduce an effective and computationally efficient block Majorize-Minimize optimizer for our objective. More specifically, we derive a valid blockwise Lipschitz constant (i.e., a bound on the Hessian's spectral norm), which can be computed efficiently via the Gershgorin circle theorem. Extensive experiments show that PRiSM improves several training-free baselines, with large gains when dealing with severe class imbalance and high numbers of classes.

---

## 论文详细总结（自动生成）

以下是对论文《PRiSM: Prototype Regularization for Few-Shot VLMs》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：视觉语言模型（VLM，如 CLIP）在下游任务的少样本自适应（Few-shot Adaptation）中表现出色。目前的评估协议通常假设类别是平衡的（即每个类别的样本数相同）。
*   **核心问题**：论文指出，现实场景中的数据往往是高度不平衡的。作者发现，在类别不平衡的情况下，现有的 SOTA 方法（如 Tip-Adapter, ProKeR 等）性能会大幅下降。令人惊讶的是，在严重不平衡时，增加样本数（Shots）反而可能因为放大了多数类的偏差而导致性能进一步恶化。
*   **研究动机**：开发一种能够纠正原型偏差、且无需依赖昂贵的验证集进行超参数调优的鲁棒自适应方法。

### 2. 方法论：PRiSM 框架
*   **核心思想**：PRiSM（Prototype Regularization for Few-Shot VLMs）是一个即插即用的原型正则化模块。它通过优化一个新的损失函数来校准由基线方法生成的类别原型。
*   **关键技术细节**：
    *   **多项损失函数**：包含三个部分：
        1.  **支持特征对齐项**：鼓励校准后的原型靠近对应类别的视觉特征均值。
        2.  **基线原型保真项**：防止校准后的原型偏离原始基线方法太远。
        3.  **类间分离正则项**：通过最大化类间成对距离，减少类别间的干扰，防止原型向多数类坍缩。
    *   **优化算法（Block MM）**：
        *   采用**分块主化最小化（Block Majorize-Minimize）**算法，交替更新“类别原型”和“视觉编码器参数”。
        *   **理论创新**：利用**盖尔圆定理（Gershgorin Circle Theorem）**推导出 Hessian 矩阵谱范数的上界，从而获得一个有效的 **Lipschitz 常数**。这使得算法拥有一个理论保证的“隐式步长”，无需通过验证集搜索学习率。
    *   **参数高效微调**：使用 LoRA 对视觉编码器的最后几层进行轻量化更新。

### 3. 实验设计
*   **数据集**：涵盖 10 个广泛使用的图像分类基准，包括 Oxford-Pets、Stanford Cars、EuroSAT、FGVC Aircraft 等，涉及细粒度识别、纹理、场景和通用对象。
*   **新基准测试（Realistic Benchmark）**：
    *   使用**狄利克雷采样（Dirichlet Sampling）**模拟现实分布。
    *   设置了四种场景：低/高类别覆盖率 × 严重不平衡/近平衡分布。
*   **对比方法**：在四种代表性免训练方法上作为插件测试：Tip-Adapter、GDA、APE 和 ProKeR。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA H100 SXM5 GPU** 上完成。
*   **训练细节**：PRiSM 优化过程非常高效，仅需 3 轮交替迭代。每轮包含 1 次原型更新和 10 次 LoRA 更新。
*   **验证集使用**：作者强调其方法是 **Validation-free** 的，仅在 ImageNet 上确定了一组通用超参数，随后直接应用于所有 10 个测试数据集，未针对特定任务调优。

### 5. 实验数量与充分性
*   **实验规模**：每个实验结果均基于 **400 个随机采样的任务**取平均值，保证了统计显著性。
*   **覆盖范围**：涵盖了从 2-shot 到 16-shot 的不同数据量，以及多种不平衡程度。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了平均性能，还详细列出了在 10 个数据集上的具体表现，并对比了不同有效类别数量下的鲁棒性，充分证明了 PRiSM 的普适性。

### 6. 主要结论与发现
*   **性能提升**：PRiSM 在所有基线方法上均实现了显著提升。在严重不平衡的 16-shot 场景下，Tip-Adapter 的平均准确率从 33.25% 提升至 68.30%（+35.05%）。
*   **纠正退化**：成功解决了“样本越多性能越差”的异常退化现象。
*   **方法一致性**：即使是较弱的基线方法，在经过 PRiSM 校准后，也能达到甚至超过更复杂的 SOTA 方法的水平。

### 7. 优点与亮点
*   **现实性强**：挑战了学术界常用的类别平衡假设，提出了更符合工业界实际的评估协议。
*   **数学严谨**：引入 Lipschitz 常数和 MM 优化器，解决了深度学习中令人头疼的学习率调优问题，具有很强的理论支撑。
*   **即插即用**：作为后处理模块，不改变原有方法的架构，易于集成到现有管线中。

### 8. 不足与局限
*   **非凸性风险**：尽管使用了 MM 算法，但目标函数本质上是非凸的，理论上仍可能陷入局部最优（虽然实验证明其结果非常稳定）。
*   **依赖基线**：PRiSM 的效果仍部分取决于初始原型的质量，如果基线方法生成的初始原型极度糟糕，正则化的提升空间可能会受限。
*   **计算开销**：虽然比全量微调轻量，但相比于纯粹的“免训练”推理（如原始 Tip-Adapter），PRiSM 引入了额外的迭代优化步骤，增加了推理前的自适应耗时。

（完）
