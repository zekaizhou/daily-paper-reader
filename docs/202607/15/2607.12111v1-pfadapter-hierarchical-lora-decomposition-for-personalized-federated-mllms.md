---
title: "PFAdapter: Hierarchical LoRA Decomposition for Personalized Federated MLLMs"
title_zh: PFAdapter：面向个性化联邦多模态大语言模型的分层 LoRA 分解
authors: "Jing Liu, Kun Yang, Yan Wang, Dingkang Yang, Xiaoshuai Hao, Wei Zhang, Yang Liu, Wei Zhou"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12111v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 在异构网络条件下使用LoRA对MLLM进行本地自适应
tldr: "针对分布式网络中多模态大语言模型（MLLM）联邦微调面临的全局知识聚合与本地个性化失衡及通信开销大等挑战，本文提出PFAdapter框架。该框架通过分层LoRA分解，将适配器参数划分为全局共享（Q、K投影）和本地私有（V、O投影）部分，并结合正交正则化防止特征冗余。实验表明，该方法在降低50%通信成本的同时，显著提升了多项边缘智能任务的准确率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12111v1/fig-001.webp\", \"caption\": \"TABLE I: Performance comparison across datasets on the aligned-modal scenario (α = 0.5). Learned federated baselines are reported as mean ± standard deviation over three seeds, and best results are highlighted in bold.\", \"page\": 8, \"index\": 1, \"width\": 1060, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2607.12111v1/fig-002.webp\", \"caption\": \"Fig. 2: Architecture of PFAdapter. The framework decomposes LoRA adapters into global-shared components (qp, kp) and local-private components (vp, op). Orthogonality regularization enforces knowledge disentanglement between these sets.\", \"page\": 5, \"index\": 2, \"width\": 1070, \"height\": 349}]"
motivation: 传统的联邦学习协议在处理异构网络下的多模态大模型时，难以有效区分全局通用特征与本地特定知识，且通信负担沉重。
method: 提出分层LoRA分解机制，将Q、K投影设为全局同步以捕获通用语义，将V、O投影保留在本地进行个性化适配，并引入正交正则化约束。
result: "在多个数据集上相比现有基准提升了2.4%至4.8%的准确率，并减少了近50%的通信开销。"
conclusion: PFAdapter为资源受限的通信网络中部署智能体AI提供了一种高效且兼顾个性化的联邦学习解决方案。
---

## 摘要
代理式人工智能（Agentic AI）系统正通过部署能够在维护网络边缘数据隐私的同时进行协作学习的自主智能代理，重塑通信与网络领域。在分布式网络环境中，多模态大语言模型（MLLMs）作为边缘设备的认知引擎，但在异构网络条件下，联邦微调在平衡全局知识聚合与本地自适应方面面临重大挑战。传统的联邦协议通常依赖于统一的参数聚合，这会将领域不变特征与客户端特定的细微差别混为一谈，从而导致个性化效果欠佳以及过高的通信开销。为应对这些挑战，我们提出了 PFAdapter，这是一个通信高效的框架，引入了分层 LoRA 分解，将适配器参数显式分离为全局共享组件和本地私有组件。查询（Query）和键（Key）投影被分配用于全局同步，以捕获跨网络的通用多模态语义，而值（Value）和输出（Output）投影则保持本地化，以进行边缘特定的自适应。此外，基于 Frobenius 范数的正交正则化强制执行这些组件之间的严格分离，防止冗余特征学习。选择性聚合协议仅在联邦网络中同步全局共享组件，从而保留本地专业知识并将通信成本降低近 50%。在 VQA-RAD、SLAKE、Hateful Memes 和 CrisisMMD 数据集上的广泛实验表明，PFAdapter 始终优于最先进的基准模型，在各种边缘智能任务中实现了 2.4% 至 4.8% 的准确率提升。因此，我们的框架为资源受限通信网络中的代理式人工智能部署建立了一种高效的解决方案。

## Abstract
Agentic AI systems are reshaping communications and networking by deploying autonomous intelligent agents capable of collaborative learning while maintaining data privacy at network edges. Within distributed network environments, Multimodal Large Language Models (MLLMs) serve as cognitive engines for edge devices, yet federated fine-tuning faces substantial challenges in balancing global knowledge aggregation with local adaptation under heterogeneous network conditions. Conventional federated protocols typically rely on uniform parameter aggregation, which conflates domain-invariant features with client-specific nuances, thereby resulting in suboptimal personalization and excessive communication overhead. To address these challenges, we propose PFAdapter, a communication-efficient framework introducing hierarchical LoRA decomposition to explicitly separate adapter parameters into global-shared and local-private components. Query and key projections are assigned to global synchronization for capturing universal multimodal semantics across the network, while value and output projections remain localized for edge-specific adaptation. Additionally, orthogonality regularization based on the Frobenius norm enforces strict separation between these components, preventing redundant feature learning. Selective aggregation protocols synchronize only global-shared components across the federated network, preserving local expertise and reducing communication costs by nearly 50%. Extensive experiments on VQA-RAD, SLAKE, Hateful Memes, and CrisisMMD datasets demonstrate that PFAdapter consistently outperforms state-of-the-art baselines, achieving accuracy improvements ranging from 2.4% to 4.8% across diverse edge intelligence tasks. Consequently, our framework establishes an efficient solution for agentic AI deployment in resource-constrained communication networks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PFAdapter** 的框架，旨在解决在分布式边缘网络中对多模态大语言模型（MLLM）进行个性化联邦学习时的效率与性能平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着代理式人工智能（Agentic AI）的发展，在边缘设备上部署 MLLM 成为趋势。联邦学习（FL）允许在保护隐私的前提下进行协作训练。
*   **核心挑战**：
    *   **全局与本地的矛盾**：传统的联邦协议采用统一聚合，容易导致“权重冲刷”（Weight Washing），即本地特定的知识被全局平均化稀释，导致个性化效果差。
    *   **通信瓶颈**：即便使用 LoRA 等参数高效微调技术，MLLM 的适配器参数量对于带宽受限的边缘网络（如 5-20 Mbps）依然过大。
    *   **知识纠缠**：难以区分哪些参数负责通用的多模态语义对齐，哪些负责特定任务的语义表达。

### 2. 方法论：PFAdapter 框架
*   **核心思想**：基于 Transformer 自注意力机制的功能差异，对 LoRA 适配器进行**分层分解**和**选择性聚合**。
*   **关键技术细节**：
    *   **分层 LoRA 分解**：将 LoRA 参数显式分为两部分。
        *   **全局共享组件（$Q, K$ 投影）**：负责捕获跨网络的通用多模态结构关系（注意力模式）。
        *   **本地私有组件（$V, O$ 投影）**：负责捕获特定任务的语义内容，保留在本地不参与同步。
    *   **正交正则化（Orthogonality Regularization）**：引入基于 Frobenius 范数的损失函数，强制全局和本地参数矩阵在特征空间上正交。这防止了本地模块重复学习全局已有的特征，实现了精准的知识解耦。
    *   **选择性聚合协议**：服务端仅收集和聚合 $Q$ 和 $K$ 的适配器参数。
*   **算法流程**：客户端本地训练（计算任务损失 + 正交损失） $\rightarrow$ 仅上传 $Q, K$ 参数 $\rightarrow$ 服务端加权平均 $\rightarrow$ 下发更新后的全局参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **医疗问答**：VQA-RAD（放射影像）、SLAKE（语义标注医疗问答）。
    *   **社会多模态**：Hateful Memes（仇恨言论检测）、CrisisMMD（灾难救援图像文本分类）。
*   **数据分布**：采用 Dirichlet 分布（$\alpha=0.5$）模拟非独立同分布（Non-IID）的异构数据场景。
*   **基准方法（Benchmarks）**：
    *   **基础对比**：Zero-shot（零样本）、Local-only（仅本地训练）。
    *   **联邦优化算法**：FedAvgM、FedAdam、FedAdagrad、FedYogi（目前最强的 MLLM 联邦基准）。
    *   **个性化 LoRA 方法**：pFedLoRA。

### 4. 资源与算力
*   **硬件设施**：单张 **Nvidia L60 GPU (48GB VRAM)**。
*   **优化技术**：使用了 8-bit 量化（int4）和梯度检查点（Gradient Checkpointing）以降低显存占用。
*   **训练细节**：
    *   模型后端：MiniCPM-V-2_6-int4。
    *   训练时长：PFAdapter 约为 **10.8 分钟/轮**（略高于 FedYogi 的 10.3 分钟，因为增加了正交损失计算）。
    *   通信开销：从 617 MB/轮降至 **315 MB/轮**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流数据集，每组实验运行 3 个随机种子并取平均值。
*   **消融实验**：详细验证了“分层分解”、“正交损失”和“选择性聚合”各自的贡献。
*   **敏感性分析**：探讨了正交权重 $\lambda_o$、分解比例、数据异构程度（$\alpha$ 从 0.1 到 5.0）以及冷启动（新客户端加入）的表现。
*   **评价**：实验设计全面，覆盖了准确率、收敛速度、通信量、显存占用等多个维度，对比实验客观且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：PFAdapter 在各数据集上比 FedYogi 提升了 **2.4% 至 4.8%** 的准确率。
*   **通信效率**：成功减少了约 **50%** 的同步流量，总通信量从 30.85 GB 降至 15.75 GB。
*   **收敛性**：正交约束减少了参数冲突，使得模型在更少的轮次内达到更高的准确率。
*   **鲁棒性**：在极端 Non-IID（$\alpha=0.1$）和模态缺失（50% 缺失）的情况下，PFAdapter 的领先优势更加明显。

### 7. 优点与亮点
*   **理论支撑扎实**：并非盲目拆分参数，而是基于 Transformer 中 $Q/K$ 负责“关系对齐”而 $V/O$ 负责“语义表达”的深刻理解。
*   **通信与性能双赢**：在显著降低通信成本的同时，反而提升了模型性能，这在联邦学习研究中较为难得。
*   **即插即用**：该方法不改变模型架构，易于集成到现有的 PEFT 框架中。

### 8. 不足与局限
*   **固定比例限制**：目前 $Q/K$ 与 $V/O$ 的 50:50 拆分是固定的，未来可能需要根据不同任务动态调整拆分比例。
*   **层间差异未探索**：论文对所有 Transformer 层应用了统一的拆分策略，但深层和浅层参数对个性化的敏感度可能不同。
*   **计算开销略增**：虽然通信减少了，但本地计算正交损失会带来微小的额外计算负担。

（完）
