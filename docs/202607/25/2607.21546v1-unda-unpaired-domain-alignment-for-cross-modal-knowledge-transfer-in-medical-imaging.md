---
title: "UnDA: Unpaired Domain Alignment for Cross-Modal Knowledge Transfer in Medical Imaging"
title_zh: UnDA：用于医学影像跨模态知识迁移的非配对领域对齐
authors: "Rafsan Jany, Shadab Tanjeed Ahmad, Ahsan Bulbul, Tahsinul Islam, Md Azam Hossain, Abu Raihan Mostofa Kamal"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21546v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 用于跨模态迁移的非配对领域对齐
tldr: 针对医疗影像中多模态配对数据获取难的问题，本文提出UnDA框架，实现非配对跨模态知识迁移。该框架通过对齐模块提取语义特征，利用不确定性加权最优传输（UCT-OT）动态抑制噪声监督，并结合ProtoNCE损失保持全局判别性。实验证明，该方法在严格非配对设置下的分割任务中表现优异，显著提升了目标模态的准确性和边界精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21546v1/fig-001.webp\", \"caption\": \"Table 2. Segmentation performance on MM-WHS (MRI → CT). Dice (%, ↑) and HD95 (mm, ↓) per cardiac structure and mean.\", \"page\": 7, \"index\": 1, \"width\": 610, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2607.21546v1/fig-002.webp\", \"caption\": \"Table 3. Comparison with existing methods on MM-WHS (MRI → CT) under a common lightweight 3D CNN backbone. Dice (%, ↑) and HD95 (mm, ↓) per cardiac structure and mean. Bold indicates best result per column.\", \"page\": 8, \"index\": 2, \"width\": 610, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2607.21546v1/fig-003.webp\", \"caption\": \"Table 4. Ablation study on MM-WHS (MRI → CT). Dice (%, ↑) and HD95 (mm, ↓) per cardiac structure and 7-structure mean. Bold indicates best result per column.\", \"page\": 9, \"index\": 3, \"width\": 712, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2607.21546v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of UnDA: Phase 1 trains the anchor model on source-domain images; a presence head supervises the class token pool via LBCE-pres, producing classdiscriminative token representations. Phase 2 freezes the anchor to generate per-class uncertainty maps and update prototype memory Pc; the target encoder produces class tokens refined by an MLP residual adapter and aligned via prototypical contrastive loss LpNCE and uncertainty-weighted optimal transport LUCT-OT.\", \"page\": 3, \"index\": 4, \"width\": 733, \"height\": 337}]"
motivation: 现有的跨模态知识蒸馏方法在处理非配对医疗数据时，难以克服巨大的模态差异及源域预测中的噪声传播问题。
method: 提出一种锚点引导框架，结合了注意力机制对齐模块、不确定性加权最优传输以及类别原型对比学习。
result: 在多种医学影像分割任务的非配对实验中，该方法显著提升了目标模态的分割性能和结构一致性。
conclusion: UnDA成功实现了在无配对数据条件下的异构模态知识迁移，为临床多模态学习提供了有效的解决方案。
---

## 摘要
基于多模态的方法在下游任务中通常优于单模态方法，因为不同模态提供了互补信息，但在现实场景中获取配对的临床数据仍是一个重大挑战。虽然跨模态知识蒸馏解决了这一问题，但现有方法往往难以应对巨大的模态差异以及来自不确定源域预测的噪声传播。为了克服这些挑战，我们提出了 UnDA，这是一个用于非配对跨模态蒸馏的锚点引导框架。我们的方法引入了一个与骨干网络无关的对齐模块（Alignment Module），该模块通过基于注意力的池化机制提取具有语义结构的类别标记（class tokens）。为了确保鲁棒的知识迁移，我们提出了不确定性加权最优传输（UCT-OT），它根据预测置信度动态加权特征级对齐，从而有效抑制噪声监督。此外，逐类别 ProtoNCE 目标函数维持了稳定的原型记忆，以增强非配对批次间的全局可区分性。在严格非配对设置下的代表性分割任务评估显示，目标模态的准确性和边界精度得到了一致提升，证明了在没有配对数据集的情况下，也可以在异构数据源之间迁移有意义的结构化知识。

## Abstract
Multimodal based approaches often outperform single modality approaches in downstream tasks as the different modalities provide complementary information, yet acquiring paired clinical data remains a significant challenge in real world scenarios. While cross-modal knowledge distillation addresses this, existing methods often struggle with large modality gaps and the propagation of noise from uncertain source-domain predictions. To overcome these challenges, we propose UnDA, an anchor-guided framework for unpaired cross-modal distillation. Our approach introduces a backbone-agnostic Alignment Module that extracts semantically structured class tokens via an attention based pooling mechanism. To ensure robust knowledge transfer, we propose Uncertainty-Weighted Optimal Transport (UCT-OT), which dynamically weights feature-level alignment based on prediction confidence, effectively suppressing noisy supervision. Furthermore, a per-class ProtoNCE objective maintains stable prototype memories to enforce global discriminability across unpaired batches. Evaluations on representative segmentation tasks under strictly unpaired settings show consistent improvements in accuracy and boundary precision in the target modality, demonstrating that meaningful structural knowledge can be transferred across heterogeneous data sources without paired datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《UnDA: Unpaired Domain Alignment for Cross-Modal Knowledge Transfer in Medical Imaging》的深度结构化总结：

### 1. 核心问题与研究动机
在医学影像分析中，多模态数据（如 MRI 的不同序列或 MRI 与 CT 结合）能提供互补的解剖结构信息，显著提升分割精度。然而，在临床实际中，获取**完全配对（Paired）**的多模态数据（即同一患者在同一时间点的多种影像）极其困难且成本高昂。
现有的跨模态知识迁移方法存在两大痛点：
*   **模态鸿沟大**：不同模态间的特征分布差异巨大，直接对齐效果差。
*   **噪声传播**：源域（锚点模态）的预测并非百分之百准确，不确定的预测会作为“噪声”干扰目标模态的学习。
**UnDA** 旨在解决如何在**完全非配对**的情况下，将知识从一个性能较好的“锚点模态”迁移到“目标模态”，并抑制噪声干扰。

### 2. 方法论：核心思想与关键技术
UnDA 采用了一个**锚点引导（Anchor-guided）**的双阶段框架：

*   **骨干无关的对齐模块（Alignment Module）**：
    *   在编码器瓶颈处引入注意力池化机制，将高维特征图压缩为 $K$ 个紧凑的“类别标记（Class Tokens）”。
    *   该模块在训练时通过辅助的“类别存在头（Presence Head）”进行监督，确保每个 Token 捕捉特定类别的语义结构。推理时该模块被移除，实现**零推理开销**。
*   **不确定性加权最优传输（UCT-OT）**：
    *   这是本文的核心创新。在对齐非配对的 Token 分布时，利用最优传输（OT）理论。
    *   引入**不确定性权重**：通过计算锚点模型预测的熵来评估可靠性。置信度高的样本在 OT 计算中分配更大的传输质量，而模糊、不确定的样本被抑制，从而减少噪声监督。
*   **原型对比学习（ProtoNCE）**：
    *   维护一个全局原型记忆库（Prototype Memory），存储锚点模态的类别中心。
    *   通过对比损失强制目标模态的 Token 向对应的原型靠拢，确保在非配对批次训练中保持全局类别的判别性。

### 3. 实验设计与 Benchmark
论文在两个具有挑战性的临床场景下进行了验证：
*   **数据集**：
    1.  **BraTS 2023 (脑肿瘤分割)**：将 T2-FLAIR 作为锚点模态，T1-native 作为目标模态。
    2.  **MM-WHS (全心脏分割)**：将 MRI 作为锚点模态，CT 作为目标模态。
*   **实验设置**：严格的非配对设置，即锚点和目标模态的数据来自完全不同的患者子集。
*   **对比方法**：对比了 SE_ASA、DAR-UNet、SIFA v2、MAPSeg 和 UMMKD 等主流的非监督/跨模态领域自适应方法。

### 4. 资源与算力
*   **硬件环境**：论文提到使用 PyTorch 和 MONAI 框架实现。
*   **具体参数**：使用了 AdamW 优化器，学习率为 $3 \times 10^{-4}$，训练周期最高为 250 Epochs（带早停机制）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长。但从其使用的 3D U-Net 骨干网络和 $128^3$ 的 Patch 大小来看，通常需要具备 16GB 以上显存的专业级 GPU（如 RTX 3090 或 A100）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型公开数据集上进行了验证。
    *   进行了详细的**消融实验**（见表 4），分别验证了 ProtoNCE、普通 OT 和 UCT-OT 对性能的贡献。
    *   进行了跨方法的横向对比，并统一了所有对比方法的骨干网络（Lightweight 3D CNN），确保了比较的**公平性**。
*   **充分性评价**：实验设计较为客观，涵盖了不同解剖部位（大脑、心脏）和不同模态组合（MRI-MRI, MRI-CT），证明了方法的通用性。

### 6. 主要结论与发现
*   **性能提升显著**：在 BraTS 任务中，UnDA 将肿瘤核心（TC）的 HD95 距离从 29.12mm 降低到了 10.10mm。
*   **超越单模态基准**：在 MM-WHS 任务中，UnDA 辅助下的 CT 分割效果（Dice 82.71%）显著优于纯 CT 监督训练的效果（71.59%），证明了利用存档的 MRI 数据提升 CT 诊断价值的可行性。
*   **边界精度高**：得益于 UCT-OT 对结构信息的精准对齐，UnDA 在代表边界精度的 HD95 指标上表现尤为突出，优于所有对比方法。

### 7. 优点与亮点
*   **架构灵活性**：Alignment Module 与骨干网络无关，可适配各种 2D/3D 编码器。
*   **推理友好**：所有对齐组件仅在训练期存在，推理时只需目标模态模型，不增加临床部署的计算负担。
*   **鲁棒性强**：通过不确定性加权解决了医疗影像中常见的预测模糊性问题，使知识迁移更加“去粗取精”。

### 8. 不足与局限
*   **超参数敏感性**：最优传输中的正则化参数 $\epsilon$ 和损失权重 $\lambda$ 可能需要针对不同任务进行精细调优。
*   **极端模态差异**：虽然在 MRI 和 CT 间有效，但对于物理成像原理差异更大的模态（如超声与 MRI），其类别标记的语义一致性是否依然成立尚待验证。
*   **算力细节缺失**：未提供训练成本的具体数据，不便于其他研究者评估复现的资源门槛。

（完）
