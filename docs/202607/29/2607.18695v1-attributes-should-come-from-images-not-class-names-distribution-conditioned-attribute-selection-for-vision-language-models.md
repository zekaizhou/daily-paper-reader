---
title: "Attributes Should Come from Images, Not Class Names: Distribution-Conditioned Attribute Selection for Vision-Language Models"
title_zh: 属性应源自图像而非类名：视觉语言模型的分布条件属性选择
authors: "Gautam Rajendrakumar Gare, Jia Shi, Zhiqiu Lin, Deepak Pathak, John Galeotti, Deva Ramanan"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18695v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 在数据偏移时产生误导；从目标图像集中选择属性
tldr: 本文针对视觉语言模型在零样本分类中过度依赖类别名称而非视觉特征的问题，提出了一种基于数据分布的属性选择方法。该方法摒弃了仅依靠大语言模型生成通用描述的传统做法，转而从目标图像集中筛选与视觉内容最匹配的属性。实验证明，该方法在处理分布偏移时更具鲁棒性，且在少样本场景下比CoOp等微调方法更高效、更具可解释性，同时能提供数据集的文本摘要。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18695v1/fig-001.webp\", \"caption\": \"Table 1: Attribute prompts stand on their own only when selected from images. Top-1 accuracy with CLIP-RN50; attributes are selected on ImageNet only and reused unchanged on the four shifted variants. Top block: with the class name in the prompt, all descriptor methods sit within about a point of each other. Bottom block: with the class name removed, LLM descriptors collapse (59.47 to 15.50 on ImageNet) while our selection recovers 3.9 points from the identical pool and 8.3 points from a larger pool. Bold marks the best result per column within each block.\", \"page\": 6, \"index\": 1, \"width\": 710, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-002.webp\", \"caption\": \"Fig. 4: Distribution shift, described in words. Top-10 rising and falling attributes (change in mean attribute score over unlabeled images, y-axis) of ImageNet-Sketch (left panel) and ImageNet-R (right panel) relative to ImageNet. Sketch gains “colorless”, “gray”, and “cartoon” and loses color attributes; R gains “cartoon”, “painting”, and “tattooed”, matching its documented composition. Within each panel attributes are ordered by their change, risers first. Blue bars rising above zero become more present under the shift; vermillion bars falling below zero become less present.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-003.webp\", \"caption\": \"Fig. 3: Attribute features trade statistical independence for named, semantically clustered directions. Pairwise mutual information on ImageNet with CLIPRN50; all three panels use the sequential viridis scale, dark purple at zero to bright yellow at high mutual information. Left: dimensions of the 1024-d CLIP embedding, near zero off the diagonal. Middle: our 634 VAW attribute features, visibly more correlated. Right: features restricted to the top-5 attributes of three classes (park bench, chocolate syrup, hay); the brighter 5×5 diagonal blocks show that a class’s own attributes share information, i.e. the correlations follow semantics.\", \"page\": 11, \"index\": 3, \"width\": 725, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-004.webp\", \"caption\": \"Fig. 1: Class-name-conditioned descriptors fail under distribution shift; image-conditioned attributes remain reliable. For the class strawberry on ImageNet-Sketch, an LLM prompted only with the class name [13] or an external knowledge base [22] produces canonical descriptors (e.g., red, green leaves) because neither ever sees an image. In contrast, our method scores a large pool of candidate attributes against the sketch images themselves, selecting semantic attributes that remain valid in the shifted distribution (e.g., heart-shaped, dotted) thus aligning them with distribution-specific cues such as colorless and hand-drawn, rather than relying on source-domain appearance priors. Section 3 quantifies the failure; Section 6.1 turns the same mechanism into a description of the shift.\", \"page\": 2, \"index\": 4, \"width\": 725, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-005.webp\", \"caption\": \"Table 4: Attribute selection is a strong extreme-few-shot learner. ImageNet top-1 accuracy with CLIP-RN50; zero-shot CLIP scores 55.31. We linear-probe the attribute features of the k-shot images to select top-k attributes per class, then evaluate zero-shot with the selected attributes; baselines fine-tune or prompt-tune on the same shots. Bold marks the best method per column. Our accuracy plateaus beyond 4 shots because the probe only ranks a fixed pool of words; the crossover is stated, not hidden.\", \"page\": 10, \"index\": 5, \"width\": 708, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-006.webp\", \"caption\": \"Fig. 7: Class-name-free localization from selected attributes. We remove the attention pooling of CLIP RN50x64, compute cosine similarity between each 14×14 patch embedding and a class’s selected attribute texts, and display the resulting heatmap. Querying with the hippopotamus attribute set (left) and the Nile crocodile attribute set (right) localizes each animal; the queries contain no class names. Warmer color denotes higher mean similarity to the attribute set.\", \"page\": 14, \"index\": 6, \"width\": 581, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-007.webp\", \"caption\": \"Fig. 2: Distribution-conditioned attribute selection. A frozen CLIP encodes the target images and a large pool of attribute texts; the cosine similarities between them form an attribute feature vector per image. Class-attribute weights are the per-class mean of these features (or the weights of a linear probe trained on them), and the top-k attributes per class define the class’s prompt set. Both encoders stay frozen throughout; the only fitted object is the linear pairing.\", \"page\": 3, \"index\": 7, \"width\": 725, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-008.webp\", \"caption\": \"Fig. 5: Examples from the collected color-variant fruit test set. The set spans natural rarities (yellow eggplant), style shifts (sketches and drawings), and artificial colorings (blue and purple lemons), over the 37 classes of the source dataset [21].\", \"page\": 13, \"index\": 8, \"width\": 581, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-009.webp\", \"caption\": \"Fig. 6: CLIP zero-shot predictions latch onto color. On color-variant fruit images, zero-shot CLIP often predicts a class that matches the color rather than the object, e.g. a yellow eggplant drifts toward lemon.\", \"page\": 13, \"index\": 9, \"width\": 652, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-010.webp\", \"caption\": \"Table 2: With the attribute budget uncapped, image-conditioned selection triples attribute-only accuracy. Top-1 accuracy, attribute-only prompts (no class name), CLIP-RN50; selection uses 16 images per class. Weighted = each attribute’s vote scaled by its class-attribute score (Eq. (2) with ωc,j = wc,j); it adds about 0.3 points everywhere, while switching from the LLM descriptor pool to VAW+LSA under identical selection adds over 27 points on ImageNet. Bold marks best result per column.\", \"page\": 8, \"index\": 10, \"width\": 712, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-011.webp\", \"caption\": \"Table 5: Domain attributes discovered from unlabeled images make useful prompt templates. Top-1 accuracy, CLIP-RN50. Rows: class-specific attribute prompts (none = plain class name). Columns: prompt templates; the rightmost ensembles the top-5 rising domain attributes of each dataset (“A cartoon photo of a {class}” on ImageNet-R). The domain template wins in most settings, by up to 3.7 points over the plain class name on ImageNet-Sketch, with ImageNet-A top-100 and two ImageNetV2 rows as exceptions. Bold marks the best template per row.\", \"page\": 12, \"index\": 11, \"width\": 644, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2607.18695v1/fig-012.webp\", \"caption\": \"Table 3: Attribute-only accuracy grows monotonically with selection images when the attribute budget is uncapped. Top-1 accuracy, attribute-only prompts (no class name), full VAW+LSA pool, CLIP-RN50. Weighted is defined as in Table 2; it adds a consistent margin of roughly 0.3 points at every shot count on every dataset. Bold marks the better of the two variants per column within each dataset.\", \"page\": 9, \"index\": 12, \"width\": 510, \"height\": 373}]"
motivation: 现有的LLM生成属性方法受限于类别标签的先验知识，导致模型在面对如素描图等分布偏移数据时，因描述符与视觉事实不符而失效。
method: 通过在CLIP的联合嵌入空间中对大规模属性池进行评分，从目标图像集合中为每个类别筛选出得分最高的视觉属性。
result: 该方法在ImageNet及多个偏移变体上的表现优于传统LLM描述符，且在少样本学习效率上显著超过了CoOp。
conclusion: 属性选择应基于实际图像分布而非仅依赖类别名称，这不仅提升了分类性能，还增强了模型的可解释性。
---

## 摘要
一种流行的可解释零样本分类方法是要求大语言模型 (LLM) 描述每个类名，并使用生成的描述符提示 CLIP。我们表明，这些描述符本身几乎不携带视觉证据：从提示中移除类名后，ImageNet 的准确率从 59.5% 骤降至 15.5%。诊断结果是，这些描述符是以标签而非图像为条件的，因此它们描述的是一般概念，并在数据发生偏移时产生误导；例如，LLM 坚持认为草莓是红色的，但 ImageNet-Sketch 中的每个草莓都是无色的线条画。因此，我们改为从目标图像集中选择属性：我们在 CLIP 的联合嵌入空间中根据图像对庞大的属性池进行评分，并保留每个类别中得分最高的属性。通过这种方式选择，不含类名的属性提示在 ImageNet 上达到了 23.8% 的准确率（相比之下，LLM 描述符为 15.5%），这种增益在四个偏移的 ImageNet 变体上依然成立，且从 LLM 自身的属性池中重新选择进一步证明了选择机制是提升的原因。在每个类别仅有一张图像的情况下，所选属性的表现比提示微调方法 CoOp 高出 3 个百分点，且运行时间不到一分钟（而非 14 小时），同时没有学习到的软提示来掩盖决策过程。由于属性集是由数据选择的，它还可以作为数据集的可读摘要，我们利用它来用文字描述分布偏移。

## Abstract
A popular route to interpretable zero-shot classification asks a large language model (LLM) to describe each class name and prompts CLIP with the resulting descriptors. We show that these descriptors carry little visual evidence of their own: removing the class name from the prompt collapses ImageNet accuracy from 59.5% to 15.5%. The diagnosis is that the descriptors are conditioned on the label rather than on the images, so they describe the concept in general and mislead exactly when the data shifts; an LLM insists that strawberries are red, but every strawberry in ImageNet-Sketch is a colorless line drawing. We therefore select attributes from the target image collection instead: we score a large attribute pool against the images in CLIP's joint embedding space and keep the top-scoring attributes per class. Selected this way, class-name-free attribute prompts reach 23.8% on ImageNet (against 15.5% for LLM descriptors), the gain holds on four shifted ImageNet variants, and reselecting from the LLM's own pool isolates the selection mechanism as the cause. With one image per class, the selected attributes outperform the prompt-tuning method CoOp by 3 points while fitting in under a minute instead of 14 hours, with no learned soft prompt to obscure the decision. Because the attribute set is chosen by the data, it doubles as a readable summary of a dataset, which we use to describe distribution shift in words.

---

## 论文详细总结（自动生成）

这是一份关于论文《Attributes Should Come from Images, Not Class Names: Distribution-Conditioned Attribute Selection for Vision-Language Models》的深度学术总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前视觉语言模型（VLM，如 CLIP）的可解释分类方法通常依赖大语言模型（LLM）根据“类名”生成描述符（如“草莓是红色的”）。然而，这种方法存在**标签偏见**：描述符仅基于类别的一般概念，而非实际图像内容。
*   **研究动机**：当数据发生分布偏移（Distribution Shift）时，LLM 生成的通用描述会失效。例如，在素描数据集（ImageNet-Sketch）中，草莓是黑白的，但 LLM 仍会提供“红色”这一属性，导致模型误判。
*   **整体含义**：论文主张属性应从**实际图像分布**中筛选，而非仅从类名中推导，从而实现更具鲁棒性、可解释性且适应特定领域（Domain-specific）的分类。

### 2. 论文提出的方法论
*   **核心思想**：利用目标图像集（即使是无标签的或极少量的）在 CLIP 的联合嵌入空间中对大规模候选属性池进行评分，筛选出与当前视觉分布最匹配的属性。
*   **关键技术细节**：
    1.  **构建属性池**：整合了来自 VAW、LSA 等数据集以及 LLM 生成的数千个候选属性词。
    2.  **属性评分**：计算图像特征向量与属性文本特征向量之间的余弦相似度。
    3.  **分布条件选择**：
        *   **零样本/无监督场景**：计算每个类别图像的平均属性得分，选取 Top-K 属性。
        *   **少样本场景**：使用线性探针（Linear Probe）在属性特征空间上进行训练，以权重大小作为属性重要性的排序依据。
    4.  **分类推理**：将选出的属性填入模板（如 "a photo of a {attribute} {class}"），通过集成多个属性的得分进行最终分类。

### 3. 实验设计
*   **数据集**：
    *   核心基准：**ImageNet-1K**。
    *   分布偏移变体：**ImageNet-V2**（自然偏移）、**ImageNet-Sketch**（素描）、**ImageNet-R**（艺术渲染）、**ImageNet-A**（对抗样本）。
    *   特定测试集：自建的 **Color-variant Fruit**（颜色变异水果，如蓝色柠檬、黄色茄子）。
*   **对比方法（Baselines）**：
    *   **CuPL / DCLIP**：使用 LLM 生成描述符的 SOTA 方法。
    *   **CoOp / Tip-Adapter**：主流的提示微调（Prompt Tuning）和少样本学习方法。
    *   **CLIP Zero-shot**：原始的类名提示方法。

### 4. 资源与算力
*   **算力消耗**：论文强调了该方法的极高效率。
    *   在 ImageNet 少样本实验中，该方法在 **1 分钟内**即可完成属性选择和分类。
    *   相比之下，对比方法 **CoOp 需要约 14 小时**（在相同硬件环境下）。
*   **硬件说明**：文中未详细列出具体的 GPU 型号和数量，但提到其方法不需要对 CLIP 进行反向传播训练，仅涉及特征提取和简单的线性运算，因此对算力要求极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个主要的 ImageNet 系列数据集。
    *   进行了从 1-shot 到 16-shot 的完整少样本性能曲线对比。
    *   包含大量的消融实验：移除类名测试（验证属性独立性）、不同属性池规模对比、不同选择策略对比。
*   **充分性与客观性**：实验设计非常严谨。特别是“移除类名”的实验（Class-name-free）有力地证明了 LLM 描述符在失去标签引导后几乎失效，而本文方法选出的属性依然具有很强的视觉辨识度。

### 6. 主要结论与发现
*   **LLM 描述符的脆弱性**：在不包含类名的情况下，LLM 描述符在 ImageNet 上的准确率从 59.5% 骤降至 15.5%，证明其严重依赖先验而非视觉证据。
*   **属性选择的优越性**：本文方法在无类名提示下达到了 23.8% 的准确率，且在分布偏移（如 Sketch）下表现远超 LLM 方法。
*   **少样本效率**：在 1-shot 场景下，该方法比 CoOp 高出 3 个百分点，且速度快了近千倍。
*   **可解释性工具**：该方法能自动提取数据集的特征摘要（如发现 ImageNet-R 的特征是“卡通”、“绘画”），为理解分布偏移提供了文本化的工具。

### 7. 优点
*   **鲁棒性强**：通过数据驱动而非标签驱动，有效解决了分布偏移带来的误导问题。
*   **极速部署**：无需复杂的梯度下降训练，适合快速适应新领域。
*   **高度可解释**：选出的属性是人类可读的自然语言，且能通过热力图（Localization）直观展示属性对应的视觉区域。
*   **即插即用**：可以作为现有 VLM 框架的增强模块。

### 8. 不足与局限
*   **属性池依赖**：性能上限受限于初始候选属性池的覆盖范围，如果池中缺乏描述特定新领域的词汇，效果会受限。
*   **少样本瓶颈**：实验显示，当样本量超过 4-shot 后，性能提升趋于平缓，在多样本场景下可能不如全参数微调或复杂的提示学习方法。
*   **语义重叠**：选出的属性之间可能存在高度相关性（语义冗余），尚未完全实现统计学上的独立性。

（完）
