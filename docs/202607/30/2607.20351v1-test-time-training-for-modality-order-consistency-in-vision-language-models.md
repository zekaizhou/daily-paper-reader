---
title: Test-Time Training for Modality Order Consistency in Vision-Language Models
title_zh: 视觉语言模型中模态顺序一致性的测试时训练
authors: "Aditi Gupta, Yossi Gandelsman"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20351v1"
tags: ["query:vlm-da"]
score: 8.0
evidence: VLM一致性的测试时训练
tldr: 本研究发现视觉语言模型（VLM）对图像和问题的输入顺序具有敏感性，通常“图像优先”优于“问题优先”。为此，作者提出了一种顺序一致性测试时训练（TTT）方法，通过在推理阶段调整模型，使不同顺序的表征趋于一致。该方法不仅缩小了顺序带来的性能差距，还提升了基准表现。研究进一步通过激活修补定位了失效发生的中间网络层，并证明TTT能有效修复这种表征失配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20351v1/fig-001.webp\", \"caption\": \"Figure 5: Activation patching results - Qwen3.\", \"page\": 14, \"index\": 1, \"width\": 814, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-002.webp\", \"caption\": \"Figure 6: TTT restores order consistency with window-dependent internal drift. TTT increases agreement between image-first and question-first predictions for all tested adaptation windows. The corresponding layerwise cosine drift differs by window: early adaptation produces broad changes across the network, middle adaptation concentrates drift near the localized failure band, and late adaptation changes mainly the final layers.\", \"page\": 14, \"index\": 2, \"width\": 822, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-003.webp\", \"caption\": \"Figure 1: Accuracy under image-first and question-first prompting. Image-first prompting consistently outperforms question-first prompting across datasets and model families.\", \"page\": 3, \"index\": 3, \"width\": 817, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-004.webp\", \"caption\": \"Figure 3: Activation patching localizes the ordering failure. We replace the question-first finaltoken hidden state at layer L with the corresponding image-first hidden state and continue the forward pass normally. Patching becomes effective only in a narrow mid-layer band, where patched questionfirst accuracy increases and IF-correct/QF-wrong examples are recovered at high rate.\", \"page\": 8, \"index\": 4, \"width\": 820, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-005.webp\", \"caption\": \"Figure 4: TTT aligns late-layers MLP computation across prompt orders. We measure normalized intersection-over-union (NIoU) between the top late-layer components used by image-first and question-first forward passes, separately for attention heads and MLP sublayers. Negative NIoU indicates lower overlap than a size-matched random baseline. Before TTT, late-layer MLP sublayers show strongly below-random IF/QF overlap. After TTT, MLP overlap becomes strongly positive across adaptation windows, whereas attention-head overlap changes only modestly.\", \"page\": 9, \"index\": 5, \"width\": 814, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-006.webp\", \"caption\": \"Table 1: Main results. Asymmetric order-consistency TTT improves question-first (QF) accuracy across all evaluated model–dataset pairs while maintaining or slightly improving image-first (IF) accuracy. Results are reported at a fixed model-specific step count: K=5 for InternVL2-8B, K=4 for Qwen2.5-VL-7B, and K=3 for Qwen3-VL-8B.\", \"page\": 5, \"index\": 6, \"width\": 827, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-007.webp\", \"caption\": \"Figure 2: Qualitative TTT trajectories. Each panel shows image-first (IF) and question-first (QF) predictions across TTT steps; the correct answer is highlighted in green. In these examples, IF reaches the correct answer earlier, while QF catches up over the adaptation trajectory.\", \"page\": 5, \"index\": 7, \"width\": 802, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2607.20351v1/fig-008.webp\", \"caption\": \"Figure 7: TTT trajectories and image-first preservation across datasets. Top row: question-first accuracy across T=10 TTT steps for InternVL2-8B (K=5, blue) and Qwen2.5-VL-7B (K=4, red). Dashed lines indicate zero-shot baselines. Bottom row: image-first accuracy before and after TTT at the reported step. Image-first accuracy is maintained or slightly improved across most settings.\", \"page\": 16, \"index\": 8, \"width\": 818, \"height\": 420}]"
motivation: 视觉语言模型在处理图像和问题时，其性能受输入顺序这一语义无关因素的影响，存在明显的模态顺序失效现象。
method: 提出一种顺序一致的测试时训练方法，在推理时对模型进行异步自适应，以促进不同输入顺序下的表征互补与一致。
result: 该方法在多个基准测试中显著缩小了模态顺序差距，提升了模型性能，并修复了中间层表征的严重分歧。
conclusion: 模态顺序敏感性是模型内部电路层面的失效，通过简单的测试时自适应即可有效缓解并增强模型鲁棒性。
---

## 摘要
我们发现视觉语言模型对一种特定的语义无关变化非常敏感：即图像和问题的呈现顺序。在三个模型和三个基准测试中，“图像优先”提示词的效果始终优于“问题优先”提示词，揭示了一种可重复的模态顺序失效现象。我们利用这一差距设计了一种顺序一致的测试时训练方法。我们的方法在所有评估设置中都显著缩小了模态顺序差距。令人惊讶的是，该方法在表现更优的“图像优先”分支上相比基准模型也有持续提升，从而引导两种顺序向相互一致的方向自举。激活修补（Activation patching）将顺序失效定位在网络中部的狭窄区域，在该区域中，不同提示顺序之间的表示存在剧烈分歧。我们发现测试时训练方法修复了跨层的这种不对齐。总之，我们的研究结果表明，模态顺序敏感性是视觉语言模型中的一种电路级失效，并证明了简单的非对称测试时自适应可以有效缓解这一问题，甚至能提升基准性能。

## Abstract
We find that vision-language models are sensitive to a specific semantically irrelevant change: the order in which the image and question are presented. Across three models and three benchmarks, image first prompting consistently outperforms question-first prompting, revealing a repeatable modality order failure. We use this gap to design an order-consistent test-time training method. Our method substantially closes the modality-order gap across all evaluated settings. Surprisingly, it also yields consistent improvements in the stronger image-first branch over the baseline, hence bootstrapping both orderings toward mutual consistency. Activation patching localizes the ordering failure to a narrow mid-network region where representations diverge sharply between prompt orders. We find that the test-time training method repairs this misalignment across layers. Together, our results identify modality-order sensitivity as a circuit-level failure in VLMs and demonstrate that simple, asymmetric test-time adaptation can effectively mitigate it and even improve performance over the baseline.

---

## 论文详细总结（自动生成）

这是一份关于论文《Test-Time Training for Modality Order Consistency in Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLMs）对输入模态的顺序具有极高的敏感性。尽管“图像+问题”与“问题+图像”在语义上完全等价，但模型在两种顺序下的表现存在巨大差异。
*   **研究背景**：作者发现，在多个主流 VLM（如 InternVL2, Qwen2.5-VL, Qwen3-VL）中，**“图像优先”（Image-First, IF）**的准确率普遍显著高于**“问题优先”（Question-First, QF）**，差距可达 6 到 26 个百分点。这种现象揭示了模型内部多模态融合机制的脆弱性和电路级（circuit-level）失效。

### 2. 方法论：顺序一致性测试时训练（TTT）
*   **核心思想**：利用表现更强的 IF 分支作为“教师”，在推理阶段（Test-Time）对当前测试样本进行微小的梯度更新，引导表现较弱的 QF 分支向其对齐。
*   **关键技术细节**：
    *   **非对称对齐目标**：对于每个测试实例，同时构造 IF 和 QF 两种提示顺序。计算两者的预测概率分布 $p_{IF}$ 和 $p_{QF}$。
    *   **损失函数**：使用 KL 散度作为损失函数 $L_{align} = D_{KL}(\text{stopgrad}(p_{IF}) || p_{QF})$。
    *   **移动教师机制（Moving Teacher）**：在每一步梯度更新后，重新计算 IF 的分布作为下一轮的教师信号，从而实现“自举”（Bootstrapping）式提升。
    *   **局部更新**：基于机制分析，仅更新模型中层（Mid-layers）的参数，而非全量参数，以获得最佳的修复效果。
    *   **实例独立性**：每个样本处理完后，模型参数会重置为原始预训练状态，确保无标签、无任务特定微调。

### 3. 实验设计
*   **数据集/场景**：
    *   **MMStar**：强调视觉依赖性的多模态推理基准。
    *   **RealWorldQA**：真实世界感知任务。
    *   **AI2D**：科学图表阅读任务（具有较强的语言先验）。
    *   **COCO (CHAIR 评估)**：用于评估开放式图像描述中的物体幻觉。
*   **对比基准（Benchmarks）**：
    *   **Zero-shot Baselines**：原始模型的 IF 和 QF 表现。
    *   **消融对比**：对比了对称损失（Symmetric CE）、硬伪标签（Hard pseudo-label）以及不同层窗口（早、中、晚期层）的更新效果。
*   **评估模型**：InternVL2-8B、Qwen2.5-VL-7B、Qwen3-VL-8B。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **推理开销**：作者在讨论中承认，该方法增加了推理成本，因为每个样本需要两次前向传播以及 3 到 5 步（最多 10 步）的梯度更新。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、3 个主流判别式基准测试、1 个生成式幻觉评估任务。
*   **消融实验**：非常充分。包括损失函数类型、教师信号是否冻结、更新层位置的选择、甚至包括了最新的“思维链（Thinking Mode）”模型对顺序敏感性的影响分析。
*   **客观性**：通过“文本唯一（Text-only）”实验排除了语言先验的干扰，并使用激活修补（Activation Patching）从因果层面验证了失效位置，实验设计严谨且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：TTT 显著缩小了 QF 的差距（提升 5.2%~26.1%），且令人惊讶地微幅提升了原本就较强的 IF 表现。
*   **失效定位**：通过激活修补发现，模态顺序失效并非分布在整个网络，而是**因果性地集中在网络中层的狭窄区域**（如第 18-22 层）。
*   **机制解释**：失效主要由后期层中的 MLP 子层引起。TTT 通过对齐这些 MLP 的计算路径，使模型在不同输入顺序下产生一致的内部表征。
*   **思维链局限**：即使是具备思维链（CoT）能力的模型，依然存在模态顺序差距，说明这是一种结构性的表征问题。

### 7. 优点与亮点
*   **即插即用**：无需重新训练模型，无需标注数据，直接在推理阶段提升现有 VLM 的鲁棒性。
*   **机制导向**：并非盲目微调，而是结合了深入的机械解释性分析（Mechanistic Interpretability），精准定位并修复失效电路。
*   **非对称性创新**：识别出 IF 优于 QF 的先验事实，并据此设计非对称目标，比传统的对称一致性约束更有效。

### 8. 不足与局限
*   **推理延迟**：由于涉及反向传播和多次迭代，推理速度会显著变慢，难以直接应用于实时性要求高的场景。
*   **任务覆盖**：主要集中在多选题（Multiple-choice）形式的基准测试，在长文本生成、复杂对话等自由格式任务上的泛化性仍需进一步验证。
*   **超参数敏感性**：不同模型需要选择不同的更新步数（K）和学习率，缺乏一种完全自动化的参数调节机制。

（完）
