---
title: "Look Less, Think Faster: Joint Token-Compute Adaptation for Multimodal LLMs"
title_zh: 少看快思：多模态大语言模型的 Token-计算联合自适应
authors: "Pengcheng Wang, Zhiquan Wang, Jayoung Lee, Zhuoyan Xu, Ran Xu, Saurabh Bagchi, Yin Li, Somali Chaterji"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20357v1"
tags: ["query:vlm-da"]
score: 6.0
evidence: 多模态大语言模型的联合 Token 与计算自适应
tldr: 多模态大语言模型（MLLM）推理成本高昂，主要源于视觉Token数量庞大及模型计算量大。本文提出SmartVL框架，首次实现了视觉Token数量与模型计算能力的联合动态调整。通过视觉端Token控制器和LLM端计算控制器的协同工作，SmartVL能根据输入内容和计算预算灵活分配资源，在多个基准测试中显著提升了推理效率与准确率的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20357v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of SmartVL. A vision-side token controller and an LLM-side compute controller jointly govern three coupled compute dimensions: vision tokens (sequence length), active attention heads and FFN capacity (width), and executed transformer layers (depth). Both controllers are conditioned on the same global budget and trained end-to-end through a differentiable latency estimator, enabling them to learn content- and budget-aware allocation strategies across all three dimensions.\", \"page\": 7, \"index\": 1, \"width\": 638, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2607.20357v1/fig-002.webp\", \"caption\": \"Fig. 7: Visualization of token-compute allocation under a 4 TFLOPs comptute budget. The upper and lower figures show the prediction results on POPE and TextVQA. Different (T,C) combinations exhibit varying performance depending on the input, demonstrating that the best budget allocation is content-dependent.\", \"page\": 14, \"index\": 2, \"width\": 688, \"height\": 381}]"
motivation: 现有的加速方法通常独立优化视觉Token或模型层数，忽视了两者在资源分配上的内在耦合关系。
method: 提出SmartVL框架，利用共享预算编码和可微分延迟估计器，联合训练视觉Token控制器与LLM计算控制器以实现跨阶段资源调度。
result: 实验表明，SmartVL在多个基准测试中一致优于现有的自适应方法，实现了更优的准确率与效率平衡。
conclusion: 联合优化视觉输入规模与模型计算深度是提升多模态大模型推理效率的有效途径。
---

## 摘要
多模态大语言模型（MLLM）近期在视觉-语言任务中展现出强大的性能。然而，由于大量的输入视觉 Token 以及大语言模型（LLM）沉重的计算负担，其高昂的推理成本仍是实际部署的主要障碍。近期的研究尝试通过自适应地优化单个维度来降低成本，例如剪枝冗余的视觉 Token 或跳过 LLM 的层和注意力头。尽管如此，先前的方法通常独立地处理这些维度，忽略了一个基本的耦合关系：即可用的计算资源必须根据输入内容在所有维度上进行动态分配。为了弥补这一差距，我们提出了 SmartVL，这是一个统一的自适应推理框架，能够根据不同的输入内容和计算预算，联合控制视觉 Token 数量和模型计算能力。SmartVL 引入了一个视觉端 Token 控制器来动态选择信息丰富的视觉 Token，以及一个 LLM 端计算控制器来自适应地调整 LLM 计算。重要的是，这些控制器经过训练可以相互协作，从而使整体推理成本满足目标预算。为了实现这种联合调度，我们使用共享的预算编码连接控制器，并利用可微分的延迟估计器进行端到端训练。这种设计使 SmartVL 能够学习跨阶段的分配策略，以适应输入复杂性和运行时计算限制。在多个 MLLM 基准测试上的实验表明，通过联合调度，SmartVL 始终优于先前的自适应方法，并实现了更优的准确率-效率帕累托前沿。项目页面：https://www.schaterji.io/publications/2026/jointtokencompute。

## Abstract
Multimodal Large Language Models (MLLMs) have recently demonstrated strong performance across vision-language tasks. However, their high inference cost, arising from both the large number of input visual tokens and the heavy computation of the large language model (LLM), remains a key barrier to practical deployment. Recent work attempts to reduce the cost by adaptively optimizing individual dimensions, e.g., pruning redundant visual tokens or skipping LLM layers and heads. Nonetheless, prior approaches typically treat these dimensions independently and overlook a fundamental coupling: the available compute resources must be dynamically allocated across all dimensions based on the input content. To bridge the gap, we propose SmartVL, a unified adaptive inference framework that jointly controls vision token number and model compute capability in response to varying input contents and compute budgets. SmartVL introduces a vision-side token controller that dynamically selects informative visual tokens and an LLM-side compute controller that adaptively adjusts LLM computation. Importantly, these controllers are trained to coordinate with each other so that the overall inference cost satisfies a target budget. To allow this joint scheduling, we connect the controllers using a shared budget encoding and leverage a differentiable latency estimator for end-to-end training. This design enables SmartVL to learn cross-stage allocation strategies that adapt to both input complexity and runtime compute constraints. Experiments across multiple MLLM benchmarks demonstrate that, with joint scheduling, SmartVL consistently outperforms prior adaptive methods and achieves superior accuracy-efficiency Pareto frontiers. Project page: https://www.schaterji.io/publications/2026/jointtokencompute.