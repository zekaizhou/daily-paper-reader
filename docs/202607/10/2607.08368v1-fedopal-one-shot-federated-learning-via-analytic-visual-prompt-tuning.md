---
title: "FedOPAL: One-Shot Federated Learning via Analytic Visual Prompt Tuning"
title_zh: FedOPAL：基于解析视觉提示微调的单轮联邦学习
authors: "Lingyu Qiu, Daniela Annunziata, Stefano Izzo, Fabio Giampaolo, Francesco Piccialli"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08368v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 联邦学习中针对非独立同分布数据的解析视觉提示微调
tldr: 针对联邦学习中通信带宽受限和非独立同分布（non-IID）数据导致的特征流形失配问题，本文提出FedOPAL框架。该框架结合一发联邦学习与解析学习，通过视觉提示微调作为特征矫正器，并引入局部近端约束，将异构数据映射至线性可分空间。FedOPAL在保持零服务器端训练成本的同时，显著提升了模型性能，为边缘大模型的高效协作提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08368v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of FedOPAL. (Left) In the local training phase, clients freeze the foundation model backbone and optimize a set of continuous visual prompt tokens Pk inserted into the input embedding sequence. Using the rectified features extracted from the prompt-augmented sequences, clients compute closed-form sufficient statistics (Rk,Ck). (Right) The server aggregates these statistics to analytically derive the optimal global classifier W∗ and averages the prompt tokens, achieving efficient one-shot federated learning.\", \"page\": 3, \"index\": 1, \"width\": 1076, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2607.08368v1/fig-002.webp\", \"caption\": \"TABLE II COMPARISON OF PRE-TRAINING BASED METHODS ACROSS FOUR DATASETS WITH VARYING HETEROGENEITY (α).\", \"page\": 4, \"index\": 2, \"width\": 466, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2607.08368v1/fig-003.webp\", \"caption\": \"TABLE III HYPERPARAMETER SENSITIVITY ANALYSIS ACROSS HETEROGENEITY LEVELS. WE EVALUATE THE IMPACT OF THE PROXIMAL COEFFICIENT µ, REGULARIZATION λ, AND LOCAL EPOCHS E ON THREE DATASETS UNDER SEVERE (α = 0.1) AND MODERATE (α = 1.0) NON-IID SETTINGS. THE DEFAULT CONFIGURATION IS µ = 0.1, E = 5, λ = 0. ”−” INDICATES THE DEFAULT SETTING.\", \"page\": 5, \"index\": 3, \"width\": 754, \"height\": 257}]"
motivation: 解析联邦学习在处理非独立同分布数据时，因静态特征假设失效导致特征流形失配，严重影响了模型的泛化性能。
method: 提出FedOPAL框架，利用视觉提示作为特征矫正器并施加局部近端约束，主动将异构数据分布修正为满足解析学习假设的线性可分空间。
result: 实验表明FedOPAL在多个基准测试上显著优于原始解析方法，且在零服务器训练成本下达到了与最先进迭代方法相当的准确率。
conclusion: 该研究通过结合视觉提示微调与解析学习，有效解决了单次联邦学习中的异构性挑战，为边缘智能的高效部署提供了高效且低成本的新方案。
---

## 摘要
随着基础模型在边缘智能中的广泛部署，通信带宽已成为限制联邦学习可扩展性的核心瓶颈。尽管单轮联邦学习通过最小化通信轮数缓解了这一问题，但现有的迭代微调或知识蒸馏方法仍面临服务器端计算成本高和超参数敏感等挑战。解析联邦学习利用最小二乘闭式解实现了高效的无梯度聚合，但在非独立同分布（non-IID）数据环境下，其静态特征假设失效，导致特征流形失配并严重损害模型性能。为解决这一矛盾，本文提出了 FedOPAL 框架。该框架将视觉提示作为特征矫正器，通过应用局部近端约束，主动将异构数据的特征分布修正到线性可分空间，从而满足解析联邦学习的理论假设。实验结果表明，FedOPAL 不仅在多个基准测试上显著优于原始解析方法，而且在保持零服务器端训练成本的同时，达到了与最先进迭代方法相当的准确率，为边缘大模型的高效协作提供了一种新的工程范式。

## Abstract
With the widespread deployment of basic models in edge intelligence, communication bandwidth has become a core bottleneck restricting the scalability of federated learning. Although one-shot federated learning alleviates this problem by minimizing communication rounds, existing iterative fine-tuning or knowledge distillation methods still face challenges such as high server-side computational costs and hyperparameter sensitivity. Analytical federated learning achieves efficient gradientfree aggregation using least-squares closed-form solutions, but in environments with non-independent and identically distributed data, its static feature assumptions fail, leading to feature manifold misalignment and severely impairing model performance. To address this contradiction, this paper proposes the FedOPAL framework. This framework adapts the visual prompts as feature rectifiers, actively correcting the feature distribution of heterogeneous data to a linearly separable space by applying local proximal constraints, thereby satisfying the theoretical assumptions of analytical federated learning. Experimental results show that FedOPAL not only significantly outperforms the original analytical methods on several benchmarks, but also achieves accuracy comparable to state-of-the-art iterative methods while maintaining zero server-side training costs, providing a new engineering paradigm for efficient collaboration of large models on the edge.

---

## 论文详细总结（自动生成）

### 论文总结：FedOPAL - 基于解析视觉提示微调的单轮联邦学习

#### 1. 核心问题与整体含义（研究动机和背景）
在边缘智能场景下，联邦学习（FL）面临**通信带宽受限**和**模型参数量巨大**（如 ViT, CLIP）的双重挑战。传统的联邦平均（FedAvg）需要多轮交互，开销巨大。虽然**单轮联邦学习（One-Shot FL）**能显著减少通信，但现有方法（如知识蒸馏或数据合成）往往将计算压力转移到了服务器端，且对超参数极其敏感。
最近出现的**解析联邦学习（AFL）**通过最小二乘法的闭式解实现无梯度聚合，效率极高，但它依赖一个强假设：冻结的骨干网络提取的特征必须是高质量且线性可分的。在**非独立同分布（non-IID）**的现实场景中，各客户端数据分布差异大，导致特征流形失配，解析解的性能会严重退化。

#### 2. 方法论：核心思想与技术细节
FedOPAL 框架的核心思想是**将特征矫正与分类器学习解耦**，利用视觉提示微调（VPT）主动修正异构数据的特征分布。
*   **本地阶段（特征矫正）**：
    *   客户端冻结基础模型（如 CLIP 的 ViT 骨干），在输入嵌入序列中插入少量可学习的**视觉提示 Token（Visual Prompts）**。
    *   通过梯度下降优化这些提示 Token，并引入**局部近端约束（Proximal Constraint）**，防止提示过度拟合本地稀疏分布，确保全局稳定性。
    *   优化完成后，客户端提取特征并计算统计量：自相关矩阵 $R_k$ 和互相关矩阵 $C_k$。
*   **服务器阶段（双重聚合）**：
    *   **提示聚合**：直接对各客户端上传的提示 Token 进行元素级平均，获得全局提示。
    *   **解析求解**：利用上传的统计量 $R_k$ 和 $C_k$，通过正则化最小二乘法的闭式解公式，直接计算出全局最优分类器权重 $W^*$，无需任何迭代训练。

#### 3. 实验设计
*   **数据集与场景**：涵盖了自然对象（CIFAR-10, CIFAR-100）、数字识别（SVHN）和纹理分类（DTD）四种典型视觉任务。
*   **异构性设置**：采用 Dirichlet 分布 $\alpha \in \{0.01, 0.1, 0.5, 1.0\}$ 模拟不同程度的 non-IID 场景，$\alpha$ 越小异构性越强。
*   **Benchmark 与对比方法**：
    *   **传统蒸馏/生成类**：Co-Boosting, DENSE。
    *   **基于基础模型的统计类**：AFL, FedPFT, FedCGS。
    *   **骨干网络**：主要使用 CLIP (ViT-B/16)。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A6000 GPU** 上完成。
*   **软件框架**：基于 PyTorch。
*   **训练细节**：本地训练轮数 $E$ 默认为 5。文中未明确给出总的训练时长，但强调了服务器端是“零训练成本”（Zero server-side training cost），因为仅涉及矩阵运算。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集上进行了详尽的对比，涵盖了从极端异构到同分布的多种情况。
*   **消融实验**：针对近端系数 $\mu$、解析正则化参数 $\lambda$、本地训练轮数 $E$ 以及数据异构度 $\alpha$ 进行了敏感性分析。
*   **客观性**：实验对比了多种不同技术路线的 SOTA 方法，结果显示 FedOPAL 在大多数自然图像任务上具有压倒性优势，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能卓越**：在 CIFAR-100 ($\alpha=0.01$) 任务上，FedOPAL 比最强竞争对手 FedCGS 高出 10.17%。
*   **鲁棒性强**：相比于 AFL，FedOPAL 通过视觉提示有效解决了特征失配问题，在极端 non-IID 下性能依然稳定（方差仅 0.32%）。
*   **高效性**：实现了单轮通信，且服务器端无需迭代训练，极大地降低了系统总开销。
*   **领域局限性**：在 SVHN（数字识别）上表现略逊于 FedCGS，反映出预训练模型（CLIP）的先验知识与特定领域数据之间存在领域间隙（Domain Gap）。

#### 7. 优点
*   **创新性结合**：首次将视觉提示微调（VPT）引入解析联邦学习，解决了 AFL 在异构数据下的失效问题。
*   **极低开销**：通信仅需一轮，服务器端计算量极小，非常适合资源受限的边缘计算环境。
*   **超参数鲁棒**：实验证明模型对解析正则化参数 $\lambda$ 不敏感，降低了调参难度。

#### 8. 不足与局限
*   **依赖预训练模型**：其性能高度依赖于基础模型（如 CLIP）的表征能力，若基础模型与目标领域差异过大（如医学影像或特殊工业数据），效果可能受限。
*   **提示聚合策略简单**：目前仅采用简单的元素平均（Averaging）来聚合提示，对于极度极端的分布差异，可能存在更优的聚合方案。
*   **应用范围**：目前主要集中在图像分类任务，尚未在目标检测、分割等更复杂的下游任务中验证。

（完）
