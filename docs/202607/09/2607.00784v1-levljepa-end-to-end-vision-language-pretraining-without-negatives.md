---
title: "LeVLJEPA: End-to-End Vision-Language Pretraining Without Negatives"
title_zh: LeVLJEPA：无负样本的端到端视觉-语言预训练
authors: "Lukas Kuhn, Giuseppe Serra, Randall Balestriero, Florian Buettner"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00784v1"
tags: ["query:vlm-da"]
score: 6.0
evidence: VLM骨干网的端到端视觉语言预训练
tldr: 本研究针对视觉语言预训练（VLP）长期依赖对比学习的问题，提出了首个完全非对比的端到端预训练方法LeVLJEPA。该方法通过跨模态预测和分布正则化进行学习，无需负样本、动量编码器或复杂的教师-学生架构。实验证明，LeVLJEPA在下游视觉语言模型和密集预测任务中表现优异，特别是在提供密集语义特征方面显著优于传统的对比学习基准，为视觉语言模型提供了更强的骨干网络。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00784v1/fig-001.webp\", \"caption\": \"Figure 1. LeVLJEPA overview. Image XV and text XT are encoded into embeddings ZV , ZT , which are then passed through modality-specific predictors to produce cross-modal predictions ẐV , ẐT . The training objective combines (i) a cross-modal MSE loss between each predictor’s output and the stop-gradient target from the other modality, and (ii) SIGReg [3] applied independently to ZV and ZT to keep each modality’s marginal embedding distribution close to isotropic Gaussian. SIGReg (right) enforces this distribution by projecting embeddings onto random univariate directions and applying a characteristic-function-based normality test to each projection, avoiding the curse of dimensionality of direct density matching.\", \"page\": 2, \"index\": 1, \"width\": 1036, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2607.00784v1/fig-002.webp\", \"caption\": \"Table 6. Frozen VLM-backbone accuracy (%) on GQA, VQAv2, and POPE, with two frozen language models. Both the ViT-B/16 vision encoder and the LLM are frozen; only an MLP bridge is trained. All encoders are pretrained on Datacomp-L. Subscripts are standard deviations over three bridge-training seeds. LeVLJEPA is the strongest backbone in every column.\", \"page\": 8, \"index\": 2, \"width\": 746, \"height\": 183}]"
motivation: 现有的视觉语言预训练主要依赖对比学习，但在生成适用于密集预测任务的细粒度语义特征方面存在局限。
method: 提出LeVLJEPA，利用跨模态预测、停止梯度目标和模态内分布正则化实现非对比预训练，无需负样本或复杂的训练策略。
result: 在GQA、VQAv2等视觉问答任务及语义分割中，LeVLJEPA作为冻结骨干网络的效果显著优于对比学习基准。
conclusion: 非对比预训练是生成高质量密集视觉语义特征的有效手段，能够为视觉语言模型和密集预测系统提供更强大的视觉表征。
---

## 摘要
视觉-语言预训练仍由对比学习目标主导，而纯视觉自监督学习已在很大程度上采用了非对比方法。与此同时，视觉-语言编码器的角色发生了转变：它们越来越多地不再作为零样本分类器使用，而是作为视觉-语言模型和稠密预测系统的冻结视觉主干，这些系统消耗的是完整的补丁标记（patch tokens）网格，而非单一的池化嵌入。我们介绍了 LeVLJEPA，这是首个完全非对比的端到端视觉-语言预训练方法。LeVLJEPA 通过带有梯度停止目标的跨模态预测和各模态分布正则化进行学习，无需负样本、温度参数、动量编码器或教师-学生调度，且在大规模训练下表现稳定。我们发现，由此产生的编码器为下游任务提供了显著更强的稠密语义特征：作为冻结的视觉-语言模型主干，LeVLJEPA 在两种不同的语言模型下，在 GQA、VQAv2 和 POPE 任务中均是所评估编码器中最强的，并且在语义分割任务上优于对比学习基准，同时在诸如线性探测等全局读取任务上保持相当水平。这些结果确立了非对比预训练是生成稠密语义视觉特征的一种有效手段。

## Abstract
Vision-language pretraining remains dominated by contrastive objectives, whereas vision-only self-supervised learning has largely adopted non-contrastive methods. At the same time, the role of vision-language encoders has shifted: they are increasingly deployed not as zero-shot classifiers but as the frozen visual backbone of vision-language models and dense prediction systems, which consume the full grid of patch tokens rather than a single pooled embedding. We introduce LeVLJEPA, the first fully non-contrastive end-to-end vision-language pretraining method. LeVLJEPA learns through cross-modal prediction with stop-gradient targets and per-modality distributional regularization, without negatives, temperature, momentum encoder, or teacher-student schedule, and trains stably at large scale. We find that the resulting encoder provides markedly stronger dense semantic features for downstream use: as a frozen vision-language-model backbone, LeVLJEPA is the strongest of the evaluated encoders across GQA, VQAv2, and POPE under two distinct language models, and outperforms contrastive baselines on semantic segmentation, while remaining on par on global readouts such as linear probing. These results establish non-contrastive pretraining as an effective means of producing dense semantic vision features.