---
title: Geometry-Enhanced Portion Estimation for Multimodal LLMs
title_zh: 面向多模态大语言模型的几何增强分量估计
authors: "Lin Liao, Peng Li"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16514v1"
tags: ["query:vlm-da"]
score: 7.0
evidence: 增强冻结的MLLM以进行零样本饮食评估
tldr: 本研究针对多模态大模型（MLLM）在食物分量估计方面的短板，提出了一种几何增强的方法。该方法在不微调MLLM且无需深度传感器的情况下，通过在冻结的DINOv2骨干网络上添加轻量级几何增强头，结合MLLM提供的食物信息，显著提升了分量估计精度，在多个基准测试中超越了现有旗舰模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16514v1/fig-001.webp\", \"caption\": \"Table 4 | Flagship MLLMs as direct portion estimators. Each model recognizes foods and estimates per-food grams directly (open-vocabulary, same decomposition prompt and matcher) on 100 multi-food dishes. Mass cov. = mass-weighted recall; higher is better. Per-food MAE: lower is better. Our model inherits Gemini-3.5-Flash recognition through the pipeline’s extraction prompt (name, box, density—no gram estimation), so its coverage differs slightly from the Gemini-3.5-Flash row, whose detections come from the directestimation prompt: same model, different prompt.\", \"page\": 11, \"index\": 1, \"width\": 636, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.16514v1/fig-002.webp\", \"caption\": \"Table 3 | Ablations on Nutrition5k. One component removed at a time from the full model; per-food MAE (grams, three-seed average) on the held-out test, broken down by single-food dishes, multi-food dishes, and all dishes (total), each with Δ vs. the full model. Lower is better.\", \"page\": 10, \"index\": 2, \"width\": 876, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2607.16514v1/fig-003.webp\", \"caption\": \"Figure 1 | Geometry-enhanced MLLM architecture. A frozen MLLM (Gemini-3.5-Flash) extracts per-food name, bounding box, and density range from the RGB image; a frozen DINOv2 ViT-S/14 supplies patch tokens. The image×food encoder and an image-only encoder feed structured heads that produce an exclusive per-patch ownership partition, a per-food density anchored to the MLLM range, and a per-patch height field. Volume estimate for food 𝑛 is the sum across all patches 𝑝: 𝑣𝑛 = ∑ 𝑝 𝑜𝑛,𝑝 ℎ𝑝 and per-food mass estimate is 𝑚𝑛 = 𝑣𝑛 · 𝜌𝑛 .\", \"page\": 3, \"index\": 3, \"width\": 884, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2607.16514v1/fig-004.webp\", \"caption\": \"Figure 2 | Image×Food encoder block. Each of the 𝐿=4 layers is a Transformer decoder-style block: multi-head self -attention over the food query tokens (food↔food), multi-head cross-attention from the foods into the per-food geometry-aware patch memory mem𝑛,𝑝 (keys/values, built once from the frozen DINOv2 patch tokens and per-food box geometry), and a feed-forward sublayer, each wrapped by a residual connection and layer normalization. The stack outputs contextualized per-food tokens 𝑍𝑛 : the density head reads 𝑍𝑛 alone, while the ownership head scores each (mem𝑛,𝑝 , 𝑍𝑛) pair.\", \"page\": 4, \"index\": 4, \"width\": 632, \"height\": 546}]"
motivation: 尽管多模态大模型擅长识别食物种类，但在缺乏深度信息的情况下，准确估计食物分量仍是其面临的主要挑战。
method: 提出一种无需微调的几何增强头，利用冻结的DINOv2特征和结构化Softmax所有权体积，结合MLLM输出的边界框和密度范围进行估计。
result: "在三个真实世界基准测试中，该方法将分量误差降低了33-41%，性能优于GPT-4等旗舰模型及专门的图像模型。"
conclusion: 通过引入几何先验，可以在不增加硬件成本和模型微调负担的前提下，大幅提升通用多模态大模型在专业领域任务中的量化估计能力。
---

## 摘要
基于图像的饮食评估有望取代昂贵且易产生偏差的人工回顾，但分量估计仍是一个主要障碍。多模态大语言模型（MLLM）能够在非受控照片中零样本识别多种食物，但在分量估计方面表现较弱——我们在当前的尖端模型（包括 Gemini、GPT 和 Claude 旗舰系列）中衡量了这一差距。我们提出了一种方法，通过一个精确的分量头来增强冻结的商业 MLLM：该分量头是一个基于冻结 DINOv2 骨干网络的小型几何增强网络，具有结构化的 softmax-ownership 体积，并利用 MLLM 提供的每种食物名称、边界框和密度范围——无需深度传感器，也无需对 MLLM 进行微调。在三个真实世界基准测试中进行的完全开放词汇评估显示，该分量头相对于单独使用 MLLM 将每种食物的分量误差降低了 33-41%，优于所有旗舰 MLLM 的直接估计，并超过了每个基准测试最初发布的纯图像模型在其报告指标上的表现。

## Abstract
Image-based dietary assessment promises to replace costly, bias-prone manual recalls, but portion estimation remains a major blocker. Multimodal LLMs (MLLMs) recognize a wide range of foods zero-shot in uncontrolled photos, yet they are weak at portion estimation -- a gap we measure across the current frontier (Gemini, GPT, and Claude flagships alike). We present a method that enhances a frozen, commercial MLLM with an accurate portion head: a small geometry-enhanced network on a frozen DINOv2 backbone with a structured softmax-ownership volume, consuming the MLLM's per-food name, bounding box, and density range -- no depth sensor, no MLLM fine-tuning. Evaluated fully open-vocabulary on three real-world benchmarks, the head cuts per-food portion error by 33-41% relative to the MLLM alone, outperforms every flagship MLLM's direct estimates, and surpasses each benchmark's originally published image-only model at its own reported metric.