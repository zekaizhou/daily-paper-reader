---
title: Continual Learning with Elastic Regularization and Synthetic Replay for Federated MLLM Fine-Tuning
title_zh: 面向联邦多模态大语言模型微调的弹性正则化与合成重放持续学习
authors: "Jing Liu, Chenxuanyin Zou, Jiayang Ren, Gaoyun Fang, Chengfang Li, Yan Wang, Zhenchao Ma, Bo Hu"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12112v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 多模态大模型的联邦微调以适应不断演化的数据流
tldr: 针对联邦多模态大模型（MLLM）微调中的灾难性遗忘问题，本文提出FedCMM框架。该框架通过模态感知的弹性权重整合、轻量级生成式回放以及基于任务相似度的梯度聚合，在保护隐私的同时实现了跨视觉、语言和跨模态表示的持续学习。实验证明其在准确率和知识保留方面优于现有基准，为动态环境下的分布式AI部署提供了稳健方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12112v1/fig-001.webp\", \"caption\": \"TABLE I: Performance comparison on 3-task PHEME and CrisisMMD under moderate and severe non-IID partitions. The values in parentheses indicate the Dirichlet heterogeneity parameter α. Higher is better for Acc and FWT, while BWT is better when closer to zero. Best results are highlighted in bold.\", \"page\": 8, \"index\": 1, \"width\": 1064, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2607.12112v1/fig-002.webp\", \"caption\": \"Fig. 1: Overview of the FedCMM framework for privacy-preserving federated continual multimodal learning. For task Tj and communication round r, the server broadcasts w(j−1)\", \"page\": 4, \"index\": 2, \"width\": 1069, \"height\": 492}]"
motivation: 联邦学习环境下多模态大模型在处理连续任务流时，面临严重的灾难性遗忘挑战，导致先前获取的跨模态知识被覆盖。
method: 提出FedCMM框架，在参数层采用模态感知弹性正则化，在数据层利用生成式回放合成嵌入，在聚合层实施基于任务相似度的梯度过滤。
result: 在两个权威基准上的实验表明，FedCMM在模型准确率和后向迁移指标上均一致优于近年来的主流基准方法。
conclusion: 通过整合多层级的模态感知优化策略，FedCMM有效提升了异构网络中多模态大模型在持续学习过程中的鲁棒性与适应性。
---

## 摘要
在分布式网络中对多模态大语言模型（MLLM）进行联邦微调，能够实现对不断演化的数据流进行隐私敏感的适配，但一个根本性障碍阻碍了其在动态环境中的稳健部署：灾难性遗忘，即连续的任务更新会抹除先前在视觉、语言和跨模态表示中习得的知识。应对这一挑战对于在安全敏感领域（如内容审核）运行的自主网络化人工智能尤为关键，在这些领域，对先验知识的可靠保留是系统完整性的基础。为此，我们提出了联邦持续多模态学习（FedCMM），这是一个在三个互补层面上将持续学习保护机制嵌入联邦优化循环的框架。在参数层面，模态感知的弹性权重整合（Elastic Weight Consolidation）分别为视觉编码器、语言主干网络和跨模态投影器计算独立的费舍尔信息矩阵，针对特定模态的遗忘提供细粒度且感知不对称性的保护。在数据层面，每个客户端训练一个轻量级的本地生成式重放模块，在不共享任何原始数据的情况下，合成无原始数据的嵌入级多模态重放元组。在聚合层面，任务相似性感知的梯度聚合通过梯度余弦相似度自动过滤并重新加权客户端更新，从而抑制冲突方向并稳定全局学习轨迹。在两个基准测试上的广泛实验表明，FedCMM 在准确率和后向迁移（backward transfer）方面始终优于近期的基准方法，证实了整体的、模态感知的优化能够在异构网络化人工智能部署中实现稳健的演化适配。

## Abstract
Federated fine-tuning of Multimodal Large Language Models (MLLMs) across distributed networks enables privacy-sensitive adaptation to evolving data streams, yet a fundamental obstacle prevents robust deployment in dynamic environments: catastrophic forgetting, wherein sequential task updates erase previously acquired knowledge across visual, linguistic, and cross-modal representations. Addressing this challenge is especially critical for autonomous networked AI operating in safety-sensitive domains, such as content moderation, where reliable retention of prior knowledge underpins system integrity. To overcome this, we propose Federated Continual Multimodal Learning (FedCMM), a framework that embeds continual-learning safeguards into the federated optimization loop at three complementary levels. At the parameter level, modality-aware elastic weight consolidation computes separate Fisher information matrices for the vision encoder, language backbone, and cross-modal projector, providing granular, asymmetry-aware protection against modality-specific forgetting. At the data level, each client trains a lightweight local generative replay module to synthesize raw-data-free embedding-level multimodal replay tuples without any raw data sharing. At the aggregation level, Task-similarity-aware gradient aggregation autonomously filters and reweights client updates by gradient cosine similarity, suppressing conflicting directions and stabilizing the global learning trajectory. Extensive experiments on two benchmarks demonstrate that FedCMM consistently outperforms recent baselines on accuracy and backward transfer, confirming that holistic, modality-aware optimization enables robust evolutive adaptation across heterogeneous networked AI deployments.

---

## 论文详细总结（自动生成）

这篇论文《Continual Learning with Elastic Regularization and Synthetic Replay for Federated MLLM Fine-Tuning》针对多模态大语言模型（MLLM）在分布式网络环境下的持续学习挑战提出了创新方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：MLLM（如视觉-语言模型）在内容审核、灾难响应等安全敏感领域表现卓越，但这些领域的数据往往以“流”的形式出现且具有隐私敏感性。
*   **核心痛点**：
    *   **灾难性遗忘**：在联邦学习（FL）框架下顺序微调新任务时，模型会迅速丢失先前任务在视觉、语言及跨模态对齐方面的知识。
    *   **模态复杂性**：MLLM 包含多个模块，遗忘在不同模态间的表现是不对称的。
    *   **数据异构性**：联邦环境下的非独立同分布（Non-IID）数据和任务漂移会导致客户端梯度冲突，破坏全局模型的稳定性。

### 2. 方法论（FedCMM 框架）
FedCMM 提出了一个三位一体的防御机制，分别在参数、数据和聚合层面实施保护：
*   **参数层：模态感知弹性权重整合（MA-EWC）**
    *   核心思想：不再对所有参数统一施加惩罚，而是将模型划分为视觉编码器、语言主干和跨模态投影器。
    *   技术细节：为每个模块独立计算对角费舍尔信息矩阵（FIM），作为参数重要性的度量。在学习新任务时，对重要参数的偏离施加模态特异性的惩罚。
*   **数据层：隐私保护联邦重放（PPFR）**
    *   核心思想：在不共享原始数据的前提下，通过生成式重放找回旧知识。
    *   技术细节：客户端训练一个轻量级的本地生成器，在**嵌入空间（Embedding-level）**合成旧任务的视觉和文本特征元组。训练新任务时，将这些合成元组与新数据混合训练。
*   **聚合层：任务相似度感知梯度聚合（TSGA）**
    *   核心思想：过滤掉与全局学习方向冲突的客户端更新。
    *   技术细节：服务器计算各客户端更新方向与平均方向的余弦相似度，剔除相似度低于阈值 $\tau$ 的更新，并根据相似度动态调整聚合权重。

### 3. 实验设计
*   **数据集**：
    *   **PHEME**：社交媒体谣言验证数据集（包含文本和图像）。
    *   **CrisisMMD**：自然灾害多模态推文数据集。
*   **Benchmark 设置**：采用事件增量学习模式，将数据集按时间顺序分为 3 个任务组。
*   **对比方法**：
    *   基础方法：Fine-tuning（下限）、Joint Training（上限）、FedAvg-CL。
    *   经典持续学习：EWC-FL、LwF-FL、GEM-FL（隐私违规参考项）。
    *   前沿联邦持续学习：FedWeIT、AF-FCL、TEKNet-FL。

### 4. 资源与算力
*   **硬件**：论文明确提到使用了 **NVIDIA L60 GPU** 进行实验。
*   **模型基础**：使用 `MiniCPM-V-2.6-int4` 作为基础 MLLM。
*   **微调技术**：采用 LoRA（低秩适配），秩 $r=8$，仅训练适配器参数以节省算力。
*   **训练参数**：每个任务进行 50 轮联邦训练，本地 1 个 epoch，学习率 $2 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的基准数据集上进行了测试，并设置了两种异构程度（Dirichlet $\alpha=0.5$ 和 $0.1$）。
*   **消融实验**：非常充分。分别验证了 MA-EWC、PPFR、TSGA 的贡献，并深入探讨了 MA-EWC 中不同模态模块（视觉 vs 语言）对性能的影响。
*   **鲁棒性分析**：包括对超参数（$\lambda, \tau$）、重放缓存大小、采样客户端数量、本地 Epoch 数量以及任务顺序异步性的敏感性分析。
*   **评价**：实验设计严谨，对比了多种 SOTA 方法，指标涵盖了准确率（Acc）、后向迁移（BWT）和前向迁移（FWT），能够客观反映模型性能。

### 6. 主要结论与发现
*   **性能领先**：FedCMM 在所有数据集和异构设置下均取得了最高准确率，且在 BWT 指标上表现优异，证明其能有效抑制遗忘。
*   **模态保护的必要性**：实验显示，独立保护视觉和语言模块比统一保护效果更好，证明了 MLLM 遗忘机制的模块化特征。
*   **收敛效率**：FedCMM 达到目标准确率的速度比传统方法快 1.6x 到 2.25x，具有更高的通信效率。

### 7. 优点与亮点
*   **多层级协同**：将正则化、重放和聚合优化有机结合，形成闭环防御。
*   **隐私与效能平衡**：通过嵌入级重放而非原始数据重放，既保护了隐私又降低了生成原始图像/长文本的计算开销。
*   **模态感知设计**：深入理解了 MLLM 的架构特性，针对性地解决了跨模态对齐中的知识丢失问题。

### 8. 不足与局限
*   **超参数敏感性**：正则化系数 $\lambda$ 和相似度阈值 $\tau$ 对结果影响较大，目前仍需手动调优，缺乏自适应机制。
*   **计算开销**：虽然使用了轻量级生成器，但每个客户端仍需额外训练一个生成模型，对端侧设备的算力有一定要求。
*   **任务长度限制**：实验主要集中在 3 个任务的短序列，对于极长序列（如 20+ 任务）的累积误差和生成器质量退化问题尚待研究。

（完）
