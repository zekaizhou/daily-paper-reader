---
title: A Step Towards Robust Unsupervised Domain Adaptation via Fine-Tuning and Reinforcement Learning
title_zh: 迈向鲁棒无监督领域自适应：一种基于微调与强化学习的方法
authors: "Sushant Dagaji Desale, Rahul Mishra, Ashutosh Kumar Sinha"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03600v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 使用CLIP视觉编码器和微调的无监督领域自适应
tldr: 本研究针对无监督领域自适应（UDA）中的对抗鲁棒性挑战，提出了SFT+RL框架。该框架基于CLIP预训练模型，通过监督微调（SFT）在源域进行对抗训练，并结合强化学习（RL）阶段的置信度引导伪标签策略，逐步标注目标域样本。该方法有效解决了伪标签噪声和跨域分布偏移问题，在提升模型准确性的同时显著增强了对抗攻击下的鲁棒性，为鲁棒UDA提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03600v1/fig-001.webp\", \"caption\": \"Figure 1: Adversarial domain alignment challenge: Clean source samples (blue), clean target samples (green), and adversarial target samples (red) tend to form indistinguishable clusters in CLIPs embedding space.\", \"page\": 1, \"index\": 1, \"width\": 535, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-002.webp\", \"caption\": \"Table 2: Comparison on PGD 20 attack at ϵ = 2/255 using OfficeHome. Avg accuracy calculated using tables (a) and (b). (a)\", \"page\": 6, \"index\": 2, \"width\": 985, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-003.webp\", \"caption\": \"Figure 2: Schematic representation of the SFT+RL framework: (a) In this stage, the model is fine-tuned by updating only the CLIP Vision Projection (CVP) layer (partially trainable) and the Linear Classifier (fully trainable), while keeping the Image Encoder (IE), Text Encoder (TE) and CLIP Text Projection (CTP) layer completely frozen. (b) This stage builds on the fine-tuned model to perform adversarial training on the target domain. It utilizes Clean Labels from the source domain and Pseudo Labels (PL) from the target domain. During this stage, both the CVP and Linear Classifier are fully trainable, while the IE, TE and CTP remain frozen. (BP: Backpropagation, SFT: Supervised Fine Tuning, CL: Clean, PGD: Projected Gradient Descent, ADV: Adversarial)\", \"page\": 3, \"index\": 3, \"width\": 829, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-004.webp\", \"caption\": \"Table 1: Comparison on PGD 20 attack at ϵ = 2/255 using PACS dataset. Avg accuracy calculated using tables (a) and (b). (a)\", \"page\": 5, \"index\": 4, \"width\": 987, \"height\": 423}]"
motivation: 现有的无监督领域自适应方法在对抗攻击下容易产生错误的伪标签，难以在分类准确性与对抗鲁棒性之间取得理想平衡。
method: 提出一种结合监督微调与强化学习的两阶段框架，利用CLIP的语义先验进行对抗训练，并通过置信度引导的伪标签策略处理目标域数据。
result: "在OfficeHome、PACS和VisDA等基准数据集上，该方法将干净样本准确率提升了10.2%，对抗鲁棒性提升了15.8%，显著优于现有最先进技术。"
conclusion: SFT+RL框架通过动态伪标签过滤和跨域对抗训练，成功增强了模型在未标记目标域上的泛化能力与抗干扰能力。
---

## 摘要
由于噪声伪标签以及干净源域与对抗扰动目标域之间固有的分布偏移，无监督领域自适应（UDA）中的对抗鲁棒性仍然是一个重大挑战。现有方法往往无法在鲁棒性与准确性之间取得最佳平衡，因为领域自适应模型生成的伪标签在对抗攻击下容易引入分类错误。在这项工作中，我们提出了 SFT+RL，这是一个在 CLIP 预训练视觉编码器之上集成有监督微调（SFT）和强化学习（RL）的两阶段鲁棒 UDA 框架。在 SFT 阶段，我们在有标签的源域上使用基于 PGD 的扰动对抗性地微调线性分类器，同时部分解冻 CLIP 的投影层。这使得模型在适应对抗噪声的同时，能够保留 CLIP 丰富的语义先验。我们在 RL 阶段引入了一种置信度引导的伪标签策略，以逐步标注无标签的目标样本。伪标签通过衰减的置信度阈值进行过滤，以平衡质量和覆盖范围，模型在由干净源样本与高置信度目标样本组成的复合数据集上进行训练。对抗训练被应用于干净示例和对抗示例的混合批次，以增强跨域鲁棒性。在 OfficeHome、PACS 和 VisDA 三个基准数据集上的全面评估证明了我们方法的有效性。值得注意的是，SFT+RL 在这三个数据集上的干净准确率平均提高了 10.2%，对抗鲁棒性平均提高了 15.8%，优于现有的最先进方法。

## Abstract
Adversarial robustness in Unsupervised Domain Adaptation (UDA) remains a significant challenge due to noisy pseudo labels and inherent distributional shifts between the clean source and adversarially perturbed target domains. Existing approaches often fail to achieve an optimal trade-off between robustness and accuracy, as pseudo-labels generated by domain-adapted models tend to introduce classification errors under adversarial attacks. In this work, we propose \textbf{SFT+RL}, a two-stage robust UDA framework that integrates Supervised Fine Tuning (SFT) and Reinforcement Learning (RL) on top of CLIP's pre-trained visual encoder. In the SFT stage, we adversarially fine-tune a linear classifier using PGD-based perturbations over the labelled source domain while partially unfreezing CLIP's projection layer. It allows adaptation to adversarial noise while preserving CLIP's rich semantic priors. We introduce a confidence-guided pseudo-labeling strategy in the RL stage to annotate unlabeled target samples progressively. Pseudo labels are filtered using a decaying confidence threshold to balance quality and coverage, and the model is trained on a composite dataset formed by combining clean source samples with high-confidence target samples. Adversarial training is applied to mixed batches of clean and adversarial examples to enhance cross-domain robustness. Comprehensive evaluations on three benchmark datasets OfficeHome~\cite{tomm-ude}, PACS~\cite{pacs}, and VisDA~\cite{visda} demonstrate the effectiveness of our approach. Notably, \textbf{SFT+RL} achieves average improvements of \textbf{10.2\%} in clean accuracy and \textbf{15.8\%} in adversarial robustness across all three datasets, outperforming existing state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SFT+RL** 的两阶段框架，旨在解决视觉语言模型（如 CLIP）在无监督领域自适应（UDA）任务中面临的对抗攻击脆弱性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在没有目标域标签的情况下，使模型既能适应目标域的分布偏移（Domain Shift），又能抵御恶意对抗扰动（Adversarial Attacks）。
*   **研究背景**：
    *   视觉语言模型（VLM）如 CLIP 虽然在干净数据上泛化性强，但在微小的对抗扰动下，视觉特征会与文本嵌入脱钩，导致分类崩溃。
    *   现有的 UDA 方法要么缺乏对抗防御机制，要么在生成伪标签时容易引入噪声，导致鲁棒性与准确性之间难以平衡。
    *   **视觉过拟合**（过分关注纹理/颜色）和**文本冗余**（激活非语义维度）是导致失败的主要原因。

### 2. 提出的方法论：SFT+RL 框架
该方法分为两个阶段，旨在利用 CLIP 的语义先验并逐步增强其鲁棒性：

*   **第一阶段：有监督微调 (SFT)**
    *   **核心思想**：在有标签的源域上进行“加固”。
    *   **技术细节**：使用 PGD（投影梯度下降）算法在源域生成对抗样本。
    *   **部分解冻策略**：保持 CLIP 的 ViT 主干冻结，仅微调线性分类器头和 CLIP 的视觉投影层（Projection Layer）。这能防止模型破坏预训练的语义空间，同时适应对抗噪声。
*   **第二阶段：强化学习/反馈驱动适配 (RL)**
    *   **核心思想**：利用目标域无标签数据进行自适应。
    *   **置信度引导的伪标签**：通过模型预测的后验概率为目标域样本分配伪标签。
    *   **课程学习机制（阈值衰减）**：引入动态衰减的置信度阈值 $\tau$。初始阈值较高（如 0.85），确保只选取高质量伪标签；随着训练进行，阈值逐渐降低（不低于 0.7），逐步引入更多目标样本。
    *   **复合训练**：将干净源样本与高置信度目标样本混合，进行联合对抗训练，确保跨域一致性。

### 3. 实验设计
*   **数据集**：
    *   **OfficeHome**：包含 4 个域（艺术、剪贴画、产品、真实世界），65 个类别。
    *   **PACS**：包含 4 个域（照片、艺术画、卡通、素描），7 个类别。
    *   **VisDA**：大规模合成到真实的自适应基准，12 个类别。
*   **对比基线 (Benchmarks)**：
    *   传统 UDA 方法：DANN。
    *   对抗鲁棒 UDA 方法：UDA+AT、UDA+TRADES、UDA+MART、ARTUDA、SRoUDA、DART。
*   **攻击设置**：主要使用 PGD-20 攻击（$\epsilon = 2/255$ 或 $8/255$），并测试了 FGSM 和 CW 攻击。

### 4. 资源与算力
*   **模型架构**：基于 CLIP 的 ViT-B/32 视觉编码器。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和总训练时长。
*   **实现细节**：提到由于对抗样本生成的内存限制，**Batch Size 设置为 8**。优化器使用 Adam，学习率在 SFT 阶段为 $10^{-3}$ 到 $10^{-5}$，在 RL 阶段固定为 $10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上进行了详尽的跨域迁移实验（如 OfficeHome 的 12 种迁移组合）。
*   **消融实验**：专门分析了 SFT 阶段和 RL 阶段的各自贡献，以及置信度阈值衰减策略的敏感性。
*   **充分性评价**：实验设计较为充分，涵盖了不同扰动大小（$\epsilon$）和不同攻击迭代次数对性能的影响。对比方法均为近年来该领域的最先进技术（SOTA），实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：SFT+RL 在三个数据集上平均提升了 **10.2% 的干净准确率**和 **15.8% 的对抗鲁棒性**。
*   **OfficeHome 表现**：相比 DANN，鲁棒性从 1.4% 飙升至 54.5%，干净准确率从 57.8% 提升至 69.5%。
*   **关键发现**：部分解冻投影层比全量微调或完全冻结更能平衡“稳定性”与“自适应性”；动态阈值策略能有效过滤对抗攻击诱导的噪声标签。

### 7. 优点与亮点
*   **两阶段解耦**：将源域加固与目标域适配分离，逻辑清晰，易于实现。
*   **利用 VLM 先验**：巧妙利用 CLIP 的预训练特征，避免了从头训练对抗模型的高昂成本。
*   **课程学习思想**：通过阈值衰减逐步引入目标域知识，有效缓解了 UDA 中常见的“错误累积”问题。

### 8. 不足与局限
*   **算力开销**：虽然只微调了投影层，但 PGD-20 迭代攻击在训练过程中依然会显著增加计算耗时。
*   **Batch Size 限制**：由于内存压力，Batch Size 仅为 8，这可能限制了模型在更复杂任务上的收敛速度和稳定性。
*   **模型局限**：实验仅限于 CLIP (ViT-B/32)，未在更大规模的 VLM（如 ViT-L 或 InternVL）或其他多模态架构上验证通用性。
*   **强化学习定义**：文中的“强化学习”更接近于带反馈的自训练（Self-training），而非标准的基于奖励函数的策略梯度或 Q-Learning 算法。

（完）
