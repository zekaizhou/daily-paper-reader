---
title: "SAMPLe: SAM-based Optimizer for Prompt Learning in VLMs"
title_zh: SAMPLe：视觉语言模型中基于 SAM 的提示学习优化器
authors: "Hossein Rajoli, Fatemeh Lotfi, Niloufar Alipour Talemi, Hossein Kashiani, Xiaolong Ma, Fatemeh Afghah"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05727v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 用于VLM提示学习的锐度感知最小化，以提高对未见数据的泛化能力
tldr: 针对视觉语言模型（VLM）在提示学习中面临的性能与泛化性失衡问题，本文提出了一种名为 SAMPLe 的锐度感知优化器。该方法通过引入锐度感知最小化（SAM）机制，在优化过程中动态考虑损失平面的局部曲率，从而有效缓解模型对训练数据的过拟合。SAMPLe 具有即插即用的特性，可无缝集成至 CoOp、MaPLe 等多种主流框架中。实验证明，该优化器在提升特定任务准确率的同时，显著增强了模型对未知分布的泛化能力，为 VLM 提示学习提供了稳健的优化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05727v1/fig-001.webp\", \"caption\": \"Table 5: Hyperparameter settings for SAM, SAGM, and SAMPLe applied to CoOp, CoCoOp, MaPLe, Co-Prompt, and TCP during training on all 11 datasets (in Table. 1).\", \"page\": 26, \"index\": 1, \"width\": 728, \"height\": 693}, {\"url\": \"assets/figures/arxiv/2607.05727v1/fig-002.webp\", \"caption\": \"Table 3: Performance improvement by SAMPLe optimizer in zero-shot domain generalization.\", \"page\": 15, \"index\": 2, \"width\": 588, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2607.05727v1/fig-003.webp\", \"caption\": \"Table 2: Performance improvement by SAMPLe optimizer in zero-shot cross-dataset.\", \"page\": 14, \"index\": 3, \"width\": 723, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2607.05727v1/fig-004.webp\", \"caption\": \"Table 1: Comparison of different prompt learning methods on base and new classes. For each method, the last four rows show performance improvements when using the SAM, FSAM, SAGM, and the proposed SAMPLe optimizers.\", \"page\": 13, \"index\": 4, \"width\": 742, \"height\": 953}, {\"url\": \"assets/figures/arxiv/2607.05727v1/fig-005.webp\", \"caption\": \"Table 4: Details of Datasets.\", \"page\": 24, \"index\": 5, \"width\": 412, \"height\": 496}]"
motivation: 旨在解决视觉语言模型提示学习中，模型容易过拟合训练数据而导致在未知分布上泛化性能下降的权衡难题。
method: 提出一种即插即用的锐度感知优化器 SAMPLe，通过动态适应局部曲率和梯度属性来平滑损失平面，平衡探索与利用。
result: 在 CoOp、CoCoOp 和 MaPLe 等多个框架上的实验显示，SAMPLe 一致性地优于现有优化器，显著提升了模型的泛化表现。
conclusion: SAMPLe 是一种模型无关的稳健优化方案，能有效保留预训练 VLM 的泛化潜力，是提升提示学习效果的有力工具。
---

## 摘要
像 CLIP 这样预训练的视觉语言模型（VLMs）已被证明是各种下游应用中非常有效的基础模型。然而，视觉语言模型中的提示学习面临着性能与泛化能力的困境：虽然可以通过微调提示在已知分布上获得高准确率，但这一微调过程往往会损害其对未知数据的泛化能力。有限的可学习提示集（用于在预训练视觉语言模型中对输入进行上下文处理和约束，以引导其完成任务）往往会过拟合训练数据，导致特定任务性能与保持泛化能力之间存在权衡。为了解决这一困境，我们引入了 SAMPLe（锐度感知最小化提示学习），这是一种插件式的锐度感知优化器，通过考虑损失景观（loss landscape）的锐度来增强提示的泛化能力。与传统方法不同，SAMPLe 通过在每一步满足目标函数约束来平衡探索与利用，并根据局部曲率和梯度特性动态适应当前的优化状态。这种方法减少了在已知分布上的过拟合，并提高了对未知数据的适应性，从而保留了预训练视觉语言模型的泛化潜力。我们将 SAMPLe 集成到多个提示学习框架中，包括 CoOp、CoCoOp、MaPLe、TCP 和 Co-Prompt，证明了其在不同方法中的有效性。实验表明，SAMPLe 提升了提示学习框架的性能，并在各种设置下始终优于现有的优化器，使其成为一种鲁棒且与模型无关的提示学习解决方案。

## Abstract
Pre-trained Vision-Language Models (VLMs) like CLIP have proven highly effective as foundation models for various downstream applications. However, prompt learning in VLMs encounters a performance-generalization dilemma: while prompts can be tuned to achieve high accuracy on seen distributions, this tuning process often undermines their generalizability to unseen data. The limited set of learnable prompts, which contextualize and condition the input to steer it toward the task within the pretrained VLM, tends to overfit the training data, leading to a trade-off between task-specific performance and preserving generalization. To address this dilemma, we introduce SAMPLe (Sharpness-Aware Minimization Prompt Learning), a plug-in sharpness-aware optimizer that enhances prompt generalizability by accounting for loss landscape sharpness. Unlike conventional methods, SAMPLe balances exploration and exploitation by satisfying objective function constraints at each step, dynamically adapting to the current optimization state based on the local curvature and gradient properties. This approach reduces overfitting on seen distributions and improves adaptability to unseen data, preserving the generalization potential of pre-trained VLM models. We integrate SAMPLe into multiple prompt learning frameworks, including CoOp, CoCoOp, MaPLe, TCP, and Co-Prompt, demonstrating its effectiveness across diverse methods. Experiments show that SAMPLe elevates prompt learning frameworks and consistently outperforms existing optimizers across diverse settings, establishing itself as a robust, model-agnostic solution for prompt learning.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SAMPLe: SAM-based Optimizer for Prompt Learning in VLMs》** 的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM，如 CLIP）在进行提示学习（Prompt Learning）时面临**“性能-泛化困境”**。
*   **研究背景**：虽然通过微调极少数的可学习提示（Prompts）可以显著提升模型在特定下游任务上的准确率，但由于提示参数空间极小，模型极易过拟合训练数据（Seen classes），导致其在未见类别（Unseen classes）或新领域上的泛化能力大幅下降。
*   **研究动机**：已有研究表明，泛化差距与损失平面（Loss Landscape）的锐度（Sharpness）密切相关。锐利极小值（Sharp Minima）通常对应较差的泛化性。本文旨在通过优化算法引导提示学习进入更平坦的区域，从而在不牺牲特定任务性能的前提下增强泛化能力。

### 2. 论文提出的方法论：SAMPLe
*   **核心思想**：提出一种名为 **SAMPLe**（Sharpness-Aware Minimization Prompt Learning）的优化器。它在锐度感知最小化（SAM）的基础上，通过动态平衡“利用”（最小化经验风险）与“探索”（寻找平坦区域）来优化提示。
*   **关键技术细节**：
    *   **双目标优化**：SAMPLe 不仅寻找低损失点，还确保该点处于平坦区域。
    *   **梯度对齐与正交约束**：
        1.  **利用（Exploitation）**：确保扰动点的梯度更新与原始经验风险最小化（ERM）梯度方向一致。
        2.  **探索（Exploration）**：通过约束扰动点的梯度与“全批次梯度”（Full-batch gradient，通过 EMA 指数移动平均近似）正交，强制模型避开由全批次偏差引起的锐利区域，寻找更具鲁棒性的局部特征。
    *   **动态适应**：根据当前的优化状态（局部曲率和梯度属性），自动调整梯度更新步长，无需像前人工作（如 GCSCoOP）那样过度依赖手动调节超参数。
*   **算法流程**：在每一步迭代中，计算原始梯度 $\nabla L$，利用 EMA 更新全局梯度估计，计算 SAM 扰动 $\epsilon^\star$，然后在扰动点计算梯度，并应用正交分量修正，最后更新提示参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **基础到新类泛化（Base-to-New）**：在 11 个数据集（如 ImageNet, Caltech101, OxfordPets, Flowers102 等）上训练基础类，测试新类。
    *   **跨数据集泛化**：在 ImageNet 上训练，直接在其他 10 个数据集上进行零样本测试。
    *   **跨域泛化**：在 ImageNet 上训练，在 ImageNet-V2、Sketch、A（对抗）、R（渲染）等变体上测试。
*   **Benchmark（基准框架）**：集成了 CoOp, CoCoOp, MaPLe, TCP 和 Co-Prompt 五种主流提示学习框架。
*   **对比方法**：对比了原始优化器（SGD/Adam）、SAM、F-SAM（Friendly SAM）和 SAGM（Sharpness-Aware Gradient Matching）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到严格遵循了原始框架（如 CoOp, MaPLe）的超参数设置，包括学习率调度、Backbone 架构和提示长度，并使用了三个随机种子取平均值。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 11 个核心数据集的完整测试。
    *   针对 5 种不同的提示学习架构进行了适配实验。
    *   进行了详细的消融实验，验证了目标函数中各项（如扰动项、对齐项）的贡献。
    *   进行了超参数敏感性分析（如扰动半径 $\rho$ 和 EMA 系数 $\lambda$）。
*   **充分性与客观性**：实验设计非常全面，不仅在准确率上进行了对比，还通过损失平面可视化直观展示了 SAMPLe 寻找平坦极小值的能力。使用多个随机种子和多种主流框架作为 Baseline，保证了结果的客观性和公平性。

### 6. 论文的主要结论与发现
*   **性能提升**：SAMPLe 在所有 5 种框架和所有测试场景下均一致性地优于现有的 SAM 变体（如 F-SAM, SAGM）。
*   **平衡能力**：在 Base-to-New 任务中，SAMPLe 显著提升了新类（New）的准确率，同时较好地保持了基础类（Base）的性能，从而获得了最高的调和平均值（HM）。
*   **鲁棒性**：实验证明 SAMPLe 对超参数（如扰动半径）的敏感度较低，表现出比传统 SAM 更稳健的优化轨迹。
*   **收敛性**：理论证明了 SAMPLe 具有 $O(\log T / \sqrt{T})$ 的收敛速率，与常规 SGD/Adam 相当。

### 7. 优点
*   **即插即用**：作为一种优化器，它可以无缝集成到现有的任何提示学习框架中，无需修改模型架构。
*   **理论支撑**：提供了收敛性证明和梯度正交性的数学分析。
*   **动态平衡**：通过引入全批次梯度偏差修正，解决了传统 SAM 在小参数空间（提示学习）中容易陷入局部锐利区域的问题。
*   **泛化性强**：在跨数据集和跨域任务中表现优异，证明了其学习到的提示具有更强的语义鲁棒性。

### 8. 不足与局限
*   **计算开销**：作为 SAM 类方法，SAMPLe 在每次迭代中需要进行两次前向和后向传播（计算原始梯度和扰动点梯度），这使得**训练时间翻倍**。
*   **超参数依赖**：虽然比前人方法更自适应，但仍引入了 $\alpha$（对齐系数）等超参数，在极端不同的任务中可能仍需微调。
*   **算力信息缺失**：未提供具体的训练资源消耗数据，不便于开发者评估部署成本。
*   **应用范围**：目前仅在视觉语言模型（VLM）的提示学习上验证，尚未探讨其在纯文本大模型（LLM）或其他参数高效微调（PEFT）方法（如 LoRA）中的表现。

（完）
