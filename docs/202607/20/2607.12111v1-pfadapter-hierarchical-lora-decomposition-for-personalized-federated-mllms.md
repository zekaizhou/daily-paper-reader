---
title: "PFAdapter: Hierarchical LoRA Decomposition for Personalized Federated MLLMs"
title_zh: PFAdapter：用于个性化联邦多模态大语言模型的分层 LoRA 分解
authors: "Jing Liu, Kun Yang, Yan Wang, Dingkang Yang, Xiaoshuai Hao, Wei Zhang, Yang Liu, Wei Zhou"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12111v1"
tags: ["query:vlm-da"]
score: 8.0
evidence: MLLM的联邦微调，用于本地自适应和领域不变特征
tldr: 针对分布式网络中多模态大语言模型（MLLM）联邦微调面临的全局知识聚合与本地个性化平衡难题，本文提出PFAdapter框架。该框架通过分层LoRA分解，将适配器参数划分为全局共享（Q、K投影）和本地私有（V、O投影）部分，并结合正交正则化防止冗余。该方法在显著降低通信开销的同时，提升了模型在多样化边缘智能任务中的个性化表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12111v1/fig-001.webp\", \"caption\": \"TABLE I: Performance comparison across datasets on the aligned-modal scenario (α = 0.5). Learned federated baselines are reported as mean ± standard deviation over three seeds, and best results are highlighted in bold.\", \"page\": 8, \"index\": 1, \"width\": 1060, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2607.12111v1/fig-002.webp\", \"caption\": \"Fig. 2: Architecture of PFAdapter. The framework decomposes LoRA adapters into global-shared components (qp, kp) and local-private components (vp, op). Orthogonality regularization enforces knowledge disentanglement between these sets.\", \"page\": 5, \"index\": 2, \"width\": 1070, \"height\": 349}]"
motivation: 传统的联邦学习协议在处理异构网络下的多模态大模型时，难以有效区分全局通用特征与本地特定知识，且通信开销巨大。
method: 提出分层LoRA分解技术，将Q、K投影设为全局同步以捕获通用语义，将V、O投影保留在本地进行个性化适配，并引入正交正则化。
result: "在多个数据集上相比现有技术提升了2.4%至4.8%的准确率，并减少了近50%的通信成本。"
conclusion: PFAdapter为资源受限的通信网络中部署智能体AI提供了一种高效且兼顾个性化的联邦学习解决方案。
---

## 摘要
代理式人工智能（Agentic AI）系统正在通过部署能够在维护网络边缘数据隐私的同时进行协作学习的自主智能体，重塑通信与网络。在分布式网络环境中，多模态大语言模型（MLLMs）作为边缘设备的认知引擎，但在异构网络条件下，联邦微调在平衡全局知识聚合与本地自适应方面面临重大挑战。传统的联邦协议通常依赖于统一的参数聚合，这会将领域不变特征与客户端特定的细微差别混为一谈，从而导致个性化效果欠佳以及过高的通信开销。为了应对这些挑战，我们提出了 PFAdapter，这是一个通信高效的框架，引入了分层 LoRA 分解，将适配器参数显式分离为全局共享组件和本地私有组件。查询（Query）和键（Key）投影被分配用于全局同步，以捕获跨网络的通用多模态语义，而值（Value）和输出（Output）投影则保持本地化，用于边缘特定的自适应。此外，基于 Frobenius 范数的正交正则化强制执行这些组件之间的严格分离，防止冗余特征学习。选择性聚合协议仅在联邦网络中同步全局共享组件，从而保留了本地专业知识，并将通信成本降低了近 50%。在 VQA-RAD、SLAKE、Hateful Memes 和 CrisisMMD 数据集上的广泛实验表明，PFAdapter 一贯优于最先进的基准模型，在各种边缘智能任务中实现了 2.4% 至 4.8% 的准确率提升。因此，我们的框架为资源受限的通信网络中的代理式人工智能部署提供了一种高效的解决方案。

## Abstract
Agentic AI systems are reshaping communications and networking by deploying autonomous intelligent agents capable of collaborative learning while maintaining data privacy at network edges. Within distributed network environments, Multimodal Large Language Models (MLLMs) serve as cognitive engines for edge devices, yet federated fine-tuning faces substantial challenges in balancing global knowledge aggregation with local adaptation under heterogeneous network conditions. Conventional federated protocols typically rely on uniform parameter aggregation, which conflates domain-invariant features with client-specific nuances, thereby resulting in suboptimal personalization and excessive communication overhead. To address these challenges, we propose PFAdapter, a communication-efficient framework introducing hierarchical LoRA decomposition to explicitly separate adapter parameters into global-shared and local-private components. Query and key projections are assigned to global synchronization for capturing universal multimodal semantics across the network, while value and output projections remain localized for edge-specific adaptation. Additionally, orthogonality regularization based on the Frobenius norm enforces strict separation between these components, preventing redundant feature learning. Selective aggregation protocols synchronize only global-shared components across the federated network, preserving local expertise and reducing communication costs by nearly 50%. Extensive experiments on VQA-RAD, SLAKE, Hateful Memes, and CrisisMMD datasets demonstrate that PFAdapter consistently outperforms state-of-the-art baselines, achieving accuracy improvements ranging from 2.4% to 4.8% across diverse edge intelligence tasks. Consequently, our framework establishes an efficient solution for agentic AI deployment in resource-constrained communication networks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PFAdapter** 的框架，旨在解决多模态大语言模型（MLLM）在联邦学习环境下的个性化与通信效率问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：随着代理式 AI（Agentic AI）的发展，MLLM 被部署在边缘设备上处理多模态数据。联邦学习（FL）成为保护隐私、实现协作学习的关键技术。
*   **核心问题**：
    *   **权重冲刷（Weight Washing）**：传统联邦协议对所有参数进行统一聚合，导致本地特定的知识被全局平均值稀释，难以实现有效的个性化。
    *   **通信瓶颈**：即便使用 LoRA 等参数高效微调技术，同步数亿参数对带宽受限的边缘网络（如 5-20 Mbps）仍是巨大负担。
    *   **知识纠缠**：通用语义特征与本地特定任务特征在适配器参数中混杂，缺乏明确的分离机制。

### 2. 方法论：分层 LoRA 分解与正交正则化
PFAdapter 的核心思想是根据自注意力机制中不同投影矩阵的功能角色进行“手术级”的参数拆分。
*   **分层 LoRA 分解**：
    *   **全局共享组件 ($\Theta_G$)**：将 **Query (Q)** 和 **Key (K)** 投影的 LoRA 适配器设为全局同步。研究认为 Q 和 K 主要负责捕获跨模态的结构化对齐和通用语义关系，具有较强的领域不变性。
    *   **本地私有组件 ($\Theta_L$)**：将 **Value (V)** 和 **Output (O)** 投影的 LoRA 适配器保留在本地。V 和 O 负责生成具体的语义内容，与本地任务和数据分布（如特定医疗设备成像风格）高度相关。
*   **正交驱动的知识解耦**：
    *   引入基于 **Frobenius 范数** 的正交正则化损失函数 ($L_o$)。
    *   通过最小化全局与本地权重矩阵乘积的范数，强制两者在参数空间中学习非冗余的特征，确保本地适配器专注于捕获全局模型无法覆盖的细微差别。
*   **选择性聚合协议**：
    *   在每一轮通信中，客户端仅上传 Q 和 K 的适配器参数。服务器聚合后下发更新后的全局参数，而本地的 V 和 O 参数在整个训练过程中不离开设备。

### 3. 实验设计
*   **数据集与场景**：
    *   **医疗 VQA**：VQA-RAD（放射影像问答）、SLAKE（双语医疗问答）。
    *   **社交多模态**：Hateful Memes（仇恨言论检测）、CrisisMMD（灾害救援图像文本分类）。
    *   **异构设置**：采用 Dirichlet 分布 ($\alpha=0.5$) 模拟非独立同分布（Non-IID）的数据分布。
*   **基准方法（Benchmarks）**：
    *   对比了多种联邦优化算法：**FedYogi**（此前最强基准）、**FedAdam**、**FedAvgM**、**FedAdagrad**。
    *   对比了个性化联邦 LoRA 方法：**pFedLoRA**。
    *   包含 **Zero-shot**（预训练模型直接推理）和 **Local-only**（仅本地训练）作为上下界参考。

### 4. 资源与算力
*   **硬件**：单张 **Nvidia L60 GPU (48GB VRAM)**。
*   **模型配置**：使用 **MiniCPM-V-2.6-int4** 作为骨干网络（基于 Qwen2），采用 8-bit 量化。
*   **训练细节**：LoRA Rank $r=8$；每轮采样 2 个客户端（共 10 个）；总计 50 轮通信；每轮本地训练 1 个 Epoch。
*   **效率提升**：相比全参数微调，VRAM 占用从 42.5GB 降至 15.8GB；相比标准联邦 LoRA，单轮通信量从 **617MB 降至 315MB**（减少约 49%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流数据集，每组实验重复 3 次并报告均值与标准差。
*   **消融实验**：针对分层拆分、正交损失、选择性聚合分别做了消融，验证了各组件的必要性。
*   **鲁棒性分析**：测试了不同 Non-IID 程度（$\alpha$ 从 0.1 到 5.0）、模态缺失（50% 客户端缺失图像或文本）、高斯噪声干扰等极端场景。
*   **评价**：实验设计较为全面，不仅关注准确率，还深入分析了通信成本、收敛速度和冷启动能力，客观性较强。

### 6. 主要结论与发现
*   **性能提升**：PFAdapter 在各数据集上比 FedYogi 提升了 **2.4% 至 4.8%** 的准确率。
*   **通信减半**：通过仅同步 Q/K 适配器，在不损失性能的前提下节省了近 **50%** 的带宽。
*   **收敛更快**：正交正则化减少了参数冲突，使模型在更少的通信轮数内达到更高的精度。
*   **个性化优势**：在数据异构性越强（$\alpha$ 越小）的情况下，PFAdapter 相比传统方法的领先优势越明显。

### 7. 优点与亮点
*   **架构洞察**：不同于以往粗暴的层级拆分（如只留最后几层），该方法深入 Transformer 内部，基于 QKV 的功能差异进行拆分，更符合大模型的内在机理。
*   **双赢方案**：同时解决了“个性化”和“通信效率”两个通常互斥的目标。
*   **即插即用**：作为一种通用的 LoRA 改进方案，易于集成到现有的联邦学习框架中。

### 8. 不足与局限
*   **拆分比例固定**：目前统一采用 50:50 的拆分（Q/K vs V/O），但不同层或不同任务可能存在更优的动态拆分比例。
*   **计算开销**：引入正交损失计算会带来约 5% 的额外本地训练时间。
*   **隐私深度探讨不足**：虽然减少了数据传输，但未深入探讨针对 Q/K 适配器更新的梯度泄露攻击等高级安全威胁。
*   **扩展性限制**：实验主要在 10 个客户端的小规模联邦环境下进行，超大规模网络下的表现有待验证。

（完）
