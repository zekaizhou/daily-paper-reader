---
title: "The Gate Always Closes: On Injecting Auxiliary Signals into Frozen Vision-Language Models"
title_zh: 门常闭：关于向冻结的视觉语言模型注入辅助信号的研究
authors: "Moshiur Farazi, Sameera Ramasinghe, Bekir Sait Ciftler, Mahbub Ahmed Turza, Shafin Rahman"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23335v1"
tags: ["query:vlm-da"]
score: 7.0
evidence: 视觉语言模型中LoRA微调的正规化
tldr: 本研究探讨了在冻结的视觉语言模型（VLM）中注入辅助信号时，可学习门控机制失效的问题。研究发现优化器倾向于抑制辅助信号，导致门控路径在推理时实际上处于关闭状态。为此，作者提出利用双曲几何辅助损失（如蕴含锥和角排斥）来正则化LoRA微调，并强调了嵌入范数对齐的重要性。实验表明，该方法在保持关系推理能力的同时提升了属性识别性能，并建议用固定比例注入取代可学习门控。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23335v1/fig-001.webp\", \"caption\": \"Fig. 5: Left: GQA relational questions where soft-prompt tuning fails and the geometric model succeeds. Failures cluster on spatial and containment queries — the very failure mode the entailment-cone structure of the geometric losses is designed to prevent. Right: Poincaré disk projection of the learned Lorentz manifold. Wider entailment cones near the origin correspond to more general regions (e.g., “room”, “field”); specific objects (e.g., “table”, “horse”) sit near the boundary, reflecting the IoA-driven spatial hierarchy learnt from containment cues.\", \"page\": 15, \"index\": 1, \"width\": 716, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2607.23335v1/fig-002.webp\", \"caption\": \"Fig. 1: Pipeline overview. From class-agnostic region proposals, we compute dense visual relations, embed them on the Lorentz hyperboloid, and route the geometric signal through two injection mechanisms: attention bias (Mechanism A) and gated prefix tokens (Mechanism B). Auxiliary losses Lent and Lang regularise LoRA during training; both gates β, γ collapse to zero under task-only loss.\", \"page\": 4, \"index\": 2, \"width\": 721, \"height\": 370}]"
motivation: 旨在解决在视觉语言模型中引入辅助信号时，可学习门控参数常被优化器抑制导致信号失效的问题。
method: 提出使用双曲视觉关系图的几何辅助损失正则化LoRA微调，并采用嵌入范数对齐的固定比例注入机制。
result: 实验证明该方法在GQA和VSR任务中有效保留了空间关系信号，并显著提升了模型对属性类问题的处理能力。
conclusion: 开发者应弃用可学习门控，转而使用范数对齐的固定注入方式，并结合几何损失来增强模型的视觉关系表征。
---

## 摘要
视觉语言模型（VLM）中的辅助信号路径通常配备可学习的门控，以便优化器决定允许多少信号通过。我们发现优化器几乎总是选择零：在五种注入设计中，每个门控路径在行为上都变得封闭，即使门控参数名义上允许 30-45% 的信号通过，推理时消融该路径对准确率也没有影响。我们将这种抑制现象归因于两种机制：一种是通过图像衍生信号的标题不变性（caption-invariance）形式化的死梯度机制，另一种是辅助信号主动损害损失函数的负效用机制。我们并非对抗这种抑制，而是利用它：我们使用来自双曲视觉关系图（由 IoA 驱动的蕴含锥和洛伦兹流形上的角排斥）的几何辅助损失来正则化 LoRA 微调，这些损失仅在训练时的前向传播中耦合，并在推理时丢弃。按问题类型分解 GQA 结果揭示了一种清晰的分离现象。在推理时不使用几何损失的三种配置在关系类问题上损失了 2.85-3.39 个百分点，而在属性类问题上获得了约 1 个百分点的提升；第四种配置在训练时使用损失但在推理时通过软提示进行，其关系类准确率下降了 5.14 个百分点，而属性类仅提升了 0.23 个百分点，这表明如果没有几何推理路径，仅靠训练时的正则化无法保护关系准确率。在推理时保留几何路径的配置保持了原始水平的关系准确率，并匹配了属性增益。在 VSR 的分布外（OOD）测试中，RMS-prefix 方案保留了空间信号；剥离几何损失（G2）导致 VSR 准确率下降 4.6 个百分点，从而确定它们是 OOD 性能的来源。次要结果：嵌入范数对齐对于生成安全的提示注入是必要的，且可学习的门控应替换为匹配尺度下的固定、非可选注入。

## Abstract
Auxiliary signal pathways in VLMs are routinely fitted with learnable gates so the optimiser can decide how much of the signal to admit. We find that the optimiser almost always decides on zero: across five injection designs, every gated pathway becomes behaviourally closed, with accuracy invariant to ablating the pathway at inference even when the gate parameter would nominally pass 30-45% of the signal. We attribute this suppression phenomenon to two regimes, a dead-gradient regime formalised through the caption-invariance of image-derived signals, and a negative-utility regime in which the auxiliary signal actively hurts the loss. Rather than fight suppression, we exploit it: we regularise LoRA fine-tuning with geometric auxiliary losses from hyperbolic visual relational graphs (IoA-driven entailment cones and angular repulsion on the Lorentz manifold), coupled only through the forward pass at training time and dropped at inference. Disaggregating GQA by question type exposes a clean dissociation. Three configurations without geometric losses at inference lose 2.85-3.39pp on relational questions while gaining ~1pp on attribute questions; a fourth that trains with the losses but infers through a soft prompt loses 5.14pp on rel for only +0.23pp on attr, so training-time regularisation alone does not protect relational accuracy without a geometric inference pathway. Configurations that keep the geometric pathway at inference preserve vanilla-level relational accuracy and match the attribute gain. Out of distribution on VSR, the RMS-prefix recipe preserves the spatial signal; stripping the geometric losses (G2) collapses VSR by 4.6pp, isolating them as the OOD source. A secondary result: embedding-norm alignment is necessary for generation-safe prefix injection, and learnable gates should be replaced with fixed, non-optional injection at matched scales.