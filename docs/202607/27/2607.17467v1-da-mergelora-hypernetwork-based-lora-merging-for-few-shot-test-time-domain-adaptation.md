---
title: "DA-MergeLoRA: Hypernetwork-Based LoRA Merging for Few-Shot Test-Time Domain Adaptation"
title_zh: DA-MergeLoRA：基于超网络的 LoRA 合并，用于少样本测试时领域自适应
authors: "Siobhan Reid, Zhixiang Chi, Li Gu, Omid Reza Heidari, Ziqiang Wang, Yang Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17467v1"
tags: ["query:vlm-da"]
score: 10.0
evidence: 在CLIP视觉编码器上使用LoRA微调进行少样本测试时领域自适应
tldr: 针对少样本测试时域自适应（FSTT-DA）中源域知识利用不足的问题，本文提出DA-MergeLoRA框架。该方法首先为每个源域训练独立的LoRA模块以捕捉特定领域特征，随后利用基于元学习训练的超网络，根据少量目标样本动态生成列级合并权重，将多个源域LoRA融合为适配目标域的模型。该方案在保留基础模型泛化能力的同时，有效提升了在未知域的适配性能，在多个基准数据集上达到SOTA水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17467v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of our hypernetwork training procedure. Given pre-trained source LoRA modules, we meta-train a hypernetwork to dynamically predict optimal weights to merge the source LoRA modules into a target-domain specific model. At each meta-training step, one source domain is randomly sampled as the pseudo-target, while the remaining domains serve as source data. Cross-attention is computed between the processed pseudo-target support images and source LoRA columns to generate columnwise merge weights, yielding ∆Wmerged. Finally, the merged model is applied to the frozen CLIP backbone and evaluated on query images to compute the meta-training loss.\", \"page\": 6, \"index\": 1, \"width\": 702, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2607.17467v1/fig-002.webp\", \"caption\": \"Table 1: Evaluation of our method (DA-MergeLoRA) on the (a) DomainNet dataset and (b) WILDS datasets. The best CNN and best ViT methods are bolded separately. Results are reported as mean (standard deviation) over three trials with different random seeds. The † symbol denotes the official dataset metric. Our method achieves SOTA results on five of the six DomainNet domains and on three of the four official WILDS metrics.\", \"page\": 11, \"index\": 2, \"width\": 729, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2607.17467v1/fig-003.webp\", \"caption\": \"Fig. 2: Heatmaps of average merge weights. These plots illustrate the average merge weights per-attention-layer produced by the hypernetwork for each of the source domains. The x-axis shows the source domains and the y-axis shows the transformer layer. Plots (a) and (c) show the merge weights before training on FMoW (\\\"Year 2016\\\" target domain) and DomainNet (\\\"Sketch\\\" target domain). Plots (b) and (d) show results after training. Pre-training merge weights are nearly uniform; post-training, the hypernetwork prioritizes visually similar source domains over dissimilar or difficult domains.\", \"page\": 12, \"index\": 3, \"width\": 725, \"height\": 506}]"
motivation: 现有的少样本测试时域自适应方法在利用源域特定知识和捕捉跨域关系方面存在局限性。
method: 为各源域训练独立的LoRA模块，并采用超网络根据目标域样本动态生成合并因子，实现多源域知识的加权融合。
result: 在多个领域自适应基准数据集上的实验结果证明，该方法取得了当前最先进的性能表现。
conclusion: 通过超网络动态合并源域LoRA模块，能够有效实现高效且精准的少样本测试时域自适应。
---

## 摘要
少样本测试时领域自适应 (FSTT-DA) 旨在仅使用少量无标签目标样本将模型迁移到新领域。这种设置比典型的领域自适应设置更具现实意义，后者通常假设在源域训练期间可以访问目标数据。然而，先前的 FSTT-DA 方法未能有效利用源域特定的知识，它们依赖于浅层的批量归一化 (Batch Normalization) 更新、将模型视为黑盒的基于提示 (Prompt) 的方法，或者无法捕获跨领域关系的集成策略。为了解决这些局限性，我们提出了一种新的 FSTT-DA 框架，该框架将 LoRA 微调与模型合并相结合。在我们的方法中，针对每个源域，在 CLIP 的视觉编码器上分别微调独立的 LoRA 模块。由于 LoRA 仅修改模型参数的一小部分，它在保留基础模型泛化知识的同时，在内部学习领域特定的特征。为了将学习到的知识迁移到特定的目标领域，我们提出了一种通过元学习训练的超网络 (Hypernetwork)，该网络生成逐列合并因子以组合 LoRA 模块。给定一小批目标图像，超网络产生合并权重，将源域 LoRA 模块融合为单一的自适应表示。我们的结果在各种领域自适应数据集上展示了最先进的性能。我们的代码公开于 https://github.com/nahbois4321/DA-MergeLoRA。

## Abstract
Few-shot Test-Time Domain Adaptation (FSTT-DA) seeks to adapt models to novel domains using only a handful of unlabeled target samples. This setting is more realistic than typical domain adaptation setups, which assume access to target data during source training. However, prior FSTT-DA approaches fail to effectively leverage source domain-specific knowledge, relying on shallow batch normalization updates, prompt-based methods that treat the model as a black box, or ensembling strategies that do not capture cross-domain relationships. To address these limitations, we introduce a new FSTT-DA framework that integrates LoRA fine-tuning with model merging. In our approach, separate LoRA modules are fine-tuned on CLIP's vision encoder for each source domain. Since LoRA modifies only a small fraction of the model's parameters, it retains the base model's generalized knowledge while internally learning domain-specific features. To adapt the learned knowledge to a specific target domain, we propose a hypernetwork trained via meta-learning that generates per-column merging factors to combine LoRA modules. Given a small batch of target images, the hypernetwork produces merging weights that fuse source LoRA modules into a single adapted representation. Our results demonstrate state-of-the-art performance across various domain adaptation datasets. Our code is publicly available at https://github.com/nahbois4321/DA-MergeLoRA.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《DA-MergeLoRA: Hypernetwork-Based LoRA Merging for Few-Shot Test-Time Domain Adaptation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**少样本测试时领域自适应（FSTT-DA）**。在实际应用（如医疗影像或机器人导航）中，模型部署到新环境时往往面临“领域偏移”，且只能获取极少量（few-shot）且无标签的目标域样本。

**研究动机：**
*   **现有方法局限：** 传统的 BatchNorm 更新（如 MABN）在样本极少时估计噪声大；基于提示（Prompt）的方法（如 VDPG）将模型视为黑盒，无法调整内部表示；集成学习方法（如 Meta-DMoE）计算开销巨大且扩展性差。
*   **核心目标：** 如何在不破坏基础模型泛化能力的前提下，高效提取并融合多个源域的特定知识，以适配完全未知的目标域。

### 2. 方法论：核心思想与关键技术
论文提出了 **DA-MergeLoRA** 框架，将自适应问题转化为“参数空间内的模型合并”问题。

*   **核心思想：** 为每个源域训练专属的 LoRA 模块，在测试时利用一个元学习训练的“超网络”根据目标样本动态计算合并权重，将多个源域 LoRA 融合成一个目标域 LoRA。
*   **关键技术细节：**
    1.  **源域 LoRA 训练：** 在冻结的 CLIP 视觉编码器上，为 $N$ 个源域分别微调低秩适配器（LoRA）。
    2.  **超网络架构（Hypernetwork）：**
        *   **输入：** 目标域图像的平均 Embedding（经 CLIP 提取）和所有源域 LoRA 的权重列。
        *   **上下文增强：** 为 LoRA 列添加“层索引 Token”、“子层类型 Token”和“领域 Token”，使超网络感知参数的来源位置。
        *   **交叉注意力（Cross-Attention）：** 以目标域 Embedding 为 Query，以增强后的 LoRA 列为 Key，计算**逐列（Per-column）**的合并权重。
    3.  **元学习训练流程：** 采用“伪目标域”策略。在训练超网络时，随机选一个源域作为伪目标域（掩盖其对应的 LoRA），要求超网络仅利用其他源域 LoRA 合成能胜任该域任务的模型。
    4.  **推理：** 测试时，输入少量目标样本，超网络一次性生成合并权重，融合后的模型直接进行推理，无需迭代更新。

### 3. 实验设计
*   **数据集：**
    *   **DomainNet：** 包含 6 个领域（如草图、照片、剪贴画等），345 个类别。
    *   **WILDS 基准：** 包括 iWildCam（野生动物监测）、FMoW（卫星影像）、Camelyon17（医疗病理切片）和 PovertyMap（贫困预测，回归任务）。
*   **Benchmark 与对比方法：**
    *   **CNN 架构方法：** ERM, CORAL, Mixup, MABN, Meta-DMoE 等。
    *   **CLIP/ViT 架构方法：** Zero-shot CLIP, MIRO, VDPG, L2C（当前的 SOTA）。
*   **实验设置：** 采用“留一法”（Leave-one-out），即一个域作为测试目标，其余作为源域。

### 4. 资源与算力
*   **模型骨干：** 使用 OpenAI 预训练的 CLIP ViT-B/16。
*   **训练细节：** LoRA 秩（Rank）设为 16；Batch Size 为 16；超网络训练 1-5 个 Epoch。
*   **算力说明：** 论文**未明确标注**具体的 GPU 型号和总训练时长。但文中强调了该方法在推理时的优势：比之前的 FSTT-DA 框架（如 L2C）**速度快约 50%，内存占用减少约 50%**，因为它避免了运行多个专家模型或复杂的 Prompt 生成分支。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个大型基准数据集，涉及分类和回归两种任务。
*   **消融实验：** 非常充分。作者对比了：
    *   不同的 LoRA 合并策略（简单平均、熵加权平均、TIES Merging、AdaMerging）。
    *   超网络组件的作用（层 Token、领域 Token、目标图像输入等）。
    *   合并粒度的影响（逐模型、逐层、逐列）。
*   **客观性：** 实验均基于 3 个随机种子取平均值并标注标准差，对比了多种不同架构的基线模型，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先：** 在 DomainNet 上平均准确率提升了 **1.24%**；在 Camelyon17 上提升了 **2.70%**；在 FMoW 的最差情况准确率（Worst-case Acc）上大幅提升了 **11.40%**。
*   **动态选择：** 权重热图显示，超网络确实学会了根据视觉相似性分配权重（例如，当目标是“草图”时，它会自动给“剪贴画”源域分配更高权重）。
*   **逐列合并的必要性：** 实验证明，相比于粗粒度的逐层合并，逐列合并能提供更精细的特征子空间选择，从而获得更好的适配效果。

### 7. 优点（亮点）
*   **参数空间适配：** 相比于黑盒的 Prompt 方法，直接在参数空间合并能更深层地调整模型内部特征。
*   **高效推理：** 合并后的 LoRA 可以直接吸收到主模型权重中，推理时没有额外参数开销。
*   **模块化：** 这种框架允许轻松添加新的源域 LoRA，而无需重新训练整个系统。
*   **元学习策略：** 通过掩码（Masking）伪目标域的训练方式，有效模拟了测试时的未知域场景。

### 8. 不足与局限
*   **依赖源域多样性：** 如果源域数量过少或与目标域差异极大，超网络可能难以合成有效的权重。
*   **类别不平衡敏感：** 在 iWildCam 数据集上的 F1 分数略低于 L2C，反映出该方法在处理极端类别不平衡时仍有改进空间。
*   **预训练成本：** 虽然推理快，但前期需要为每个源域单独训练 LoRA，当源域数量极多时，前期准备工作量较大。
*   **应用限制：** 目前主要基于 CLIP 这种强泛化模型，在非视觉语言模型上的表现尚未验证。

（完）
