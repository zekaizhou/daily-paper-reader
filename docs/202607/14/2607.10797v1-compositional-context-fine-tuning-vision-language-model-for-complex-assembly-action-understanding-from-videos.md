---
title: Compositional Context Fine-Tuning Vision-Language Model for Complex Assembly Action Understanding from Videos
title_zh: 面向视频中复杂装配动作理解的组合上下文微调视觉语言模型
authors: "Hao Zheng, Jinyi Huang, Tiantian Zheng, Xun Xu, Tuka Alhanai"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10797v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 通过微调将视觉语言模型（VLM）适配到特定的挑战性领域
tldr: 针对人机协作装配中动作识别难的问题，本文提出了一种组合上下文微调（CCFT）方法。该方法将复杂的装配动作分解为动词、对象和工具三个语义要素，并利用视觉语言模型通过问答形式进行识别。同时，引入层分区交替训练（LP-AT）策略，通过特定要素的低秩适配器减少多任务干扰。在HA-ViD-VQA和IKEA-ASM-VQA数据集上的实验证明，该方法在识别精度和可解释性上均优于现有基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10797v1/fig-001.webp\", \"caption\": \"TABLE I: Action Recognition Accuracy on HA-ViD-VQA and IKEA-ASM-VQA. The accuracy (%) for left-hand and right-hand action recognition is reported separately under side, front, and top views for HA-ViD-VQA. Our method outperforms baselines.\", \"page\": 5, \"index\": 1, \"width\": 394, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-002.webp\", \"caption\": \"Fig. 2: Qualitative Comparison of Assembly Action Understanding Outputs from Our Fine-Tuned Model, the Base Model, and the Base Model with Contextual Prompt.\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 740}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-003.webp\", \"caption\": \"Fig. 1: The Overall Framework of Compositional Context Fine-Tuning with Layer-Partitioned Alternating Training.\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 442}]"
motivation: 传统的动作识别方法难以处理装配任务中细微的动作变化和复杂的手物交互。
method: 提出CCFT方法将动作分解为语义要素，并结合LP-AT策略利用特定层的低秩适配器进行交替训练。
result: 在两个装配视频问答数据集上的实验结果显示，该方法显著提升了识别性能并提供了可解释的预测结果。
conclusion: 该研究通过语义分解和高效微调策略，为复杂装配场景下的视觉理解提供了有效且可解释的解决方案。
---

## 摘要
装配动作理解是实现高效人机协作装配的关键，但由于动作细微以及精细的手物交互，这仍然是一项具有挑战性的任务。我们通过组合上下文微调（CCFT）将视觉语言模型（VLM）应用于这一挑战领域。该方法将装配动作分解为语义元素（动词、物体、工具），并利用模板化的问答对微调 VLM 以识别每个动作元素。这种方法确保了近乎确定性的输出。为了在有限数据下实现高效且多任务学习，本文提出了一种层分区交替训练（LP-AT）方法，该方法通过元素特定的低秩适配器（low-rank adapters）分配不同的模型层来识别特定的动作元素。LP-AT 在元素特定的适配器之间交替更新权重，在减少跨任务干扰的同时，实现了针对每个适配器的超参数优化。此外，我们基于现有的装配视频数据集构建了 HA-ViD-VQA 和 IKEA-ASM-VQA 数据集。在这些数据集上的广泛实验表明，我们的方法始终优于强大的动作识别基线模型，同时提供了可解释的元素级预测，能够支持多种下游应用。

## Abstract
Assembly action understanding is a key enabler for effective human-robot collaborative assembly, yet it remains challenging due to subtle motions and fine-grained hand-object interactions. We adapt vision-language models (VLMs) to this challenging domain with Compositional Context Fine-Tuning (CCFT), a method that decomposes assembly actions into semantic elements (Verb, Object, Tool) and fine-tunes VLMs to recognize each action element using templated question-answering pairs. This approach ensures near-deterministic outputs. To enable efficient and effective multi-task learning under limited data, a Layer-Partitioned Alternating Training (LP-AT) method is presented, which assigns distinct model layers to recognize specific action elements through element-specific low-rank adapters. LP-AT alternates weight updates across element-specific adapters, reducing cross-task interference while enabling per-adapter hyperparameter optimization. Furthermore, we create HA-ViD-VQA and IKEA-ASM-VQA datasets from existing assembly video datasets. Extensive experiments on these datasets demonstrate that our method consistently outperforms strong action recognition baselines while providing interpretable element-level predictions that can support diverse downstream applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compositional Context Fine-Tuning Vision-Language Model for Complex Assembly Action Understanding from Videos》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在人机协作（HRC）场景下，利用视觉语言模型（VLM）精确理解复杂的装配动作。
*   **研究动机**：
    *   **动作复杂性**：装配动作包含细微的手物交互，且存在“同动异义”或“异动同义”的语义模糊性。
    *   **确定性需求**：工业协作要求模型输出必须高度确定且可解释，而生成式 VLM 往往存在幻觉或输出格式不稳定的问题。
    *   **数据稀缺**：专门用于装配任务的视频问答（VQA）数据匮乏，且多任务微调时容易出现任务间干扰。

### 2. 方法论：CCFT 与 LP-AT
论文提出了两个互补的核心技术：
*   **组合上下文微调（CCFT）**：
    *   **核心思想**：将复杂的整体动作分解为三个语义要素：**动词（Verb）**、**操作对象（Object）**和**工具（Tool）**。
    *   **技术细节**：通过模板化的 VQA 对（如“视频中显示的动作动词是什么？”）进行微调，将生成任务转化为受限答案空间下的判别任务，确保输出的近确定性和可解释性。
*   **层分区交替训练（LP-AT）**：
    *   **核心思想**：针对不同要素（动词/对象/工具）分配 VLM 语言解码器的不同层组，并应用独立的 LoRA 适配器。
    *   **流程**：
        1.  将模型 $N$ 层划分为不相交的组（如前 10 层负责动词，中间 14 层负责对象等）。
        2.  **交替优化**：在每个 epoch 内，依次针对不同子任务更新其对应的 LoRA 参数，随后合并权重。
        3.  这种方式允许为不同复杂度的任务设置不同的超参数（学习率、LoRA 秩等），并有效缓解了多任务学习中的参数干扰和灾难性遗忘。

### 3. 实验设计
*   **数据集**：
    *   **HA-ViD-VQA**：基于 HA-ViD 改造，包含多视角、双手的精细装配动作（56 种动作）。
    *   **IKEA-ASM-VQA**：基于 IKEA-ASM 改造，侧重于家具组装的宏观动作（24 种动作）。
*   **基准模型（Baselines）**：对比了传统的强力动作识别模型，包括 CNN 类的 **TSM**、Transformer 类的 **UniFormerV2** 和 **VideoMAE V2**。
*   **评估指标**：采用基于语义相似度（MiniLM 嵌入）的 Top-1 准确率，阈值设为 0.95 以确保语义精确匹配。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX A6000 GPU**（每台 48GB 显存）。
*   **训练细节**：
    *   基础模型为 Qwen2.5-VL-7B。
    *   训练时长为 40 个 epoch。
    *   视频采样率为 2 fps，最大帧数限制为 76 帧。
    *   冻结了视觉编码器，仅微调语言解码器。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同风格的数据集上进行了验证，涵盖了侧视、前视、顶视等多个视角。
*   **消融实验**：非常充分。作者对比了：
    1.  组合式 vs. 非组合式微调（证明分解任务的有效性）。
    2.  层分区 vs. 共享适配器（证明层隔离的优越性）。
    3.  交替训练 vs. 独立训练 vs. 顺序训练（证明 LP-AT 解决了参数干扰和遗忘问题）。
*   **客观性**：通过与 SOTA 动作识别模型对比，证明了 VLM 在处理语义复杂动作时的潜力，实验设计逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：CCFT 方法在所有视角和数据集上均显著优于 VideoMAE V2 等传统基准。
*   **任务复杂度差异**：工具识别最准确（视觉特征明显），动词识别次之，对象识别最具挑战性（受遮挡和细微外观影响）。
*   **微调必要性**：定性分析显示，仅靠 Prompting（提示词）无法让基础 VLM 理解特定装配语义，必须进行微调。
*   **LP-AT 的关键作用**：交替训练比顺序训练平均准确率提升了约 46%，证明了其在多任务平衡上的卓越能力。

### 7. 优点与亮点
*   **可解释性强**：将动作拆解为“动词-对象-工具”，直接对应工业逻辑，便于下游机器人任务规划。
*   **架构创新**：LP-AT 提供了一种在有限数据下微调大型 VLM 的高效策略，解决了多任务干扰的痛点。
*   **实用性**：通过模板化 VQA 解决了 VLM 输出不稳定的“顽疾”，使其更符合工业应用标准。

### 8. 不足与局限
*   **实时性挑战**：基于 7B 参数量的 VLM 进行视频推理，其计算开销较大，目前可能难以达到实时 HRC 的响应要求。
*   **层分区依赖经验**：目前层组的划分（如 10:14:4）主要基于启发式经验和初步实验，缺乏自动化的最优划分理论。
*   **视觉特征受限**：由于冻结了视觉编码器，模型对极端遮挡或极细微手势的捕捉仍依赖于预训练特征，未针对装配场景进行底层视觉优化。
*   **组合逻辑简单**：最终动作的合成仅靠字符串拼接，缺乏更高级的逻辑推理来纠正单个元素的预测错误。

（完）
