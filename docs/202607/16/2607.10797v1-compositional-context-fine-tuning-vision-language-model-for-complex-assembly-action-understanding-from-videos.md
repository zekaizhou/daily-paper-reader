---
title: Compositional Context Fine-Tuning Vision-Language Model for Complex Assembly Action Understanding from Videos
title_zh: 用于视频中复杂装配动作理解的组合上下文微调视觉语言模型
authors: "Hao Zheng, Jinyi Huang, Tiantian Zheng, Xun Xu, Tuka Alhanai"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10797v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 通过微调将VLM应用于具有挑战性的动作理解领域
tldr: 本研究针对复杂装配动作理解中的细微动作和手物交互挑战，提出了组合上下文微调（CCFT）方法。该方法将动作分解为动词、物体和工具等语义元素，并结合层分区交替训练（LP-AT）策略，利用特定元素的低秩适配器优化视觉语言模型。实验证明，该方法在提升识别准确率的同时，提供了具有可解释性的元素级预测，能有效支持多样化的下游应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10797v1/fig-001.webp\", \"caption\": \"TABLE I: Action Recognition Accuracy on HA-ViD-VQA and IKEA-ASM-VQA. The accuracy (%) for left-hand and right-hand action recognition is reported separately under side, front, and top views for HA-ViD-VQA. Our method outperforms baselines.\", \"page\": 5, \"index\": 1, \"width\": 394, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-002.webp\", \"caption\": \"Fig. 2: Qualitative Comparison of Assembly Action Understanding Outputs from Our Fine-Tuned Model, the Base Model, and the Base Model with Contextual Prompt.\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 740}, {\"url\": \"assets/figures/arxiv/2607.10797v1/fig-003.webp\", \"caption\": \"Fig. 1: The Overall Framework of Compositional Context Fine-Tuning with Layer-Partitioned Alternating Training.\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 442}]"
motivation: 装配动作理解因动作细微且涉及复杂的手物交互，导致现有视觉语言模型在精细化识别上存在困难。
method: 提出CCFT方法将动作分解为语义元素，并采用层分区交替训练策略，通过元素特定的低秩适配器减少多任务学习中的干扰。
result: 在HA-ViD-VQA和IKEA-ASM-VQA数据集上的实验表明，该方法性能显著优于强基准模型且具备良好的预测可解释性。
conclusion: 通过语义分解和高效的适配器微调策略，该研究为复杂工业装配场景下的动作理解提供了有效的技术支撑。
---

## 摘要
装配动作理解是实现高效人机协作装配的关键，但由于细微的动作和细粒度的手物交互，这仍然具有挑战性。我们通过组合上下文微调（CCFT）将视觉语言模型（VLM）应用于这一具有挑战性的领域。该方法将装配动作分解为语义元素（动词、对象、工具），并利用模板化的问答对微调 VLM 以识别每个动作元素。这种方法确保了近乎确定性的输出。为了在有限数据下实现高效且多任务学习，本文提出了一种层分区交替训练（LP-AT）方法，该方法通过特定元素的低秩适配器分配不同的模型层来识别特定的动作元素。LP-AT 在特定元素的适配器之间交替更新权重，在减少跨任务干扰的同时，实现了针对每个适配器的超参数优化。此外，我们基于现有的装配视频数据集创建了 HA-ViD-VQA 和 IKEA-ASM-VQA 数据集。在这些数据集上的广泛实验表明，我们的方法始终优于强大的动作识别基准模型，同时提供了可解释的元素级预测，能够支持多种下游应用。

## Abstract
Assembly action understanding is a key enabler for effective human-robot collaborative assembly, yet it remains challenging due to subtle motions and fine-grained hand-object interactions. We adapt vision-language models (VLMs) to this challenging domain with Compositional Context Fine-Tuning (CCFT), a method that decomposes assembly actions into semantic elements (Verb, Object, Tool) and fine-tunes VLMs to recognize each action element using templated question-answering pairs. This approach ensures near-deterministic outputs. To enable efficient and effective multi-task learning under limited data, a Layer-Partitioned Alternating Training (LP-AT) method is presented, which assigns distinct model layers to recognize specific action elements through element-specific low-rank adapters. LP-AT alternates weight updates across element-specific adapters, reducing cross-task interference while enabling per-adapter hyperparameter optimization. Furthermore, we create HA-ViD-VQA and IKEA-ASM-VQA datasets from existing assembly video datasets. Extensive experiments on these datasets demonstrate that our method consistently outperforms strong action recognition baselines while providing interpretable element-level predictions that can support diverse downstream applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compositional Context Fine-Tuning Vision-Language Model for Complex Assembly Action Understanding from Videos》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在人机协作（HRC）场景下，利用视觉语言模型（VLM）精确理解复杂的装配动作。
*   **研究动机**：
    *   **动作复杂性**：装配动作包含细微的肢体运动和精细的手物交互，传统模型难以区分动作相似但语义不同的行为（如拧紧 vs. 拧松）。
    *   **VLM 的局限性**：通用 VLM 倾向于生成式输出，存在幻觉风险，且在特定工业领域缺乏确定性和解释性。
    *   **数据与效率**：装配领域标注数据有限，如何在有限数据下高效微调大规模 VLM 并处理多任务（动词、物体、工具识别）是关键挑战。

### 2. 方法论：CCFT 与 LP-AT
论文提出了两个互补的核心技术：
*   **组合上下文微调 (CCFT)**：
    *   **核心思想**：将复杂的整体动作分解为语义元素（Verb 动词, Object 物体, Tool 工具）。
    *   **技术细节**：通过模板化的视觉问答（VQA）对进行微调。例如，询问“视频中的动作动词是什么？”，模型输出预定义的标签。这种方法将生成式任务转化为受限空间下的判别任务，确保了输出的**近确定性**和**可解释性**。
*   **层分区交替训练 (LP-AT)**：
    *   **核心思想**：针对不同语义元素分配 VLM 的不同层组，并使用独立的低秩适配器（LoRA）进行训练。
    *   **关键流程**：
        1.  **层分区**：将 VLM 的解码器层划分为不相交的组（如前 10 层负责动词，中间 14 层负责物体等）。
        2.  **交替优化**：在每个训练 epoch 内，轮流更新不同任务的 LoRA 参数，并将其合并回基础模型。
        3.  **优势**：减少了多任务学习中的参数干扰，允许为不同复杂度的任务设置不同的超参数（如 Rank、学习率）。

### 3. 实验设计
*   **数据集**：
    *   **HA-ViD-VQA**：基于 HA-ViD 改造，包含多视角、双手的精细装配动作（56 种动作）。
    *   **IKEA-ASM-VQA**：基于 IKEA-ASM 改造，侧重于家具组装的宏观动作（24 种动作）。
*   **基准模型 (Benchmarks)**：
    *   **CNN 类**：TSM。
    *   **Transformer 类**：UniFormerV2。
    *   **预训练掩码自编码器**：VideoMAE V2。
*   **对比维度**：整体动作识别准确率（Top-1 Acc）以及各组成元素的识别准确率。

### 4. 资源与算力
*   **硬件配置**：使用了 **4 台 NVIDIA RTX A6000 GPU**（每台 48GB 显存）。
*   **训练细节**：
    *   基础模型为 Qwen2.5-VL-7B。
    *   训练时长为 40 个 Epoch。
    *   视频采样：最大 76 帧，处理速率为 2 fps。
    *   微调策略：冻结视觉编码器，仅微调语言解码器（28 层）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流装配数据集上进行了完整测试。
    *   针对 HA-ViD-VQA 的三个视角（侧视、前视、顶视）和左右手分别进行了详尽对比。
    *   **消融实验**：对比了“组合 vs. 非组合”、“层分区 vs. 共享适配器”、“交替训练 vs. 独立训练 vs. 顺序训练”。
*   **评价**：实验设计非常充分且客观。通过消融实验有力地证明了 LP-AT 在解决“灾难性遗忘”和“参数干扰”方面的有效性。

### 6. 主要结论与发现
*   **性能领先**：CCFT 方法在所有数据集和视角下均显著优于传统的动作识别基准模型。
*   **分解优势**：将动作分解为元素不仅提高了准确率（平均提升 2.33%），还提供了中间推理过程，增强了透明度。
*   **训练策略至关重要**：实验发现，独立训练后合并适配器会导致性能崩溃，而 LP-AT 的交替训练能保持各任务的兼容性。
*   **任务难度差异**：工具识别最准确，而物体识别在精细装配中（HA-ViD）比动词识别更难。

### 7. 亮点与优点
*   **工业实用性**：通过模板化 VQA 解决了 VLM 输出不稳定的痛点，使其更符合工业自动化对确定性的要求。
*   **架构创新**：LP-AT 提供了一种优雅的参数高效微调（PEFT）方案，解决了多任务学习中的干扰问题。
*   **可解释性**：能够清晰指出模型是错在“动词”还是“物体”上，便于下游任务（如机器人纠错）调用。

### 8. 不足与局限
*   **实时性挑战**：虽然模型精度高，但基于 7B 参数的 VLM 在实时人机协作中的推理延迟可能仍是瓶颈。
*   **层分区依赖经验**：目前的层划分策略（如 10:14:4）主要基于经验和启发式估计，缺乏自动化的最优分区理论支撑。
*   **视觉特征受限**：由于算力限制冻结了视觉编码器，可能导致模型无法充分捕捉某些极细微的手部特征。
*   **组合逻辑简单**：目前仅通过字符串拼接组合元素，缺乏更高级的逻辑推理（如利用 LLM 修正不合理的元素组合）。

（完）
