---
title: "Decoupled Visual Processing: Efficient Multimodal Adaptation via Modality-Specific Transformer Substitution"
title_zh: 解耦视觉处理：通过模态特定 Transformer 替换实现高效多模态适配
authors: "Mingkuan Feng, Zhengqi Wen, Jianhua Tao"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26596v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 通过MLLM中的模态特定Transformer替换进行高效多模态适配
tldr: 针对多模态大语言模型（MLLM）全参数微调计算成本高的问题，本文提出了解耦视觉处理（DVP）框架。该方法基于视觉与文本表征在深层网络中存在差异的发现，在解码器后半部分将两者分离，仅使用一个轻量级的独立Transformer块处理视觉标记，而冻结原始文本路径。在LLaVA-1.5上的实验证明，该方法在大幅减少训练参数的同时，在MME、POPE等基准测试中保持了竞争性能，实现了高效的多模态适配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26596v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of Decoupled Visual Processing (DVP). Input tokens pass through shared decoder layers [0, 16], then are split by modality. Visual tokens are processed by a single trainable transformer block (shown in orange), while textual tokens pass through the original frozen decoder layers [17, 31]. The outputs are concatenated before the language modeling head. Only the single transformer block is trained during instruction tuning.\", \"page\": 3, \"index\": 1, \"width\": 950, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2607.26596v1/fig-002.webp\", \"caption\": \"Table 1: Main results on multimodal benchmarks. Bold indicates the best performance and underline indicates the second best. DVP (Efficient Training) achieves competitive performance while training only ∼3.1% of the total model parameters. The single transformer block that processes visual tokens is the only trainable component.\", \"page\": 6, \"index\": 2, \"width\": 896, \"height\": 181}]"
motivation: 现有的多模态模型全参数微调成本极高，且视觉与文本标记在深层网络中的表征需求存在显著差异。
method: 在解码器中层将视觉和文本标记分离，让视觉标记通过一个新增的轻量级Transformer块，而文本标记沿用原始冻结层。
result: 在LLaVA-1.5框架下，DVP仅需训练极少比例的参数，即可在MME、POPE和ChartQA等多个基准测试中取得与全量微调相当的性能。
conclusion: 实验证明通过解耦的参数高效路径可以有效学习多模态模型中的视觉表征，为高效适配MLLM提供了新思路。
---

## 摘要
多模态大语言模型 (MLLM) 通过在统一的 Transformer 架构中集成视觉和文本理解，展现了卓越的能力。然而，为了视觉指令微调而对这些模型的所有参数进行微调，计算成本高昂且通常是不必要的，因为视觉和文本 token 的表示需求在网络的深层中存在显著差异。在本文中，我们提出了解耦视觉处理 (DVP)，这是一种高效的训练框架，它将预训练 LLM 的上层解码器层替换为一个轻量级、可独立训练的单个 Transformer 块，专门用于视觉 token 处理。具体而言，在经过解码器前一半层的共享处理后，视觉和文本 token 被拆分：视觉 token 被路由到一个新初始化的单个 Transformer 块，而文本 token 则继续通过原始的冻结解码器层。随后，这两股流在语言建模头之前进行拼接。在训练期间，仅更新该单个 Transformer 块，从而大幅减少了可训练参数的数量。在 LLaVA-1.5 框架上的实验表明，DVP 在 MME、POPE 和 ChartQA 基准测试中取得了具有竞争力的性能，同时仅训练了极少比例的总参数，这表明 MLLM 中的视觉表示可以通过解耦且参数高效的路径进行有效学习。

## Abstract
Multimodal large language models (MLLMs) have demonstrated remarkable capabilities by integrating visual and textual understanding within a unified transformer architecture. However, fine-tuning all parameters of these models for visual instruction tuning is computationally expensive and often unnecessary, as the representation requirements for visual and textual tokens diverge significantly in the deeper layers of the network. In this paper, we propose Decoupled Visual Processing (DVP), an efficient training framework that replaces the upper decoder layers of a pretrained LLM with a lightweight, independently trainable single transformer block dedicated exclusively to visual token processing. Specifically, after shared processing through the first half of the decoder layers, visual and textual tokens are split: visual tokens are routed through a newly initialized single transformer block while textual tokens continue through the original frozen decoder layers. The two streams are then concatenated before the language modeling head. During training, only the single transformer block is updated, dramatically reducing the number of trainable parameters. Experiments on the LLaVA-1.5 framework demonstrate that DVP achieves competitive performance on MME, POPE, and ChartQA benchmarks while training only a fraction of the total parameters, suggesting that visual representations in MLLMs can be effectively learned through a decoupled, parameter-efficient pathway.

---

## 论文详细总结（自动生成）

这是一份关于论文《Decoupled Visual Processing: Efficient Multimodal Adaptation via Modality-Specific Transformer Substitution》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了多模态大语言模型（MLLM）在视觉指令微调过程中的**效率与表征冗余**问题。
*   **背景**：当前的 MLLM（如 LLaVA）通常将视觉和文本 token 混合，通过 LLM 的所有解码器层。
*   **动机**：作者观察到，视觉和文本 token 在网络深层的表征需求存在显著差异。全参数微调不仅计算成本极高，而且在深层强行让视觉 token 遵循文本导向的处理路径可能并非最优。
*   **核心问题**：是否可以为视觉 token 提供一条独立的、更轻量级的深层处理路径，从而在大幅降低训练成本的同时保持甚至提升模型性能？

### 2. 方法论：解耦视觉处理 (DVP)
DVP 提出了一种“共享-拆分-处理-拼接”的架构范式：
*   **核心思想**：将 LLM 解码器分为两部分。前一半层保持共享以进行跨模态对齐，后一半层针对不同模态进行路径解耦。
*   **关键流程**：
    1.  **共享处理**：输入图像经过 Vision Encoder 和 Projector 后，与文本 token 一起进入 LLM 的前 $K$ 层（如 Vicuna-7B 的前 17 层）。
    2.  **模态拆分**：在第 $K$ 层输出处，根据 token 来源将隐藏状态拆分为视觉流和文本流。
    3.  **解耦处理**：
        *   **文本路径**：继续通过原始 LLM 剩余的 $L-K$ 层，但这些层在训练时被**冻结**。
        *   **视觉路径**：跳过原始的深层，转而进入一个**新初始化的、可训练的单层 Transformer 块**。
    4.  **拼接与预测**：将处理后的两路特征重新拼接，送入原始的语言模型头（LM Head）进行预测。
*   **参数效率**：训练期间仅更新这个新增的单层 Transformer 块，参数量仅占总体的约 3.1%。

### 3. 实验设计
*   **基座模型**：以 LLaVA-1.5 为框架，使用 Vicuna-7B 作为语言模型背骨，CLIP ViT-L/14 (336px) 作为视觉编码器。
*   **数据集**：使用 LLaVA-1.5-665K 视觉指令微调数据集。
*   **Benchmark（基准测试）**：
    *   **MME**：评估感知（Perception）和认知（Cognition）能力。
    *   **POPE**：评估物体幻觉（Object Hallucination）。
    *   **ChartQA**：评估图表理解和逻辑推理。
*   **对比方法**：
    *   **LLaVA-1.5-7B**：全参数微调（性能上限参考）。
    *   **Normal Training**：采用相同的拆分架构，但所有参数（包括两路深层路径）均参与训练。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   但文中强调了 DVP 相比全参数微调实现了约 **32倍** 的可训练参数量减少（从 7B 降至约 0.2B），暗示了其对显存和计算时间的显著节省。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流多模态基准上进行了对比，并提供了 MME 的细分任务得分。
*   **消融与分析**：对比了“全参数训练的拆分架构”与“DVP（冻结+单层替换）”，证明了冻结原始权重具有正则化作用。
*   **充分性评价**：实验设计较为客观，直接对比了全量微调的 SOTA 基准。但**消融实验略显单薄**，例如缺乏对拆分点 $K$ 的系统性消融（仅凭经验选在中间层），也未在不同规模（如 13B, 70B）的模型上验证泛化性。

### 6. 主要结论与发现
*   **性能保持**：DVP 在仅训练 3.1% 参数的情况下，MME 感知得分达到全量微调的 96.3%。
*   **抑制幻觉**：在 POPE 测试中，DVP 表现优于全量微调（0.8619 vs 0.8577），说明解耦路径能减少文本偏见对视觉理解的干扰。
*   **表征冗余**：实验证明，用一个 Transformer 块替换视觉路径中的 15 个解码器层是可行的，揭示了 MLLM 视觉处理中存在巨大的结构冗余。

### 7. 优点（亮点）
*   **极高的效率**：通过结构化替换而非简单的 Adapter，实现了极低的可训练参数占比。
*   **抗幻觉特性**：通过物理上的路径隔离，减少了语言先验对视觉感知的“污染”。
*   **模块化潜力**：这种设计允许未来针对不同模态（如音频、视频）插入专门的轻量级“表示适配器”，而无需改动核心语言能力。

### 8. 不足与局限
*   **复杂推理受限**：在 ChartQA 等需要高频跨模态交互的任务中，DVP 表现虽优于基准但弱于全参数训练的拆分模型，说明深层的完全隔离限制了复杂的迭代推理。
*   **超参数敏感性**：拆分点 $K$ 的选择对性能至关重要，目前缺乏自动化的选择机制。
*   **评估覆盖面**：未在 COCO Captioning、VQAv2 等更广泛的任务上进行测试，且缺乏在最新开源模型（如 Llama-3）上的验证。

（完）
