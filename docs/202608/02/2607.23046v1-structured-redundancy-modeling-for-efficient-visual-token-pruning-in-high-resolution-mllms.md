---
title: Structured Redundancy Modeling for Efficient Visual Token Pruning in High-Resolution MLLMs
title_zh: 高分辨率 MLLM 中高效视觉 Token 剪枝的结构化冗余建模
authors: "Jouwon Song, Woohyeong Kim, Kyeongbo Kong"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23046v1"
tags: ["query:vlm-da"]
score: 6.0
evidence: 高分辨率多模态大语言模型 (MLLM)
tldr: 针对高分辨率多模态大模型（MLLM）中视觉标记过多导致的推理延迟问题，本文提出了一种名为 SFPruner 的单次前向剪枝框架。该方法通过结构化重构将冗余控制直接嵌入评分空间，取代了传统的迭代式子集优化。SFPruner 利用语义引导的岭杠杆方案和基于排名的定向掩码，在保持高性能的同时显著降低了标记选择的计算开销，实现了真正的推理加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23046v1/fig-001.webp\", \"caption\": \"Table 3: Main Results on Video MLLM Benchmarks. Performance comparison using the LLaVA-Video-7B model across multiple frames. Relative performance (Rel. (%)) is normalized to the unpruned baseline. Light blue rows indicate heuristic-based methods, and light green rows represent optimizationbased methods.\", \"page\": 11, \"index\": 1, \"width\": 677, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-002.webp\", \"caption\": \"Fig. 1:Conceptual comparison of token selection frameworks. (a)Heuristic (Top-K) relies on isolated scores, lacking structural redundancy control. (b) Traditional sequential search mitigates redundancy but introduces iterative dependencies. (c) Our framework enables efficient single-pass selection via SG-RLS and directional masking, eliminating iteration overhead.\", \"page\": 2, \"index\": 2, \"width\": 652, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-003.webp\", \"caption\": \"Table 2: Main Results on Qwen2.5-VL-7B. Performance comparison across various token pruning methods at different retention ratios. Relative performance (Rel. (%)) is normalized to the unpruned baseline. Light blue rows indicate heuristic-based methods, and light green rows represent optimizationbased methods.\", \"page\": 10, \"index\": 3, \"width\": 681, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-004.webp\", \"caption\": \"Fig. 3: End-to-end latency and peak memory analysis across varying numbers of input tokens (N). While existing token pruning methods (DivPrune, CDPruner) incur severe time overheads as sequence length increases, ours maintains exceptionally low overhead even at a high resolution of N = 16384. It closely approaches the pure inference time of the 16K baseline (1,531 ms), delivering practical computational acceleration. Bar charts indicate total latency, and line plots represent peak memory.\", \"page\": 14, \"index\": 4, \"width\": 616, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-005.webp\", \"caption\": \"Table 1: Performance comparison on LLaVA-NeXT-7B at different token retention budgets. Relative performance (Rel.) is normalized to the unpruned vanilla model. Light blue rows indicate heuristic-based methods, and light green rows indicate optimization-based methods.\", \"page\": 9, \"index\": 5, \"width\": 681, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-006.webp\", \"caption\": \"Fig. 2: Pipeline of the non-iterative token pruning framework. (Top) Semantics-Guided RLS (SG-RLS): The semantic guidance (Sguide) is modulated by the leverage score map (ℓ) to suppress redundant background features, yielding a covariance-aware score (SSG-RLS). (Bottom) Ranking-Based Directional Masking: A ranking mask (M) is applied to the pairwise similarity matrix. Tokens receive a directional suppression penalty (Pi) based on their maximum similarity to higher-priority competitors, extracting the final pruned subset through single-pass tensor operations.\", \"page\": 5, \"index\": 6, \"width\": 690, \"height\": 398}]"
motivation: 现有的视觉标记剪枝方法依赖迭代优化，在高分辨率场景下选择开销巨大，导致理论上的计算量减少难以转化为实际的推理加速。
method: 提出 SFPruner 框架，通过语义引导的岭杠杆方案抑制协方差层面的冗余，并结合基于排名的定向掩码通过并行张量操作消除标记间的残余重叠。
result: 在 Qwen2.5-VL 模型上，该方法将 512 个标记的选择耗时从 112.4 毫秒大幅缩减至 2.5 毫秒，同时在极高压缩率下仍保持了优异的性能。
conclusion: 该研究通过结构化冗余建模成功解决了视觉标记剪枝的效率瓶颈，为实现高效的高分辨率多模态模型推理提供了有力支持。
---

## 摘要
近期的高分辨率多模态大语言模型 (MLLM) 在每次输入时会生成数千个视觉 Token，导致视觉 Token 爆炸并引入严重的延迟瓶颈。虽然 Token 剪枝缓解了这一问题，但最先进的子集优化方法通常依赖于迭代式子集构建，以共同捕捉视觉多样性和指令相关性。随着视觉 Token 数量的增加，这种顺序依赖性引入了显著的选择开销，严重限制了理论上的 FLOPs 减少转化为实际推理速度提升的效果。为了解决这一局限性，我们提出了 Single-Forward Pruner (SFPruner)，这是一种视觉 Token 剪枝的结构化重构，它将冗余控制直接嵌入到评分空间中，从而绕过了对迭代组合优化的需求。我们的非迭代框架通过两种互补机制，在单次前向传递中实现了冗余感知的显著性选择。首先，为了在协方差层面减弱冗余，我们引入了一种语义引导的岭杠杆 (ridge leverage) 方案。通过整合指令相关性和视觉显著性，该机制抑制了主导协方差方向并减轻了表示偏差。其次，基于排名的定向掩码通过非对称相似性竞争解决残余重叠，其中高分 Token 通过并行张量操作显式抑制冗余的低分备选 Token。广泛的评估表明，我们的方法保持了稳定的选择成本，在 Qwen2.5-VL 中，当保留 512 个 Token 时，将 Token 选择过程从 112.4 ms 缩短至仅 2.5 ms，减少了高达 110 ms。这种结构化效率成功地将理论上的 Token 减少转化为切实的推理加速，同时在激进压缩下保持了与最先进技术相比极具竞争力的性能。

## Abstract
Recent high-resolution Multimodal Large Language Models (MLLMs) generate thousands of visual tokens per input, leading to a visual token explosion that introduces severe latency bottlenecks. While token pruning mitigates this issue, state-of-the-art subset-optimization methods typically rely on iterative subset construction to jointly capture visual diversity and instruction relevance. As visual token counts scale, this sequential dependency introduces significant selection overhead, severely limiting the translation of theoretical FLOPs reductions into actual wall-clock speedups. To address this limitation, we propose Single-Forward Pruner (SFPruner), a structural reformulation of visual token pruning that embeds redundancy control directly into the scoring space, bypassing the need for iterative combinatorial optimization. Our non-iterative framework achieves redundancy-aware importance selection in a single forward pass through two complementary mechanisms. First, to attenuate redundancy at the covariance level, we introduce a semantics-guided ridge leverage scheme. By integrating instruction relevance and visual saliency, this mechanism suppresses dominant covariance directions and mitigates representation bias. Second, ranking-based directional masking resolves residual overlap through asymmetric similarity competition, where higher-scoring tokens explicitly suppress redundant lower-scoring alternatives via parallel tensor operations. Extensive evaluations demonstrate that our approach maintains stable selection costs, reducing the token selection process by up to 110 ms, from 112.4 ms to just 2.5 ms at 512 tokens in Qwen2.5-VL. This structural efficiency successfully translates theoretical token reductions into tangible inference speedups while preserving highly competitive performance against state-of-the-art techniques under aggressive compression.