---
title: "What CLIP Knows but Cannot Say: Recovering Negation from Frozen Intermediate Features"
title_zh: CLIP 知道但无法表达：从冻结的中间特征中恢复否定语义
authors: "Chen-Yi Lu, Yueh-Shao Chen, Somali Chaterji"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23271v1"
tags: ["query:vlm-da"]
score: 8.0
evidence: CLIP视觉语言模型特征的分析与修正
tldr: CLIP模型在处理否定词（如“not a dog”）时存在表征坍缩问题，即最终层为了视觉对齐而丢失了中间层构建的语法信息。本文提出PeakPatch，一种轻量级的后处理修正系统，通过在编码器的“组合峰值”层提取否定信号并注入最终嵌入，在不改变预训练权重的情况下显著提升了CLIP对否定语义的理解能力，并在多项基准测试中超越了微调方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23271v1/fig-001.webp\", \"caption\": \"Table 1: Negation MCQ accuracy (%) on NegBench [3]. Aff, Neg, and Hyb denote affirmation, negation, and hybrid template types; Avg is computed over all samples across template types. All methods use CLIP ViT-B/32 [39] unless noted.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-002.webp\", \"caption\": \"Fig. 9: Near-tie qualitative examples. Five MCQ questions where CLIP’s top-2\", \"page\": 26, \"index\": 2, \"width\": 725, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-003.webp\", \"caption\": \"Fig. 3: Method overview. With all CLIP parameters frozen, we extract hidden states at three layers identified by the LCD analysis (Sec. 3.2): anchor la, compositional peak lp, and final layer L. The ECN (Sec. 3.3) attends over the full token sequence at lp with a learned query, fuses the result with peak- and anchor-layer embeddings, and predicts a deviation vector δ added to the collapsed layer-L embedding. The SCN (Sec. 3.3) contrasts the corrected embedding f̂T with the lp representation and the image embedding fI to predict a bounded scalar correction ∆s to the cosine similarity. Both modules are trained jointly (Sec. 3.4) while all CLIP parameters remain frozen.\", \"page\": 7, \"index\": 3, \"width\": 616, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-004.webp\", \"caption\": \"Fig. 5: t-SNE of text embeddings for 10 object categories. Circles: affirmative captions; crosses: negated captions; colors: object categories. Left : original CLIP embeddings (affirmative and negated overlap within each object). Right : after ECN correction (affirmative captions stay in tight per-category clusters; negated captions separate into a looser shared region).\", \"page\": 14, \"index\": 4, \"width\": 519, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-005.webp\", \"caption\": \"Table 9: Negation MCQ accuracy (%) comparing parser-dependent methods with PeakPatch on NegBench [3]. All methods use ViT-B/32 architecture.\", \"page\": 23, \"index\": 5, \"width\": 699, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-006.webp\", \"caption\": \"Table 2: Negation retrieval (Text→Image, %) on NegBench [3]. R@k: Recall@k on negated queries. All methods use CLIP ViT-B/32 [39] unless noted. FT= encoder fine-tuning; PH=post-hoc (frozen backbone).\", \"page\": 12, \"index\": 6, \"width\": 725, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2607.23271v1/fig-007.webp\", \"caption\": \"Table 14: Architecture details for the ECN and SCN modules (CLIP ViT-B/32, d=512).\", \"page\": 27, \"index\": 7, \"width\": 608, \"height\": 313}]"
motivation: 发现CLIP在最终层为了视觉对齐而牺牲了中间层已建立的语法结构，导致其无法区分语义相反的否定表达。
method: 提出PeakPatch系统，利用嵌入修正网络（ECN）和分数修正网络（SCN）从中间层提取否定信号并重新注入最终表征。
result: "在NegBench测试中，PeakPatch在COCO MCQ上比原始CLIP提升了35.1%，且在参数量仅增加3.5%的情况下优于所有微调基准。"
conclusion: 证明了冻结的CLIP中间层保留了被最终层遗忘的语法信息，通过轻量级插件即可有效恢复其否定理解能力并推广至下游任务。
---

## 摘要
对比式视觉语言模型（如 CLIP）将语义相反的短语（例如“一只狗”与“不是一只狗”）映射到几乎相同的嵌入向量，使其对否定词不敏感。我们将这一失败归因于一种被称为“表示崩溃”（Representational Collapse）的现象：通过追踪 CLIP 文本编码器中的组合差异和视觉对齐，我们展示了中间层如何构建组合语法，但随着视觉对齐度的提升，最终层使这种结构发生了崩溃，从而产生了对语法盲视的最终表示。为了在不改变预训练权重的情况下恢复丢失的否定信号，我们提出了 PeakPatch，这是一个轻量级的后置修正系统，它在编码器的组合峰值处进行拦截。嵌入修正网络（ECN）利用交叉注意力机制从峰值层提取特定于否定的信号，并以稳定的基准为锚点，预测一个偏差向量，将丢失的语法重新注入到最终层的嵌入空间中。互补的分数修正网络（SCN）则为判别性任务预测有界的标量分数偏移。两个模块在所有 CLIP 参数保持冻结的情况下进行端到端联合训练，仅增加了 520 万个参数（占主干网络的 3.5%），并保留了标准的余弦相似度接口。在 NegBench 上，PeakPatch 在 COCO MCQ 上达到了 74.3%（比 CLIP 高 35.1%，比最佳编码器微调方法高 17.8%），在 VOC MCQ 上达到了 65.5%，同时在完全分布外（OOD）的否定检索任务中优于所有微调基准，尽管其训练参数仅占 3.5%。修正后的嵌入还可以迁移到文本生成图像任务（否定分数提升 18.4），并能泛化到 ViT-B/32、ViT-L/14 和 SigLIP 等主干网络。项目地址：https://stevencylu.github.io/PeakPatch/。

## Abstract
Contrastive vision-language models such as CLIP map semantically opposite phrases (e.g., "a dog" vs. "not a dog") to nearly identical embeddings, rendering them insensitive to negation. We attribute this failure to a phenomenon we call Representational Collapse: by tracking compositional divergence and visual alignment across the CLIP text encoder, we show that middle layers build compositional syntax, but the final layers collapse this structure as visual alignment rises, producing a syntax-blind final representation. To recover the lost negation signal without altering pretrained weights, we propose PeakPatch, a lightweight post-hoc correction system that intercepts the encoder at its compositional peak. An Embedding Correction Network (ECN) uses cross-attention to extract a negation-specific signal from the peak layer, anchored to a stable baseline, and predicts a deviation vector that re-injects the lost syntax into the final-layer embedding space. A complementary Score Correction Network (SCN) predicts bounded scalar score offsets for discriminative tasks. Both modules are trained jointly end-to-end while all CLIP parameters remain frozen, adding only 5.2M parameters (3.5% of the backbone) and preserving the standard cosine similarity interface. On NegBench, PeakPatch achieves 74.3% on COCO MCQ (+35.1 over CLIP, +17.8 over the best encoder fine-tuning method) and 65.5% on VOC MCQ, while outperforming all fine-tuning baselines on fully out-of-distribution negation retrieval despite training only 3.5% of the parameters. The corrected embeddings also transfer to text-to-image generation (+18.4 negation score) and generalize across ViT-B/32, ViT-L/14, and SigLIP backbones. Project URL: https://stevencylu.github.io/PeakPatch/.

---

## 论文详细总结（自动生成）

这是一份关于论文《What CLIP Knows but Cannot Say: Recovering Negation from Frozen Intermediate Features》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了视觉语言模型（VLM）如 CLIP 在处理**否定语义**（如“a dog”与“not a dog”）时的严重失效。尽管 CLIP 在零样本分类和检索中表现优异，但它倾向于将否定句和肯定句映射到几乎相同的嵌入空间，导致其在否定理解任务（如 NegBench）上的表现甚至低于随机水平。
*   **核心发现**：作者发现了一种**“表示崩溃”（Representational Collapse）**现象。CLIP 的中间层实际上构建了区分否定的语法结构，但最终层为了最大化与图像的视觉对齐，主动“丢弃”了这些语法信息，导致最终输出对语法盲视。
*   **研究目标**：在不修改 CLIP 预训练权重、不引入外部大型模型的前提下，从冻结的中间层恢复这些丢失的否定信号。

### 2. 方法论：PeakPatch 系统
作者提出了 **PeakPatch**，一个轻量级的后置修正框架，包含两个核心模块：
*   **层级组合差异（LCD）分析**：引入 LCD 指标量化每一层区分否定句的能力。通过分析发现，否定信号在中间某层（组合峰值层 $l_p$）达到最强，随后在最终层崩溃。
*   **嵌入修正网络（ECN）**：
    *   **核心思想**：从 $l_p$ 层提取否定特征，并将其作为偏差向量注入到最终层嵌入中。
    *   **技术细节**：使用交叉注意力机制（Cross-Attention）让一个可学习的查询（Query）在 $l_p$ 层的全令牌序列中寻找否定信号，并结合一个稳定的“锚点层” $l_a$ 的特征，预测偏差向量 $\delta$。
*   **分数修正网络（SCN）**：
    *   **核心思想**：针对判别性任务（如多选题），预测一个有界的标量偏移量 $\Delta s$，对余弦相似度分数进行微调。
*   **联合训练**：CLIP 保持完全冻结，仅训练 ECN 和 SCN（约 5.2M 参数）。

### 3. 实验设计
*   **数据集与场景**：
    *   **NegBench**：包含 COCO 和 VOC 的多选题（MCQ）及检索任务。
    *   **下游任务**：文本生成图像（使用 GALIP 生成器）、标准检索（MSCOCO 5K, Flickr30K）、零样本分类（CIFAR, ImageNet）。
*   **基准（Benchmark）与对比方法**：
    *   **原始模型**：CLIP (ViT-B/32, ViT-L/14), SigLIP。
    *   **编码器微调方法**：NegCLIP, CoN-CLIP, NegationCLIP 等。
    *   **后置处理方法**：DCSM（基于密集令牌匹配）。
    *   **解析器依赖方法**：SpaceVLM（需调用 Mistral-7B）。

### 4. 资源与算力
*   **GPU 型号与数量**：使用了 **1 张 NVIDIA A100 (40GB)**。
*   **训练时长**：在约 106 万个图像-文本对上训练，总耗时约 **2.7 小时**。
*   **参数量**：新增参数仅 5.2M，占主干网络的 3.5%。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的实验，包括在 3 种不同架构（ViT-B, ViT-L, SigLIP）上的验证、在 2 个主流否定基准上的测试、以及跨任务（检索、MCQ、生成）的评估。
*   **消融实验**：详细测试了 ECN、SCN 各自的贡献、层选择策略（$l_p$ 和 $l_a$ 的影响）以及联合训练的必要性。
*   **客观性与公平性**：作者使用了与对比方法相同的训练数据生成协议（基于 CC12M），并进行了控制变量实验（如 LCD 控制实验，证明该现象仅针对否定词而非同义词），实验设计严谨且客观。

### 6. 主要结论与发现
*   **否定信号确实存在**：CLIP 并非“不懂”否定，而是“说不出来”。中间层（如 ViT-B/32 的第 8 层）保留了丰富的否定语义。
*   **性能飞跃**：PeakPatch 在 COCO MCQ 上比 CLIP 提升了 **35.1%**，在完全分布外（OOD）检索中优于所有全参数微调方法。
*   **保持通用性**：修正后的嵌入在提升否定理解的同时，几乎不损害原始 CLIP 的零样本分类能力和标准检索性能。

### 7. 优点
*   **高效且非侵入式**：无需微调原始模型，保护了预训练权重的通用性，且推理开销极小。
*   **可解释性强**：通过 LCD 曲线直观地揭示了“表示崩溃”现象，为 VLM 的内部机制提供了新见解。
*   **即插即用**：生成的修正嵌入符合标准余弦相似度接口，可直接用于现有的下游生成或检索管线。

### 8. 不足与局限
*   **仅修正文本侧**：目前仅对文本编码器进行修正，图像编码器保持冻结，这导致在某些需要图文深度协同微调的场景（如域内检索）中略逊于全参数微调。
*   **白盒依赖**：需要访问 CLIP 的中间层激活值，无法应用于仅提供 API 接口的黑盒模型。
*   **否定语义的复杂性**：虽然能区分“有”和“无”，但对于否定后的具体补集概念（例如“不是狗”具体指代什么）仍难以精确建模。

（完）
