Title: Cross-Platform Domain Adaptation for Multi-Modal MOOC Learner Satisfaction Prediction

URL Source: https://arxiv.org/pdf/2604.13247v1

Published Time: Thu, 16 Apr 2026 00:09:37 GMT

Number of Pages: 6

Markdown Content:
> 1

# Cross-Platform Domain Adaptation for Multi-Modal MOOC Learner Satisfaction Prediction 

Jakub Kowalski ∗, Magdalena Piotrowska †

Abstract —Learner satisfaction prediction from MOOC reviews and behavioral logs is valuable for course quality improvement and platform operations. In practice, models trained on one platform degrade significantly when deployed on another due to domain shift in review style, learner population, behav-ioral logging schemas, and platform-specific rating norms. We study cross-platform domain adaptation for multi-modal MOOC satisfaction prediction under limited or absent target-platform labels. We propose ADAPT-MS, a platform-adaptive framework that (i) encodes review text with a frozen LLM encoder and behavioral traces with a canonical-vocabulary MLP, (ii) aligns cross-platform representations via domain-adversarial training with gradient reversal, (iii) corrects platform-specific rating bias through a latent-variable calibration layer, and (iv) handles miss-ing behavioral modalities via gated fusion with modality dropout. Experiments on a multi-platform MOOC dataset spanning three major platforms demonstrate that ADAPT-MS achieves target-platform RMSE of 0.66 in the unsupervised setting (zero labeled target samples) and 0.60 with 1000 labeled target samples, outperforming strong baselines including naive pooling, domain-adversarial alignment without calibration, and full fine-tuning. Ablation studies confirm the independent contribution of each component, and few-shot adaptation curves demonstrate stable improvement even with as few as 50 labeled target samples. 

Index Terms —MOOC, learner satisfaction prediction, cross-platform domain adaptation, domain-adversarial training, rating calibration, multi-modal learning, transfer learning, learning analytics 

I. I NTRODUCTION 

# MASSIVE open online courses (MOOCs) have scaled globally, with learners enrolling across diverse plat-forms such as Coursera, edX, and XuetangX [1]–[3]. These platforms accumulate rich feedback signals—star ratings, tex-tual reviews, and behavioral interaction logs—that can be mined to predict and improve learner satisfaction [4]–[6]. Recent advances in pretrained language models [7], [8] and multi-modal fusion have enabled accurate satisfaction predic-tion within a single platform. However, real-world deployment requires models that gen-eralize across platforms. Platform heterogeneity manifests along at least four axes: 1) Linguistic style shift : review text from different plat-forms differs in vocabulary, formality, and cultural ex-pression of sentiment. 2) Population and course mix shift : platforms attract differ-ent learner demographics and subject-area distributions. 3) Behavioral logging schema shift : the same interaction (e.g., “video view”) may be defined differently or logged at different granularities. 4) Rating scale semantics drift : learners on some platforms reserve 5 stars for perfection, while others use 5 stars as the default positive rating, creating systematic bias in predicted satisfaction levels. As a result, a model trained on a source platform S may exhibit substantial degradation when deployed on a target platform T ,even when the underlying satisfaction mechanisms are similar. Existing work on MOOC satisfaction prediction largely ignores cross-platform generalization, evaluating models on held-out samples from the same platform used for training [4], [5], [9]. Classical domain adaptation methods [10]–[12] address distribution shift but do not model label semantics drift or multi-modal missingness jointly. To bridge this gap, we propose ADAPT-MS (Adaptive Domain-Transfer Multi-modal Satisfaction predictor), a uni-fied framework illustrated in Fig. ?? . ADAPT-MS fuses LLM text embeddings and canonical behavioral features, aligns cross-platform representations via a gradient-reversal domain discriminator, corrects platform-specific rating bias with a latent-variable calibration layer, and maintains robustness to missing behavioral modalities through gated fusion and modal-ity dropout. 

Main contributions. This paper makes the following con-tributions: 1) We identify and quantify four key sources of cross-platform shift for MOOC satisfaction prediction: feature distribution shift, behavioral logging gaps, population mix shift, and rating scale semantics drift. 2) We propose ADAPT-MS, a domain-adaptive multi-modal framework that jointly addresses representa-tion alignment and label calibration under a missing-modality-robust fusion architecture. 3) We design a rigorous cross-platform evaluation proto-col (unsupervised, few-shot, and pairwise transfer) and demonstrate consistent improvements over strong base-lines with detailed ablations and sensitivity analyses. The remainder of this paper is organized as follows. Sec-tion II reviews related work. Section III defines the cross-platform adaptation problem. Section IV presents ADAPT-MS in detail. Section V reports experimental results. Section VI concludes the paper. II. R ELATED WORK 

A. MOOC Satisfaction Prediction and Review Mining 

Automated satisfaction mining for MOOCs has been studied using lexicon-based sentiment analysis, aspect-level opinion extraction, and supervised learning on review text [4], [9], [13]. Hew et al. showed that combining sentiment features 

> arXiv:2604.13247v1 [cs.CE] 14 Apr 2026 2

with gradient-boosted classifiers achieves competitive satisfac-tion prediction [5]. Behavioral traces have also been used to augment text-based models, exploiting the complementarity of interaction logs and textual feedback [14], [15]. However, virtually all existing work evaluates within a single platform, leaving cross-platform generalization unaddressed. 

B. Transfer Learning and Domain Adaptation 

Pan and Yang surveyed transfer learning and highlighted covariate shift as the dominant challenge for cross-domain deployment [10]. Transfer Component Analysis (TCA) aligns feature distributions via kernel-based moment matching [11]. Domain-adversarial neural networks (DANN) by Ganin et al. 

introduced gradient reversal to learn domain-invariant features [12], and subsequent deep domain adaptation surveys have extended these ideas to a wide range of architectures [16]. In educational data mining, transfer learning has been applied to knowledge tracing and dropout prediction [10], but cross-platform satisfaction prediction with label calibration has not been systematically studied. 

C. Label Shift and Output Calibration 

Label shift—where the marginal label distribution p(y)

differs between source and target—is distinct from covariate shift [17]. Calibration methods such as temperature scaling and Platt scaling correct prediction confidence [18]. Rating scale differences in satisfaction surveys are a structured form of label shift that is amenable to affine calibration. We leverage this structure to design a lightweight but effective calibration layer that can be adapted with very few labeled target samples. 

D. Multi-Modal Learning with Missing Modalities 

Multi-modal fusion [19] typically assumes all modalities are available at both training and inference. Missing modality robustness has been studied in vision-language models and clinical prediction [20], where modality dropout and gating mechanisms provide effective solutions. In MOOC settings, behavioral logs are frequently absent on the target platform or incompatible with the source schema, making missing-modality robustness essential for cross-platform deployment. 

E. LLM Representations for Short Text 

BERT [7] and RoBERTa [8] provide powerful contextual representations for short, noisy text. Freezing the pretrained encoder and training only a task-specific head is competitive in low-resource settings and reduces the risk of source-platform overfitting, which is particularly important when target data are scarce. III. P ROBLEM FORMULATION 

Let P = {1, . . . , P } index a set of MOOC platforms. Each enrollment instance from platform p is a tuple (ri, bi, y i, p i),where ri is the review text, bi ∈ Rdb is the behavioral feature vector, yi ∈ [1 , 5] is the satisfaction label, and pi ∈ P is the platform indicator. 

Definition 1 (Cross-Platform Shift) . Let Dp = {(ri, bi, y i)}

denote the distribution on platform p. Cross-platform shift is the joint discrepancy DS̸ = DT , encompassing feature distribution shift in (ri, bi), missing-modality patterns, and label semantics shift in yi.

Definition 2 (Cross-Platform Adaptation) . Given labeled source data {(ri, bi, y i)} from platforms S ⊂ P and (possibly unlabeled) target data {(rj , bj )} from target platform T /∈ S ,learn a predictor f that minimizes expected target-platform error: 

min  

> f

E(r, b,y )∼D T

ℓ(f (r, b), y ), (1) 

using only the source labels and unlabeled (or few-labeled) target instances. 

We evaluate three adaptation regimes: (i) unsupervised (k =0 labeled target samples), (ii) few-shot (k ∈ { 50 , 200 , 1000 }), and (iii) pairwise transfer (train on one source platform, test on another). IV. P ROPOSED METHOD : ADAPT-MS 

A. Multi-Modal Encoder with Canonical Behavior Features 1) Text Encoder: Review text ri is encoded using a frozen pretrained LLM (RoBERTa-base [8]). We extract the [CLS] 

token representation: 

hi = PLM( ri)[CLS] ∈ Rdllm . (2) Freezing the encoder reduces the risk of overfitting to source-platform vocabulary while retaining general linguistic repre-sentations suitable for cross-platform transfer. 

2) Canonical Behavioral Features: Because platforms log different low-level event types, we map raw behavioral logs to a platform-agnostic canonical feature vector: 

bcan  

> i

= vi, q i, f ri , f wi , d i, ρ i

 ∈ Rdb , (3) where vi = minutes watched, qi = quiz attempts, f ri = forum reads, f wi = forum posts, di = active days, and ρi = video rewatch rate. These six aggregates are computable from any standard MOOC logging schema. A two-layer MLP ϕ with batch normalization maps bcan  

> i

to a d-dimensional behavioral embedding gi = ϕ(bcan  

> i

).

3) Gated Fusion with Modality Dropout: Let mi ∈ { 0, 1}

indicate whether behavioral logs are available for instance i.A scalar gate controls the behavioral contribution: 

αi = σ w⊤ 

> g

[hi; mi] + bg

 , (4) and the fused representation is 

zi = hi; αi gi

. (5) During training, we apply modality dropout by randomly zeroing the behavioral modality with probability pmod = 0 .3,forcing the model to rely on text alone for a fraction of training steps. This prevents degenerate behavior when gi is unavailable at inference on the target platform. 3

B. Domain-Adversarial Representation Alignment 

To encourage platform-invariant representations, we train a domain discriminator Dψ to predict the platform label 

pi ∈ { 1, . . . , P } from the shared representation zi. A gradient reversal layer (GRL) [12] is inserted between the encoder and the discriminator, which multiplies gradients by −λ during backpropagation. The composite objective is: 

Ltotal = Ltask (f (zi), y i) − λ Ldom (Dψ (zi), p i), (6) where Ltask is the mean squared error (MSE) on labeled source data, and Ldom is the cross-entropy for the platform classification task. The alignment weight λ is treated as a hyperparameter tuned on the validation split. 

Theoretical motivation. By the H-divergence bound from domain adaptation theory [21], minimizing the source task loss and the domain divergence simultaneously provides an upper bound on the target task loss under mild assumptions on the hypothesis class. 

C. Platform-Specific Rating Calibration 

Even after representation alignment, label semantics drift causes systematic prediction bias. We model a latent satisfac-tion score si = f (zi) and a platform-specific affine calibration layer: 

ˆyi = cpi (si) = api si + bpi , (7) where (ap, b p) are platform-specific scale and bias parameters. 

1) Supervised Calibration (labeled target): When k > 0

labeled target samples are available, we optimize (aT , b T )

jointly with a small learning rate while keeping the shared encoder frozen. 

2) Unsupervised Calibration (zero labels): When no target labels exist, we estimate calibration parameters via distribution matching. Let ¯s and σs denote the mean and standard deviation of source predictions on the target domain, and let ¯yhist  

> T

and 

σT 

> y

denote the historical target rating statistics (obtainable from platform metadata or audited summary statistics). Then: 

aT = σT

> y

σs

, (8) 

bT = ¯ yhist  

> T

− aT ¯s. (9) This moment-matching approach requires only the first two moments of the target rating distribution, which can often be obtained from platform dashboards or a small audited sample without individual labels. Table I summarizes the key notation. V. E XPERIMENTS 

A. Dataset and Platform Statistics 

We conduct experiments on a multi-platform MOOC dataset covering three major platforms (denoted A, B, and C). Each platform contributes review text, star ratings, and partial be-havioral interaction logs. Table II summarizes platform-level shift diagnostics. The pronounced differences in mean rating (4.10 on B vs. 4.70 on C) and missing behavioral log rates (5% on A vs.                                      

> TABLE I SUMMARY OF KEY NOTATION
> Symbol Meaning
> S,TSource and target platforms
> riReview text of instance i
> bcan
> iCanonical behavioral feature vector
> hiLLM text embedding
> giBehavioral MLP embedding
> αiModality gate scalar
> ziFused multi-modal representation
> siLatent satisfaction score
> ˆyiCalibrated satisfaction prediction
> (ap, b p)Platform-specific calibration parameters
> λDomain alignment weight
> miBehavioral modality availability indicator TABLE II CROSS -P LATFORM SHIFT DIAGNOSTICS
> Platform Mean Rating Avg. Review Length Missing Behav.
> A (source) 4.35 ±0.72 28 tokens 5% B (source) 4.10 ±0.85 35 tokens 12% C (target) 4.70 ±0.55 18 tokens 40%

40% on C) motivate both the calibration and gated fusion components of ADAPT-MS. Overall, the dataset comprises 480,000 enrollments, 95M behavioral events, and 1.8M review snippets across all platforms. 

B. Evaluation Protocols 

We evaluate under three protocols:  

> •

Unsupervised transfer : Train on A+B, test on C with zero labeled target samples ( k = 0 ).  

> •

Few-shot adaptation : Same split, with 

k ∈ {50 , 200 , 1000 } labeled target samples used to update calibration parameters only.  

> •

Pairwise transfer : Train on A, test on B, and vice versa, to assess sensitivity to specific platform pairs. All experiments use time-based splits within each platform (earliest 70% for training, next 15% for validation, most recent 15% for testing) to prevent temporal leakage. 

C. Baselines 

We compare ADAPT-MS against the following baselines:  

> •

Source-Only : Train on source platforms with no adapta-tion.  

> •

Pool-NoAdapt : Pool all source data without domain labels; no adaptation.  

> •

Fine-Tune : Initialize from source model and fine-tune all parameters on k labeled target samples.  

> •

DANN-Only : Domain-adversarial alignment [12] without calibration.  

> •

ADAPT-MS (Ours) : Full framework. 4                                             

> TABLE III UNSUPERVISED CROSS -P LATFORM TRANSFER TO PLATFORM C ( k= 0
> TARGET LABELS ;↓LOWER IS BETTER )
> Model RMSE ↓MAE ↓
> Source-Only (A only) 0.79 ±0.01 0.62 ±0.01 Pool-NoAdapt (A+B) 0.76 ±0.01 0.60 ±0.01 DANN-Only [12] 0.70 ±0.01 0.55 ±0.01
> ADAPT-MS (Ours) 0.66 ±0.01 0.52 ±0.01
> TABLE IV FEW -S HOT ADAPTATION PERFORMANCE (RMSE ON PLATFORM C; ↓
> LOWER IS BETTER )
> Model k= 0 k= 50 k= 200 k= 1000
> Fine-Tune (all params) 0.79 0.73 0.69 0.66
> ADAPT-MS (calib+gate only) 0.66 0.63 0.61 0.60

D. Implementation Details 

ADAPT-MS uses RoBERTa-base [8] (frozen) as the text encoder ( dllm = 768 , projected to d = 256 ). The behavioral MLP has two hidden layers of size 128 with ReLU and batch normalization. The fusion hidden size is df = 512 . The do-main discriminator is a two-layer MLP with gradient reversal. We train with the Adam optimizer [22], learning rate 3×10 −4,batch size 256, and early stopping on validation RMSE. The alignment weight λ is tuned over {0.0, 0.1, 0.3, 0.5, 1.0}. For few-shot adaptation, only the calibration parameters (aT , b T )

and the gating network are updated, keeping the shared encoder frozen for stability. All results are averaged over 3 random seeds. 

E. Main Results: Unsupervised Transfer 

Table III reports RMSE and MAE on the target platform C under unsupervised adaptation ( k = 0 ). ADAPT-MS achieves a 16% relative RMSE reduction over Pool-NoAdapt and a 5.7% reduction over DANN-Only: 

∆RMSE = 0.70 − 0.66 0.70 ≈ 5.7% . (10) The gap between DANN-Only and ADAPT-MS is primarily attributed to the calibration layer correcting the systematic over-prediction bias caused by Platform C’s inflated 5-star usage (mean rating 4.70 vs. 4.10 on B). 

F. Few-Shot Adaptation Curve 

Table IV reports RMSE as the number of labeled target samples k increases, comparing fine-tuning all parameters against updating only the calibration layer and gate. Two observations stand out. First, at k = 0 ADAPT-MS already achieves 0.66, whereas full fine-tuning collapses to 0.79 (the source-only baseline) without any target labels. Second, the gap narrows as k increases, with both methods converging near 0.60–0.66 at k = 1000 . This confirms that updating only the lightweight calibration parameters is both data-efficient and stable.                                   

> TABLE V SENSITIVITY TO DOMAIN ALIGNMENT WEIGHT λ(RMSE ON PLATFORM
> C; ↓LOWER IS BETTER )
> λ0.0 0.1 0.5 1.0 Target RMSE 0.76 0.70 0.66 0.68 TABLE VI ABLATION STUDY ON PLATFORM C (RMSE, k= 0 ;↓LOWER IS BETTER )
> Model Variant RMSE ↓
> Full ADAPT-MS 0.66
> w/o calibration layer 0.71 w/o domain-adversarial alignment 0.74 w/o modality dropout / gating 0.70 w/o canonical behavior features 0.73 Text-only (no behavior, no alignment) 0.76

G. Sensitivity to Alignment Weight 

Table V shows target-platform RMSE as a function of the domain alignment weight λ.Performance peaks at λ = 0 .5: too little alignment ( λ = 0 .1)fails to remove platform-specific features, while too strong alignment ( λ = 1 .0) suppresses predictive signals that are common across platforms but superficially correlated with platform identity. 

H. Ablation Study 

Table VI isolates the contribution of each ADAPT-MS component on Platform C under unsupervised transfer. The calibration layer and domain-adversarial alignment pro-vide the two largest contributions (RMSE +0.05 and +0.08 respectively when removed). Modality dropout and gating are particularly important for Platform C, where 40% of instances lack behavioral logs: removing these components increases RMSE by 0.04. 

I. Pairwise Transfer Analysis 

Table VII reports pairwise transfer results between all three platform pairs, confirming that ADAPT-MS consistently outperforms the no-adaptation baseline regardless of transfer direction. The largest gain (13.2% RMSE reduction) is observed on A+B →C, where both distribution shift and rating scale shift are most pronounced. The A →B transfer shows smaller gains because the two platforms share a more similar learner population and rating culture. 

J. Failure Mode Analysis 

We identify two primary failure modes in cross-platform transfer: 1) Logging schema gaps : Platform C lacks fine-grained video interaction events (seek/rewind), making the re-watch feature ρi uncomputable. Gated fusion mitigates but does not fully eliminate this gap. 5                               

> TABLE VII PAIRWISE CROSS -P LATFORM TRANSFER RMSE ( ↓LOWER IS BETTER ; P OOL -N OADAPT VS . ADAPT-MS)
> A→BB→CA+B →CModel RMSE MAE RMSE MAE RMSE MAE Pool-NoAdapt 0.74 0.58 0.78 0.61 0.76 0.60
> ADAPT-MS 0.69 0.54 0.70 0.55 0.66 0.52
> Relative gain 6.8% 6.9% 10.3% 9.8% 13.2% 13.3%

2) Domain-specific vocabulary : Platform-specific UI termi-nology (e.g., “XP points”, “nanodegree”) causes spuri-ous correlations in the LLM representations that domain-adversarial training struggles to fully remove, motivating lightweight target-domain language adaptation as future work. VI. C ONCLUSION 

We proposed ADAPT-MS, a cross-platform domain adapta-tion framework for multi-modal MOOC learner satisfaction prediction. By combining domain-adversarial representation alignment, a platform-specific affine calibration layer, and missing-modality- robust gated fusion with modality dropout, ADAPT-MS achieves target-platform RMSE of 0.66 in the zero-label setting and 0.60 with 1000 labeled target samples— outperforming naive pooling, source-only, and alignment-only baselines. Ablation studies confirm that all four components contribute independently, with calibration and adversarial alignment providing the largest gains. The few-shot adaptation curve demonstrates that ADAPT-MS remains effective with as few as 50 labeled target samples, making it practical for real-world platform onboarding scenarios. Future directions include: (i) extending to multilingual cross-platform settings where linguistic style shift is com-pounded by language shift, (ii) handling temporal drift within a single platform (concept drift over cohorts), (iii) exploring online calibration updates using streaming target data, and (iv) connecting cross-platform adaptation to fairness auditing to ensure that calibration does not disproportionately harm underrepresented learner groups on the target platform. APPENDIX 

A. Data Splits and Leakage Control 

Within each platform, course runs are sorted chronologi-cally. Training, validation, and test sets use the earliest 70%, next 15%, and most recent 15% of runs, respectively. For pairwise transfer experiments, the source platform training set is the full chronological 85% split; the target test set is its most recent 15%. 

B. Canonical Feature Computation 

All six canonical behavioral features (Eq. (3)) are computed within a fixed 28-day window from course start. Platform-specific features not in the canonical set are excluded. Missing features are imputed with the within-platform training mean and flagged with the modality indicator mi = 0 .

C. Hyperparameter Search 

For each model variant, we run random search with 20 trials on the validation split of the source platforms. Tuned parameters: learning rate ∈ { 10 −4, 3 × 10 −4, 10 −3}, dropout 

∈ { 0.1, 0.2, 0.3}, fusion hidden size ∈ { 256 , 512 }, and align-ment weight λ ∈ { 0.0, 0.1, 0.3, 0.5, 1.0}. The same budget is used for all variants. 

D. Computational Budget 

ADAPT-MS trains in approximately 1.5 hours per fold on a single NVIDIA A100 GPU (40 GB). Text embeddings are pre-computed and cached (approximately 30 minutes per platform). Calibration-only few-shot adaptation requires less than 5 minutes per k setting. REFERENCES [1] J. Reich and J. Ruiperez-Valiente, “The mooc pivot,” Science , vol. 363, no. 6423, pp. 130–131, 2019. [2] D. Shah, “By the numbers: Moocs in 2020,” Class Central Report , 2020. [3] H. Chi, C. Qi, S. Wang, and Y. Ma, “Active learning for graphs with noisy structures,” in Proceedings of the 2024 SIAM International Conference on Data Mining (SDM) . SIAM, 2024, pp. 262–270. [4] C. Qi and S. Liu, “Evaluating on-line courses via reviews mining,” IEEE Access , vol. 9, pp. 35 439–35 451, 2021. [5] K. F. Hew, X. Hu, C. Qiao, and Y. Tang, “What predicts student satisfaction with moocs: A gradient boosting trees supervised machine learning and sentiment analysis approach,” Computers & Education , vol. 145, p. 103724, 2020. [6] O. Almatrafi and A. Johri, “Systematic review of discussion forums in massive open online courses (moocs),” IEEE Transactions on Learning Technologies , vol. 12, no. 3, pp. 413–428, 2019. [7] J. Devlin, M.-W. Chang, K. Lee, and K. Toutanova, “Bert: Pre-training of deep bidirectional transformers for language understanding,” in NAACL ,2019. [8] Y. Liu, M. Ott, N. Goyal et al. , “Roberta: A robustly optimized bert pretraining approach,” arXiv preprint arXiv:1907.11692 , 2019. [9] A. Onan, “Sentiment analysis on massive open online course evaluations: A text mining and deep learning approach,” Computer Applications in Engineering Education , vol. 29, no. 3, pp. 572–589, 2020. [10] S. J. Pan and Q. Yang, “A survey on transfer learning,” IEEE Transac-tions on Knowledge and Data Engineering , vol. 22, no. 10, pp. 1345– 1359, 2010. [11] S. J. Pan, I. W. Tsang, J. T. Kwok, and Q. Yang, “Domain adaptation via transfer component analysis,” IEEE Transactions on Neural Networks ,vol. 22, no. 2, pp. 199–210, 2011. [12] Y. Ganin, E. Ustinova, H. Ajakan, P. Germain, H. Larochelle, F. Lavio-lette, M. Marchand, and V. Lempitsky, Domain-Adversarial Training of Neural Networks . Springer, 2017, pp. 189–209. [13] Z. Kastrati, “Aspect-based opinion mining of students’ reviews,” Inter-national Conference on Computing , 2020. [14] K. F. Hew, “Promoting engagement in online courses: What strategies can we learn from three highly rated moocs,” British Journal of Educational Technology , vol. 47, no. 2, pp. 320–341, 2016. [15] X. Peng et al. , “Investigating learners’ behaviors in moocs,” Computers & Education , 2020. 6

[16] M. Wang and W. Deng, “Deep visual domain adaptation: A survey,” 

Neurocomputing , vol. 312, pp. 135–153, 2018. [17] Z. C. Lipton, Y.-X. Wang, and A. Smola, “Detecting and correcting for label shift with black box predictors,” in ICML , 2018, pp. 3122–3130. [18] J. C. Platt, “Probabilistic outputs for support vector machines and comparisons to regularized likelihood methods,” in Advances in Large Margin Classifiers , 1999, pp. 61–74. [19] T. Baltruˇ saitis, C. Ahuja, and L.-P. Morency, “Multimodal machine learning: A survey and taxonomy,” IEEE Transactions on Pattern Analysis and Machine Intelligence , vol. 41, no. 2, pp. 423–443, 2019. [20] M. Ma, J. Ren, L. Zhao, S. Tulyakov, C. Wu, and X. Peng, “SMIL: Multimodal learning with severely missing modality,” in AAAI , 2021, pp. 2302–2310. [21] S. Ben-David, J. Blitzer, K. Crammer, A. Kulesza, F. Pereira, and J. W. Vaughan, “A theory of learning from different domains,” vol. 79, no. 1–2, 2010, pp. 151–175. [22] D. P. Kingma and J. Ba, “Adam: A method for stochastic optimization,” in ICLR , 2015.