---
title: "P3T: Prototypical Point-level Prompt Tuning with Enhanced Generalization for 3D Vision-Language Models"
title_zh: P3T：面向 3D 视觉-语言模型的增强泛化性原型点级提示微调
authors: "Geunyoung Jung, Soohong Kim, Kyungwoo Song, Jiyoung Jung"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15703v1"
tags: ["query:vlm-da"]
score: 9.0
evidence: 针对3D视觉语言模型的参数高效提示微调
tldr: 针对预训练3D视觉语言模型（VLM）微调成本高且易过拟合的问题，本文提出原型点级提示微调（P3T）。该方法通过Point Prompter生成实例感知的点级提示，并结合Text Prompter的可学习文本提示，实现了参数高效的任务适配。此外，引入原型损失以增强嵌入空间对齐并减少类内方差。P3T在分类和少样本学习中表现优异，且在跨数据集场景下展现出极强的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15703v1/fig-001.webp\", \"caption\": \"Fig. 5. The t-SNE visualization of the 3D embeddings from PB dataset before (left) and after (right) fine-tuning. Embeddings with the same color correspond to the same category.\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.15703v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the P3T framework. The upper part represents the 3D branch with a Point Prompter, and the lower part corresponds to the text branch with a Text Prompter.\", \"page\": 2, \"index\": 2, \"width\": 1054, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.15703v1/fig-003.webp\", \"caption\": \"Fig. 2. Architecture of Point Prompter. It takes n patches of a point cloud, and generates a prompt token and offsets for both local points and center of each patch. The offsets are added to the target patches to create deformed patches, which are then concatenated to original patches.\", \"page\": 3, \"index\": 3, \"width\": 1054, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.15703v1/fig-004.webp\", \"caption\": \"TABLE I CLASSIFICATION ACCURACIES (%) ON TWO DATASETS. #LP DENOTES THE NUMBER OF LEARNABLE PARAMETERS. ∗ INDICATES THE PERFORMANCE UNDER PPT SETTING.\", \"page\": 4, \"index\": 4, \"width\": 477, \"height\": 615}]"
motivation: 传统的全量微调计算资源消耗大，而现有的提示微调方法在3D视觉语言模型上容易过拟合，限制了其泛化性能。
method: 提出包含点级提示器、文本提示器以及旨在减少类内方差的原型损失函数的参数高效微调框架。
result: 实验结果显示，该方法在分类和少样本学习任务中达到或超过了全量微调的水平，并在数据偏移环境下保持了鲁棒性。
conclusion: P3T通过直接作用于输入数据的双重提示机制，为3D视觉语言模型提供了一种兼顾效率与泛化能力的适配方案。
---

## 摘要
随着 3D 点云领域预训练模型在广泛实际应用中的兴起，将其适配到下游任务变得越来越重要。然而，传统的全量微调方法计算成本高且存储密集。尽管提示微调（prompt tuning）已成为一种高效的替代方案，但它往往面临过拟合问题，从而损害了泛化能力。为了解决这一问题，我们提出了原型点级提示微调（P3T），这是一种专为预训练 3D 视觉-语言模型（VLMs）设计的参数高效提示微调方法。P3T 由两个组件组成：1) 点提示器（Point Prompter），为输入点云生成实例感知的点级提示；2) 文本提示器（Text Prompter），在输入文本中使用可学习的提示而非手工设计的提示。由于这两个提示器直接作用于输入数据，P3T 能够在不牺牲泛化性的情况下实现 3D VLM 的任务特定适配。此外，为了增强嵌入空间对齐（这是微调 3D VLM 的关键），我们引入了原型损失（prototypical loss）以减少类内方差。大量实验表明，我们的方法在分类和少样本学习方面达到或优于全量微调，并在跨数据集设置下的数据偏移中表现出鲁棒的泛化能力。代码可在 https://github.com/gyjung975/P3T 获取。

## Abstract
With the rise of pre-trained models in the 3D point cloud domain for a wide range of real-world applications, adapting them to downstream tasks has become increasingly important. However, conventional full fine-tuning methods are computationally expensive and storage-intensive. Although prompt tuning has emerged as an efficient alternative, it often suffers from overfitting, thereby compromising generalization capability. To address this issue, we propose Prototypical Point-level Prompt Tuning (P$^3$T), a parameter-efficient prompt tuning method designed for pre-trained 3D vision-language models (VLMs). P$^3$T consists of two components: 1) \textit{Point Prompter}, which generates instance-aware point-level prompts for the input point cloud, and 2) \textit{Text Prompter}, which employs learnable prompts into the input text instead of hand-crafted ones. Since both prompters operate directly on input data, P$^3$T enables task-specific adaptation of 3D VLMs without sacrificing generalizability. Furthermore, to enhance embedding space alignment, which is key to fine-tuning 3D VLMs, we introduce a prototypical loss that reduces intra-category variance. Extensive experiments demonstrate that our method matches or outperforms full fine-tuning in classification and few-shot learning, and further exhibits robust generalization under data shift in the cross-dataset setting. The code is available at \textcolor{violet}{https://github.com/gyjung975/P3T}.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **P3T**（Prototypical Point-level Prompt Tuning）的参数高效微调方法，旨在提升 3D 视觉-语言模型（VLM）在下游任务中的性能与泛化能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着大规模预训练 3D 模型（如 ULIP）的出现，如何高效地将这些模型适配到下游任务成为关键。
*   **现有挑战**：
    *   **全量微调（Full Fine-tuning）**：计算和存储成本极高。
    *   **现有提示微调（Prompt Tuning）**：如 IDPT、DAPT 等，通常在模型内部插入可学习参数，这会破坏预训练模型已对齐良好的多模态嵌入空间，导致模型在面对新数据时容易过拟合，泛化性变差。
*   **核心目标**：在保持预训练模型参数完全冻结的前提下，通过在输入空间（点云坐标级和文本输入级）进行提示微调，实现高效适配并增强泛化性。

### 2. 方法论
P3T 框架由三个核心部分组成，所有可学习参数均位于模型外部：

*   **点提示器（Point Prompter）**：
    *   **目标补丁选择**：受 PointNet“关键点”启发，P3T 反其道而行之，识别出贡献度最低的“脆弱补丁（Vulnerable Patches）”。
    *   **变形补丁生成**：使用 `EdgeConv` 提取局部几何特征，生成坐标偏移量（Offsets），将其加到脆弱补丁上形成“变形补丁”，并与原始点云拼接。
    *   **点提示正则化（$L_{reg}$）**：通过约束变形补丁的质心位置和尺寸，防止点云结构发生剧烈漂移。
*   **文本提示器（Text Prompter）**：
    *   **可学习上下文**：将手工设计的模板替换为可学习的向量。
    *   **一致性损失（$L_{con}$）**：为了防止过拟合，引入一致性约束，使可学习提示生成的文本嵌入与手工提示生成的嵌入保持接近，从而保留通用知识。
*   **原型损失（Prototypical Loss, $L_{proto}$）**：
    *   **核心思想**：计算每个类别的平均嵌入作为“原型”。
    *   **作用**：鼓励同类样本的 3D 嵌入向原型靠拢，减少类内方差。这对于处理带有噪声和遮挡的真实扫描数据（如 ScanObjectNN）尤为有效。

### 3. 实验设计
*   **数据集**：
    *   **ModelNet40 (MN40)**：合成数据集，包含 40 个类别。
    *   **ScanObjectNN**：真实扫描数据集，包含 OBJ ONLY、OBJ BG、PB 三种难度。
    *   **Objaverse-LVIS**：用于跨数据集泛化实验的源数据集。
*   **Benchmark 与对比方法**：
    *   **全量微调方法**：Point-BERT, Point-MAE, ULIP-2, RECON 等。
    *   **参数高效微调（PEFT）方法**：IDPT, DAPT, PPT, Point-PRC 等。
*   **实验场景**：3D 对象分类、少样本学习（1/2/4/8/16-shot）、跨数据集泛化。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。
*   **参数量**：论文提到 P3T 仅需约 **2M** 的可学习参数，相比全量微调（约 22M）减少了 91% 的参数更新需求。
*   **输入配置**：默认使用 1,024 个采样点。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集的四个子集上进行了分类性能对比。
    *   进行了详尽的消融实验，验证了 Point Prompter、Text Prompter、各种损失函数（$L_{proto}, L_{reg}, L_{con}$）以及补丁选择策略（脆弱 vs 关键 vs 随机）的有效性。
    *   提供了 t-SNE 可视化分析，直观展示了嵌入空间的聚类效果。
*   **充分性评价**：实验设计较为全面，涵盖了从合成到真实、从全量数据到少样本、从同分布到跨分布的多种场景，客观地证明了该方法在性能与泛化性之间的平衡。

### 6. 主要结论与发现
*   **性能卓越**：P3T 在 MN40 上达到 94.0% 的精度，在 ScanObjectNN 的多个子集上刷新了 SOTA 记录，甚至超过了部分全量微调方法。
*   **泛化性强**：在跨数据集实验中，P3T 在未见过的目标数据集上平均领先基准方法（PPT）达 8.3%，证明了“模型外微调”对保持泛化性的重要性。
*   **脆弱补丁更有用**：实验发现，对不重要的补丁进行提示（Prompting）比对关键补丁进行提示效果更好，因为这能为模型提供互补的信息。

### 7. 优点与亮点
*   **非侵入式设计**：完全冻结预训练模型，通过输入空间的变换实现适配，极大地保护了预训练的知识结构。
*   **双重正则化**：通过点云坐标约束和文本一致性约束，有效地解决了提示微调中常见的过拟合问题。
*   **鲁棒性**：原型损失显著提升了模型在处理噪声和不完整点云（真实扫描数据）时的表现。

### 8. 不足与局限
*   **极端少样本下的不稳定性**：在 PB 数据集的极低样本（如 1-shot, 2-shot）情况下，由于原型构建不准确，性能略逊于部分竞争对手。
*   **计算开销**：虽然参数高效，但 Point Prompter 在推理时需要额外的 `EdgeConv` 计算和补丁变形操作，相比直接输入原始点云会增加一定的计算延迟。
*   **超参数敏感性**：补丁选择比例 $\alpha$ 和各项损失的权重系数（$\beta, \gamma, \lambda$）可能需要针对不同数据集进行精细调优。

（完）
