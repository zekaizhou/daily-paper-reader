---
title: Bi-Level Optimization for Single Domain Generalization
title_zh: 面向单领域泛化的双层优化
authors: "Marzi Heidari, Hanping Zhang, Hao Yan, Yuhong Guo"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06349v1"
tags: ["query:vlm-da"]
score: 8.0
evidence: 用于单领域泛化的领域提示编码器
tldr: 针对单域泛化（SDG）中源域数据单一且分布受限的挑战，本文提出了BiSDG双层优化框架。该框架通过标签保持变换构建代理域来模拟分布偏移，并利用领域提示编码器生成轻量化调制信号以增强特征表示。通过双层优化策略，BiSDG在内层优化任务性能，外层提升跨域泛化能力，并引入梯度近似方案实现高效训练，在多个基准测试中取得了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06349v1/fig-001.webp\", \"caption\": \"Table 2. Classification accuracy and standard deviation results (%) on the four target domains (SVHN, MNIST-M, SYN, and USPS) of the Digits benchmark, with MNIST as the source domain. Best results are in bold font.\", \"page\": 7, \"index\": 1, \"width\": 694, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.06349v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of the BiSDG framework for Single Domain Generalization (SDG). Starting from a labeled source dataset, BiSDG constructs multiple surrogate domains via semantically coherent, label-preserving transformation pipelines to simulate unseen distribution shifts. A domain prompt encoder is introduced to capture domain-specific knowledge, which is deployed to perform FiLM-based feature modulation, supporting generalizable prediction. BiSDG formulates SGD as a bi-level optimization problem: the inner objective updates the model parameters Θ = (θ, ϕ) using the source domain, combining supervised cross-entropy loss Lcl in and adversarial consistency loss Ladv in . The outer objective updates the prompt encoder parameters ω to maximize generalization across surrogate domains.\", \"page\": 4, \"index\": 2, \"width\": 932, \"height\": 369}]"
motivation: 旨在解决在缺乏目标域数据的情况下，如何仅从单个源域学习并泛化到未知领域这一极具挑战性的问题。
method: 提出一种双层优化框架，利用领域提示编码器生成特征调制信号，并通过模拟代理域的分布偏移来解耦任务学习与领域建模。
result: 实验结果表明，BiSDG在多个单域泛化基准任务上均显著优于现有方法，确立了新的性能基准。
conclusion: 研究证明了结合领域提示建模与双层优化策略能有效提升模型在单域泛化场景下的鲁棒性与泛化能力。
---

## 摘要
在训练期间无法访问任何目标数据的情况下，从单个有标签的源领域泛化到未见过的目标领域，仍然是鲁棒机器学习中的一个基本挑战。我们针对这一尚未得到充分探索的设置（称为单领域泛化，SDG），提出了 BiSDG，这是一个显式地将任务学习与领域建模解耦的双层优化框架。BiSDG 通过对源数据进行保持标签的变换来构建代理领域，从而模拟分布偏移。为了捕捉特定领域的上下文，我们提出了一个领域提示编码器，该编码器生成轻量级的调制信号，通过特征级线性调制产生增强特征。学习过程被公式化为一个双层优化问题：内部目标在固定提示下优化任务性能，而外部目标通过更新领域提示编码器来最大化在代理领域上的泛化能力。我们进一步开发了一种实用的梯度近似方案，能够在不使用二阶导数的情况下实现高效的双层训练。在各种 SDG 基准测试上的广泛实验表明，BiSDG 一贯优于先前的方法，在 SDG 设置中达到了新的最先进性能。

## Abstract
Generalizing from a single labeled source domain to unseen target domains, without access to any target data during training, remains a fundamental challenge in robust machine learning. We address this underexplored setting, known as Single Domain Generalization (SDG), by proposing BiSDG, a bi-level optimization framework that explicitly decouples task learning from domain modeling. BiSDG simulates distribution shifts through surrogate domains constructed via label-preserving transformations of the source data. To capture domain-specific context, we propose a domain prompt encoder that generates lightweight modulation signals to produce augmenting features via feature-wise linear modulation. The learning process is formulated as a bi-level optimization problem: the inner objective optimizes task performance under fixed prompts, while the outer objective maximizes generalization across the surrogate domains by updating the domain prompt encoder. We further develop a practical gradient approximation scheme that enables efficient bi-level training without second-order derivatives. Extensive experiments on various SGD benchmarks demonstrate that BiSDG consistently outperforms prior methods, setting new state-of-the-art performance in the SDG setting.